/**
 * 控制器包：界面事件处理与业务控制逻辑（MVC 的 Controller 层）。
 *
 * <p>已实现 {@link BattleController}：持有 {@code model.Battle} 对局实例，
 * 负责开新对局、处理「玩家出招」并为敌方按期望伤害自动决策招式；
 * 不依赖任何 JavaFX 类型，可独立单元测试。
 */
package com.bao01.controller;
