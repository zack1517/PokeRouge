# QA 问题表

> 用途：与"客户"沟通的任何需求/验收问题，**必须**记录于此表，口头沟通不作为依据。
> 每条记录：新增一行并保留日期，问题解决后在"状态"标记。

| 序号 | 日期 | 提出人 | 问题描述 | 客户答复 / 依据 | 影响 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-09-08 | [待确认] | AP 每段上限、段总数、逐段敌人数值与成长曲线是否有官方参考数值？由谁定稿？ | | 流程整体数值 | 待确认 |
| 2 | 2026-09-08 | [待确认] | 初始可选宝可梦（数量/清单）、技能机、道具/复活药清单及价格数据表是否有？ | | 养成/商店 | 待确认 |
| 3 | 2026-09-08 | [待确认] | 性格共有几种？各性格对物攻/物防/特攻/特防/速度/HP 的具体加成与削弱系数表？ | | 性格机制 | 待确认 |
| 4 | 2026-09-08 | [待确认] | 努力值「抓捕越多成长越快」量化规则：按物种累计？每次抓捕 +多少？上限？对面板的实际影响公式？ | | EV 机制 | 待确认 |
| 5 | 2026-09-08 | [待确认] | 同类合并取最高等级后，个体已有努力值/已学技能/已用道具是否继承或重置？ | | 合并规则 | 待确认 |
| 6 | 2026-09-08 | [待确认] | 抓捕成功率公式（血量比例、精灵球种类系数）？大师球是否必定成功、数量/获取是否唯一？ | | 抓捕 | 待确认 |
| 7 | 2026-09-08 | [待确认] | 进化等级条件、升级经验曲线、可学习技能表是否沿用原作数据？ | | 养成 | 待确认 |
| 8 | 2026-09-08 | [待确认] | 伤害/属性克制是否沿用原作（2x / 0.5x、STAB、随机浮动）？包含哪些属性、数量多少？ | | 战斗 | 待确认 |
| 9 | 2026-09-08 | [待确认] | 商店「随进展商品种类与数量越多」的具体规则：按段/次数递增？商品池何时刷新？ | | 经济 | 待确认 |
| 10 | 2026-09-08 | [待确认] | 「野外精灵有概率消耗 0 点」的具体概率？不同阶段是否变化？ | | AP | 待确认 |
| 11 | 2026-09-08 | [待确认] | 路人胜利获金币、各类失败扣金币的具体数值/公式？道馆/四天王重试「大量扣金币」具体数值？金币可否为负？ | | 经济/惩罚 | 待确认 |
| 12 | 2026-09-08 | [待确认] | 医院是否收费？能治愈濒死+受伤全部？每段/进入次数有无限制？ | | 恢复 | 待确认 |
| 13 | 2026-09-08 | [待确认] | 队伍可同时携带/上阵几只？备用（盒子）容量？初始可选几只？ | | 队伍 | 待确认 |
| 14 | 2026-09-08 | [待确认] | 四天王失败一次后重试：整场连打重来还是仅重试该场？扣金币后立即重试还是可先补给？ | | 流程 | 待确认 |
| 15 | 2026-09-08 | [待确认] | 神兽「仅后期」判据（第几段/击败几个道馆后）？神兽战胜利是否可抓捕神兽？ | | 神兽线 | 待确认 |
| 16 | 2026-09-08 | [待确认] | 自回血 1/5 是否涵盖道馆/四天王/Boss 等必然节点？濒死是否指战斗内 HP 归零即判？ | | 恢复 | 待确认 |
| 17 | 2026-09-08 | [待确认] | 首领侵略战是否仅当「开启剧情线且首领未被提前击败」时，在击败冠军后触发？（抓捕事件中击败首领则该分支闭合） | | 剧情线 | 待确认 |
| 18 | 2026-09-08 | [待确认] | 三种结局（正常通关 / 侵略战后通关 / 失败结束）的达成条件与界面提示文案确认？ | | 结局 | 待确认 |
| 19 | 2026-09-11 | UI / 存档（同一人） | 一轮远征结束（通关 / 战败 / 队伍全灭）后，该档位里那份「已结束」的快照如何处置：保留但禁止载入（当前实现，玩家回启动页开新档）、还是结束后直接清空该档？ | 用户要求「肉鸽结束就在初始主界面选择新游戏」，据此实现为**保留快照 + 读档时按 `isRogueRunFinished()` 拒绝载入并提示**；档位列表暂不区分「进行中 / 已结束」 | 读档体验 | 待确认 |
| 20 | 2026-09-11 | UI / 存档（同一人） | 主菜单的「返回主界面」是否需要二次确认？点一下就回启动页（当前实现，离开前会 `autoSave` 落盘，进度不丢，但要重新读档才能回到这一局） | 按「不要直接退出游戏，而是到初始主界面」实现为**无确认直接返回**，退出确认文案仅保留给窗口关闭按钮 | 误触代价 | 待确认 |
| 21 | 2026-09-11 | 成长 / 战斗（同一人） | 击倒一只路人精灵的**升级节奏**：当前经验曲线 `expToNextLevel = 3n²+3n+1`（41 级需 5167 点），击倒一只同段对手只有约 100 点，**约 50 只对手才升 1 级** | **已确认数值偏少，但本轮不调整，交由他人调整**。本轮只补可见反馈（经验确实逐只入账，问题在于无日志）；调节点：`GrowthService` 的 `BASE_EXP_DIVISOR`（当前 7，调小则经验变多）与 `MIN_EXP`（30）。经验曲线与宝可梦系统 `medium-fast`（`n³×0.8`）并存属既有 D-09 / R-03 已知问题 | 升级节奏 / 养成 | 待他人调整 |
| 22 | 2026-09-11 | 商店 / 道具（同一人） | 需求方给出完整道具对照表（26 种球、约 60 件携带物、约 30 种树果），但现有引擎只能表达「固定倍率 / 必定捕获 / 解除异常」三类道具，条件倍率球、携带物被动、树果均无对应机制。本轮要落到什么范围？ | **分阶段落地**：第一阶段只做「球类上架 + 固定倍率球」（已实现：纪念球 / 贵重球 / 狩猎球 / 竞赛球）。纪念球与贵重球在正作中即基准倍率（与精灵球同效），照原样保持 ×3、商店与精灵球同价。条件倍率球（等级球 / 月亮球 / 沉重球等）与携带物、树果留作后续 | 商店丰富度 | 部分完成（后续待做） |
| 23 | 2026-09-11 | 道具 / 树果（同一人） | 树果是否有必要全部纳入？其中「努力值降低」类（榴石果 HP−10 / 藻根果 攻击−10 / 比巴果 防御−10 / 哈密果 特攻−10 / 萄葡果 特防−10 / 茄番果 速度−10，同时增加亲密度）涉及洗努力值与亲密度两条本作尚未实现的系统 | **需求方明确答复：这 6 种树果不需要加入**。其余树果（异常治疗 / HP·PP 回复 / 危机能力提升 / 受击能力提升 / 属性减伤）仍待评估 | 道具池范围 | 已确认（不纳入） |
| 24 | 2026-09-11 | 道具 / 战斗（同一人） | 讲究系（讲究眼镜 / 讲究围巾）在原作里「只能使用第一个招式」，若该招式 PP 耗尽则由「挣扎」兜底；本作**没有挣扎机制**，死锁时玩家将无法出招 | 当前实现为**锁定招式 PP 耗尽后自动解禁**（可改用其他招式），避免卡死。若后续补挣扎机制，应改为「解体后只能挣扎」。**此为实现方自主决策，未经需求方确认** | 讲究系手感 | 待确认 |
| 25 | 2026-09-11 | 存档（同一人） | 存档机制的两处确定性缺陷：①携带物此前**完全不落存档**（读档后装备凭空消失）；②旧验收体系 `com.bao01.save.SaveSystemTest` 偶发失败（`mvn -o test` 约一半概率报 `capture → restore → capture 稳定 -> 二次采样文本不同`），在改动前的基线提交 `0f958de` 上同样偶发（6 次中 2 次通过） | ①已修复：`org.example.save` 的 `PP` 行末新增装备 id 段（见 §15.4），并补 5 个回归用例。②**根因**：旧体系 `PartyEntry` 只存 `物种id / 等级 / 当前HP`，`restore` 走 `GameData.createPokemon` 重新随机个体值 → `maxHp` 变化 → `takeDamage(maxHp − cur)` 落点漂移 → 二次采样文本不同。正式体系（`org.example.save`）存了个体值，无此问题。**本轮不修**旧演示体系（仅测试自测用，不参与正式游戏） | 存档可靠性 / 测试噪声 | ①已修复 ②待处理 |
| 26 | 2026-09-11 | 道具 / 树果（同一人） | 异常治疗树果（樱子果 / 桃桃果 / 莓莓果 / 零余果 / 利木果 / 柿仔果 / 木子果）的**触发时机**：原作只要「持有者处于该异常」即会在回合末自动食用，本作只在**招式刚施加异常成功**时判定 | 当前实现为**仅「刚被招式施加」时触发**——换人上场时已带异常、战斗开始时已带异常、火焰/剧毒宝珠或天气等其它路径施加的异常**均不触发**。原因是本作没有「回合末自动进食」的统一结算钩子，且宝珠自身施加的异常若立刻被树果抵消语义上反而诡异。**此为实现方自主决策，未经需求方确认** | 树果手感 | 待确认 |
| 27 | 2026-09-11 | 道具 / 树果（同一人） | 木子果（`ALL`）是否应当治愈**混乱**？正作中木子果只解除主要异常状态（麻痹/中毒/剧毒/灼伤/睡眠/冰冻），**不含混乱**（混乱由柿仔果单独负责） | 需求方给出的对照表原文为「木子果 \| 治疗所有异常状态」，**按字面实现为包含混乱**（`cureStatuses()` 返回 `StatusCondition` 全集的补集，仅排除 `NONE` 与 `FAINTED`）。若需与正作对齐，改为排除 `CONFUSION` 即可（一行改动） | 道具还原度 | 待确认 |
| 28 | 2026-09-11 | 道具 / 树果（同一人） | 6 种危果树果（勿花果 / 异奇果 / 乐芭果 / 芭亚果 + 对应口味）在正作中附带「**性格不喜该味道时会陷入混乱**」的副作用，本作**无性格 / 口味系统**（`natures.csv` 尚未接入战斗结算，`Pokemon` 也无口味字段） | **副作用未实现**——只保留「HP 低于 1/4 时回复最大 HP 的 1/8」的正面效果，玩家获得纯收益。若后续接入性格与口味，需为树果补「口味 ↔ 性格」判定 | 道具还原度 | 待确认（依赖性格系统） |
| 29 | 2026-09-11 | 道具 / 战斗（同一人） | HP 回复树果（橙橙果 / 文柚果 / 危果树果）的**判定时机**：原作只在**回合末**结算一次，本作同时挂在了「受击后」与「回合末」两个点 | 当前实现为**双触发点**：受击且未倒下后立即判定一次，`applyFieldEndEffects` 末尾（天气 / 异常 / 宝珠结算之后）再判定一次。收益是「被打残立刻吃果」手感更好，副作用是**同一棵树果可能在受击点就被消耗**，回合末不再触发（消耗品，符合直觉）。**此为实现方自主决策，未经需求方确认** | 树果手感 | 待确认 |
| 30 | 2026-09-12 | 道具 / 战斗（同一人） | 批次④「招式标记 / 会心 / 畏缩」中的**会心与畏缩判定时机**：原作里这两个效果在招式威力计算前判定，且「变化类招式」「无效招式」也不产生畏缩 | 当前实现为**只在「招式命中并实际造成伤害到未倒下的目标」之后判定**：变化类招式、0 倍（无效）招式都不参与；会心判定先于 `computeDamage`（每次攻击恰好消耗一次 `random.nextInt(分母)`，不改变既有伤害浮动的随机序列位置）。**此为实现方自主决策，未经需求方确认** | 会心 / 畏缩手感 | 待确认 |
| 31 | 2026-09-12 | 道具 / 战斗（同一人） | 凸凸头盔（`CONTACT_PUNISH`）反伤的三处细节：①装备是否消耗？②能否被攻击方的保命装备（气势披带 / 气势头带）救回？③反伤能否击倒攻击方？ | 当前实现为：①**不消耗**（与正作一致，是常驻被动）；②**不受保命装备保护**——`survivedByHeldItem` 只在 `performAttack` 的「被招式命中」路径上调用，反伤不经过该路径；③**可以击倒攻击方**，并照常播报倒下 / 结算胜败。反伤量 `max(1, maxHp × 0.1667)`（约 1/6）。**此为实现方自主决策，未经需求方确认** | 道具还原度 | 待确认 |
| 32 | 2026-09-12 | 道具 / 战斗（同一人） | 畏缩（`FLINCH_CHANCE`）的有效窗口：本作是「回合末 + 换人时清除」，因此只有在**目标本回合尚未行动**时才会实际生效 | 当前实现与正作一致：`finishRound()` 在回合末对双方 `clearFlinch()`，`switchActive()` 换人时也清除；速度更快的攻击方使出的畏缩才有效，速度更慢者使出的畏缩在本回合基本无效。日志为「XX 畏缩了，无法行动！」。**此为实现方自主决策，未经需求方确认** | 畏缩手感 | 待确认 |
| 33 | 2026-09-12 | 道具 / 战斗（同一人） | 防尘护目镜（`POWDER_IMMUNE`）是否应当**同时免疫沙暴 / 冰雹的回合末削血**？正作中防尘护目镜确实同时具备这两个效果，但需求方给出的对照表只写了「免疫粉末类招式」 | 当前实现为**两者都免疫**：①粉末类招式（含**变化类**的催眠粉 / 毒粉）直接落空；②`weatherChip` 中携带者不参与沙暴 / 冰雹削血。依据是 `HeldItemEffect` javadoc 自添加该枚举值起即写明了第二条，本版补齐实现。**此为实现方自主决策，未经需求方确认** | 道具还原度 | 待确认 |
| 34 | 2026-09-12 | 道具 / 战斗（同一人） | 粉末免疫的**判定位置**：放在 `performAttack`（伤害路径）还是 `executeMove`（出招入口）？ | 当前实现为 **`executeMove` 入口**（`performAttack` 调用之前）。原因：`executeMove` 会先对 `isStatus()` 的招式走 `tryInflict` 分支，若只在 `performAttack` 里判定，**催眠粉 / 毒粉这两个变化类粉末招式会永远漏掉**（已删除原先 `performAttack` 内的死代码检查）。**此为实现方自主决策，未经需求方确认** | 实现正确性 | 待确认 |
| 35 | 2026-09-12 | 数据 / 战斗（同一人） | 拳类招式（`PUNCH`）是否**同时**标记为 `CONTACT`？这决定了「不持拳套时拳类招式会被凸凸头盔反伤」 | `moves.csv` 中 `fire-punch` / `ice-punch` / `shadow-punch` 均标注为 **`CONTACT\|PUNCH`**（同时具备两个标记）；只有攻击方**携带拳击手套**时，其拳类招式才被豁免接触反伤（该招式「不再视为接触」）。**此为实现方自主决策，未经需求方确认** | 道具还原度 | 待确认 |
| 36 | 2026-09-12 | 道具 / 战斗（同一人） | 节拍器（`CONSECUTIVE_BOOST`）的连招增伤是否应当用**能力等级系统**（正作是能力等级提升）表达？ | 当前实现为**装备内联乘算**（`min(2.0, 1 + 0.2 × (连续次数 − 1))`，换招归零，记录存于引擎内存态、键为精灵 `uuid`、不落存档），与讲究系「锁招状态不进模型」同一处理方式（见第 24 条）。**根因是本作的能力等级系统（批次③）尚未落地**；批次③ 完成后应复核是否迁移为能力等级。**此为实现方自主决策，未经需求方确认** | 道具还原度 / 架构一致性 | 待确认（依赖批次③） |
| 37 | 2026-09-12 | 道具 / 战斗（同一人） | 能力等级（`statStages`）的**上下限与越界行为**：原作上限 +6、下限 −6，一次提升 2 级但只剩 1 级空间时应当怎么处理？ | 当前实现为**封顶于 ±6**（`Pokemon#MAX_STAT_STAGE = 6`），且 `changeStatStage(...)` **返回实际变化量**（越界时可能小于请求量，甚至为 0），日志按**实际**变化量播报（到顶时播报「已经无法再提高了！」）。倍率表：±1 → 1.5/0.67、±2 → 2/0.5、±3 → 2.5/0.4、±4 → 3/0.33、±5 → 3.5/0.29、±6 → 4/0.25。**此为实现方自主决策，未经需求方确认** | 能力等级手感 | 待确认 |
| 38 | 2026-09-12 | 道具 / 战斗（同一人） | 清净坠饰（`CLEAR_AMULET`）应当拦下哪些能力下降？正作只拦「对手造成的下降」，不拦自身招式（如近身战自降） | 当前实现为**只拦 `delta < 0` 且「来源存在且不是自己」的下降**（`source != null && source != target`）；自身招式自降、自身强化、对手的**提升**（如刺耳声之外的反向操作）一律不受影响。拦截时播报「XX 借助清净坠饰，能力没有被降低！」，**不消耗**。**此为实现方自主决策，未经需求方确认** | 道具还原度 | 待确认 |
| 39 | 2026-09-12 | 道具 / 战斗（同一人） | 突击背心（`ASSAULT_VEST`）的「不能使用变化类招式」应当在**哪一层**拦截？若放在回合推进之后拦截会造成「空过一回合 + 白扣 PP」 | 当前实现为**放在 `useMove` 入口**（与讲究系 `choiceAllows` 判定相邻）：**不消耗 PP、不推进回合、不消耗随机数**，直接返回播报「XX 因突击背心的效果，无法使用变化招式！」；同时 `pickFoeMove()` 为携带该装备的**敌方 AI 过滤掉变化招式槽位**，避免 AI 每回合空过。**此为实现方自主决策，未经需求方确认** | 道具手感 / 实现正确性 | 待确认 |
| 40 | 2026-09-12 | 道具 / 战斗（同一人） | 突击背心的**特防加成**作用范围：是否影响物理招式的伤害、是否影响混乱自伤（`selfHit`）？ | 当前实现为**只在特殊招式（`SPECIAL`）的伤害计算路径生效**，即在 `computeDamage` 中对**防守方**的特防乘 `1.5`；**物理招式不受影响**，`selfHit`（混乱自伤）也不参与（不经过 `computeDamage`）。依据是原作中突击背心只提升特防，故对物理招式无影响。**此为实现方自主决策，未经需求方确认** | 道具还原度 | 待确认 |
| 41 | 2026-09-12 | 道具 / 战斗（同一人） | 万能伞（`UTILITY_UMBRELLA`）的免除范围：原作只免除**天气**的影响，那么本作的**场地**（电气 / 青草 / 薄雾 / 精神）增伤是否也应当免除？ | 当前实现为**只抵消天气倍率**（晴天火 ×1.5、雨天水 ×1.5 对携带者完全失效），**场地倍率仍正常生效**（电气场地电系 ×1.3 等照吃）。判定点在 `computeDamage` 的天气修正处（`weatherChip` 处另行处理削血免疫，见第 42 条）。**此为实现方自主决策，未经需求方确认** | 道具还原度 | 待确认 |
| 42 | 2026-09-12 | 道具 / 战斗（同一人） | 万能伞是否应当**同时免疫沙暴 / 冰雹的回合末削血**？这与防尘护目镜（第 33 条）的语义重叠 | 当前实现为**免疫，并与 `POWDER_IMMUNE` 合并为同一处判定**（`weatherChip` 中两者共用一个分支与同一条播报「XX 不受沙暴影响！」），因此两件装备在「天气削血免疫」上的行为**完全一致**。**此为实现方自主决策，未经需求方确认** | 道具还原度 / 代码一致性 | 待确认 |
| 43 | 2026-09-12 | 道具 / 战斗（同一人） | 场地种子（`TERRAIN_SEED`）的**触发时机**：若只在「场地刚开启」时判定，则「场地已开、种子后上场」永远不触发（原作是上场即触发） | 当前实现为**双触发点**：①`setTerrain(...)` 开启 / 重置场地时对**双方当前出战精灵**判定并触发（`applyTerrainSeeds`）；②`switchActive(...)` 换上精灵时对**换上的那一只**判定（`applyTerrainSeed`）。两处均要求「场地与 `seedTerrainParam()` 一致」才触发，触发后按 `statParam()` 提升 `statLevels()` 级并**消耗**。**此为实现方自主决策，未经需求方确认** | 道具手感 / 实现正确性 | 待确认 |
| 44 | 2026-09-12 | 道具 / 战斗（同一人） | 弱点保险（`WEAKNESS_POLICY`）的触发条件「被效果拔群的招式命中」如何判定？标靶（`IGNORE_IMMUNITY`）把 0 倍抬到 1.0 倍算不算？ | 当前实现为**只在 `effectiveness > 1.0` 时触发**——标靶把免疫抬到 1.0 倍**不算克制**、不触发；触发后物攻与特攻**各提升 `statLevelsParam()` 级**（弱点保险为 `2`，即 +2/+2）并**消耗**。口径与既有的 `SUPER_EFFECTIVE` 判定一致。**此为实现方自主决策，未经需求方确认** | 道具还原度 | 待确认 |
| 45 | 2026-09-12 | 战斗 / 数据（同一人） | 新增命中判定（`accuracy`）后如何**保证既有战斗零回归**？若每次攻击都多掷一次「是否命中」的骰子，会整体打乱伤害浮动的随机序列，使既有对拍用例（`new Random(42)` 比对）全部失效 | 当前实现约定为**必中招式不消耗随机数**：`rollHit` 在 `accuracy < 0`（数据写 `-1`）或 `accuracy >= 100` 时**直接返回命中、不调用 `random`**；只有 `accuracy` 落在 `1~99` 才掷骰，`accuracy == 0` 也掷骰（因此永不命中）。由于既有 `moves.csv` 中**绝大部分招式 `accuracy = 100`**，随机序列与 v1.17 完全一致，既有 407 例对拍用例无一需要改动。**此为实现方自主决策，未经需求方确认** | 零回归 / 测试稳定性 | 待确认 |
| 46 | 2026-09-12 | 道具 / 战斗（同一人） | 需求方给出的 58 行道具对照表中，有 **9 件在现有引擎下无法实现**：紧缠勾爪 / 机变骰子 / 逃脱按键 / 避难背包 / 红牌 / 美丽空壳 / 厚底靴 / 红线 / 特性护具。请确认是否需要为它们补建底层系统，或从需求中移除 | **2026-09-12 需求方裁决：从需求中移除（「先不实现」，直接去除）。** 本项目**不实现、也不为其建 `equipment.csv` 行**（未注册 ⇒ 游戏中不可获得），底层系统**不立项**。逐条受阻原因（留档）：**紧缠勾爪**（需「无法逃走」状态与换人禁令）、**机变骰子**（需多段攻击（连击）段数模型，本作招式无 `hits` 字段）、**逃脱按键 / 避难背包 / 红牌 / 美丽空壳**（需「战斗中强制换人」链路，本作换人只由玩家 `switchActive` 主动发起，无被动换人入口）、**厚底靴**（需入场危害地形系统：撒菱 / 毒菱 / 隐形岩 / 黏黏网均未实现）、**红线**（需「能力下降时反弹给对手」的被动触发钩子）、**特性护具**（需特性替换机制，本作 `Ability` 未接入战斗结算）。以上均属**大系统缺口**，不是数据遗漏 | 道具池范围 | **已裁决：移除**（2026-09-12） |
| 47 | 2026-09-12 | 战斗 / 数据（同一人） | 两条开发分支**独立实现了同一套「能力等级 + 命中率 + 先制度」**，但契约不同（能力项枚举 `StatModifier`（含 `HP`）vs `Stat`（不含），能力变化载体 `MoveStatChange(stat, delta, self)` vs `StatChange(recipient, stat, delta)`，CSV 段格式 `能力项\|等级[\|SELF]` vs `受方:能力项:幅度`，且两者都占用 `moves.csv` 第 11~13 列但顺序相反）。合并时如何取舍？ | 裁决为**保留对方的机制实现**（`Stat` / `StatChange` / `dev` 的引擎结算顺序与先制度、命中率、补位机制全部保留），本地 15 件装备的能力等级联动效果**重新搭到该套系统上**：本地 `MoveStatChange` 与其测试整体删除，能力项统一改 `Stat`、能力变化统一改 `StatChange`，`moves.csv` 列序定为 `…,inflictionChance,statChanges,effect,flags`（`effect` 从未被填写，放在 `statChanges` 之后让变化技能书写更自然）。同时把「招式造成的能力变化」并入装备路径的唯一漏斗 `BattleEngine#applyStatChange`，使两条路径共用清净坠饰守卫与同一套日志措辞。**此为实现方自主决策，未经需求方确认** | 分支合并 / 契约一致性 | 待确认 |
| 48 | 2026-09-12 | 道具 / 商店（同一人） | 需求方指出「我要求更新的道具没有加入到商店」。查证属实：`ShopStock` 商品池只有 16 件消耗品、**装备一件都没有**，77 件装备与树果仅能靠肉鸽「装备补给」节点等概率随机产出（重复即落空）。装备要上架，缺少三样东西：**定价依据**（`equipment.csv` 无价格列）、**解锁段位规则**、**装备与消耗品的格位分配** | **2026-09-12 需求方裁决：全部 77 件（47 件装备 + 30 种树果）都加进商店，按段位解锁 + 定价。** 具体口径由实现方定为：①**定价**按 `HeldItemEffect` 同类同价（60~900 基础价，再走既有的每段 +15% 通胀），不新增数据列；②**解锁段位**同样按 `effectType` 分类（通用便宜的前置到第 1 段，讲究系 / 生命宝珠 / 剩饭 / 突击背心后置到第 4~5 段）；③**格位**改为「消耗品 + 装备」两个分区各自抽签，`RouteConfig#shopEquipmentStockSize` 给装备固定留 1 格（第 3 段起 2 格），剩余格位归消耗品，避免 77 件装备把 16 件消耗品挤出商店；④**已拥有的装备不再上架**（装备全库唯一），售出后从货架下架。**此为实现方自主决策，未经需求方确认** | 商店 / 装备获取 | 待确认 |
| 49 | 2026-09-12 | UI / 道具（同一人） | 需求方要求「加入个道具图鉴」。图鉴形态、入口位置与是否可操作，需求文档未定（原文仅「图鉴界面（如有）｜收集展示」，且 §九 第 9 条仍把「图鉴是否纳入首版」列为待确认） | **2026-09-12 需求方经三问确认：①形态＝全量目录（93 件全部列出，标注已拥有 / 未拥有，不做收集解锁）；②入口＝主菜单新增「道具图鉴」按钮（不放启动页、不并进商店）；③交互＝可操作，已拥有的装备可直接穿戴 / 脱下。** 实现方据此落地，另有三项细节由实现方自主决策：**a)** 消耗品**不**在图鉴内使用（只读，战斗内才可使用）；**b)** 图鉴的定价 / 解锁段位**复用商店同一份口径**（`ShopStock#catalog()` 与 `forSegment` 共用消耗品池与 `equipmentBasePrice` / `equipmentUnlockSegment`），不另立一套数值；**c)** 排序取「消耗品在前 → 解锁段位 → 基础价 → 名称」。另外，需求文档 §7 的「图鉴弹窗」指主菜单点精灵名进入的**精灵详情页**（已实现），与本次新增的**道具图鉴页**是两个页面。**a)~c) 与两页并存的事实均未经需求方逐条确认** | 图鉴形态 / 信息架构 | 待确认 |
