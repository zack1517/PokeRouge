# 日报

## 日期：2026-09-10　|　提交人：[待确认，战斗系统负责人]

### 一、今日完成

- [x] 战斗系统新增**训练师轮战**：与训练家战斗时，**仅当某一方（己方或对方）所有精灵倒下才退出战斗**
  - 新增 `org.example.model.Trainer`（对手队伍模型，与 `Player` 同一套队伍契约：`MAX_PARTY=6`、
    `getActive / switchToNextHealthy / isPartyAllFainted / leadWithFirstHealthy`）
  - `BattleEngine.resolveRoundEnd()`：敌方出战精灵倒下 → 训练师自动换上下一只健康精灵；
    换不出人 → `PLAYER_WIN` 并按**对方整队**一次性结算经验；己方同理换宠，换不出 → `PLAYER_LOSE`
  - 换宠当回合新上场的精灵**满血且不行动**（下回合才出手）
  - 训练师战中 `tryRun()` 返回提示且不消耗回合，对训练师投球返回提示且**球不消耗、不消耗回合**
  - 新增工厂 `BattleServices.newTrainerBattle(Player, Trainer[, Random])`；玩家无存活精灵或训练师
    队伍无健康精灵时抛 `IllegalArgumentException`
- [x] 修正 `BattleController` 敌方面板渲染：统一改用 `engine.foeActive()`（训练师战中 `getWild()` 为 `null`，
  原实现会导致敌方信息空白）；HUD 遭遇标签与胜利文案区分训练师战/野生遭遇
- [x] 新增单测 `src/test/java/org/example/battle/BattleEngineTrainerTest.java`（9 用例）：对方单只/三只倒下、
  换宠当回合新精灵满血且不行动、己方自动换宠、己方最后一只倒下才判负、不可逃跑、不可捕捉且球不消耗、
  野生遭遇回归、训练师全倒拒绝开战
- [x] 合并 `origin/dev`（战斗界面 UI 与背景图更新，18 文件），无冲突
- [x] 文档同步：
  - `docs/接口文档_战斗服务.md` → **v1.4**：新增 §3.4 训练师轮战判定对照表，§3.3 补 `getTrainer()/foeActive()`，
    §4 补 `newTrainerBattle` 工厂，§5 补训练师示例，§6/§8 补状态机与约束
  - `docs/接口文档_UI模块.md` → **v0.1.4**：§3.3 补训练师轮战界面要求，§4.1 契约表补 `foeActive()/getTrainer()`
    并说明 `getWild()` 在训练师战为 null，§4.2 补 `newTrainerBattle`，§4.3 补 `Trainer` 只读查询
  - `docs/测试用例草稿.md`：A 段战斗模块补 COMBAT-07~12（训练师整队全灭才判胜负、换宠当回合行为、
    不可逃跑/不可捕捉、开战边界）
- [x] 战斗系统新增**异常状态（异常状态系统，参照原版宝可梦）**
  - `org.example.model.StatusCondition`：6 种主要异常（麻痹/中毒/剧毒/灼伤/睡眠/冰冻）+
    混乱（挥发性异常）+ 倒下；集中提供分类（`isMajor/isVolatile/preventsAction`）、
    倍率（`speedMultiplier/attackMultiplier`）、回合末伤害（`residualDamageRatio/residualMessage`）、
    施加判定（`canApply`：属性免疫 > 已混乱 > 已有主要异常）、免疫表（`immunityType`）、
    `parse`/`getDisplayName`
  - `Pokemon`：新增 `confusionTurns/sleepTurns/badlyPoisonCounter` 与状态 API
    （`tryApplyStatus/cureStatus/isConfused/applyConfusion/clearConfusion/clearAllStatus/tickSleep/
    tickConfusion/getBadlyPoisonCounter/increaseBadlyPoisonCounter`）及 `effectiveSpeed/effectiveAttack`；
    倒下与换宠自动清除混乱，主要异常保留
  - `BattleEngine`：行动判定 `canAct`（睡眠→冰冻 20% 解冻→麻痹 25% 略过→混乱 33% 自伤）、
    命中后 `tryInflict`（属性免疫/概率/随机 2~5 回合）、回合末 `statusEndTurn`（中毒 1/8、
    灼伤 1/16 且物攻减半、剧毒 `max(1,计数)/16` 递增）、先手/逃跑/伤害统一改用实际速度与物攻；
    **异常判定前置于 PP 消耗**，避免「麻痹无法行动却扣了 PP」
  - `Move` 新增 `inflicts`/`inflictionChance`（11 参重载构造），`moves.csv` 扩展为 11 列
    （第 9/10 列为异常与概率），运行期 `pokemon.infrastructure.GameData.loadMoves` 与
    `PokemonBattleAdapter.toBattleMove` 打通数据链路；补齐 will-o-wisp/sleep-powder/thunder-wave/
    toxic/confuse-ray 等 14 个招式的附带异常与初始种族池习得项
  - 道具：`ItemCategory.CURE` + `Item.curesSpec`（支持 `|`/`,`/空白 分隔多项，`ALL` 为全部主要异常）
    + `canCure/curedStatuses/curesAll`；`items.csv` 新增 6 个解除道具，新档由
    `grantStartingItems` 发放；`useItem` 仅在使用成功时消耗道具
  - 界面：`BattleView` 双方卡片新增状态徽章（橙底），招式说明叠加异常概率提示，
    队伍信息/队伍菜单显示异常摘要与实际速度；`BattleController` 道具菜单显示解除范围并对
    无对应异常的道具置灰
  - 新增单测 `src/test/java/org/example/battle/BattleEngineStatusTest.java`（**32 用例**），
    用 `ScriptedRandom` 固定随机源覆盖施加/免疫/概率未命中/各异常行动与持续伤害/解除道具/规则表；
    `mvn -o test` **68/68 通过**
- [x] **战斗模块改为「只消费」数据（战斗服务 v1.6）**：
  - 新增只读数据端口 `org.example.battle.BattleDataPort`（`findMove`/`findSpecies`/
    `wildSpeciesPool`/`createPokemon`）与 `BattleDataPorts.none()` 空实现；
  - `BattleEngine` 移除 `org.example.data.GameData` 依赖，升级学招与进化改为经端口查询；
    新增 4 个带端口的构造器，旧构造器保持可用（缺省走空端口）；
  - `BattleServices` 移除数据依赖，`newBattle`/`newTrainerBattle` 各补 2 个带端口重载，
    `randomWild` 补 `(level, dataPort)` 重载（旧签名保留，等价于空端口）；
  - 数据侧新增适配器 `org.example.data.GameDataBattleDataPort`（把 `GameData` 适配为端口，
    不改其既有 API），组装层 `PokemonBattleAdapter#battleDataPort()` 统一提供，
    `MainController` 与 `demo.BattleConsole` 在此注入；
  - 约束落实：`org.example.battle` 包内不再出现任何内建数据与数据模块引用；
  - 新增单测 `src/test/java/org/example/battle/BattleDataPortTest.java`（**8 用例**），
    覆盖空端口、学招/进化走端口（注入与未注入对照）、随机遭遇走端口、
    默认个体生成截断与查不到种族的降级；`mvn -o test` **76/76 通过**
- [x] 文档同步：
  - `docs/接口文档_战斗服务.md` → **v1.5**：新增 §10 异常状态（分类与效果、规则查询 API、
    `Pokemon` 状态契约、引擎结算流程、技能与 CSV 数据契约、解除道具、界面展示、测试覆盖）
  - `docs/接口文档_战斗服务.md` → **v1.6**：§2 模块结构补端口文件与「数据零依赖」说明、
    §4 工厂表补端口重载、§5 示例改为注入端口，**新增 §11 数据端口**
    （定位/空实现/默认实现/外部实现与注入/解耦约束/测试覆盖）
  - `docs/测试用例草稿.md`：A 段战斗模块补 COMBAT-13~23（施加/免疫/不共存/麻痹/睡眠·冰冻/混乱/
    持续伤害/灼伤减攻/解除道具/数据链路）与 COMBAT-24~27（端口注入/降级/随机遭遇/解耦约束）
  - `docs/接口文档_UI模块.md` → **v0.1.6**：同步异常状态（v0.1.5）与数据端口（v1.6），
    §1.2 战斗系统行注明数据由外部经端口注入
  - `docs/适配说明_宝可梦技能队伍接口v1.md`：新增 §六 战斗模块「只消费」数据的分工调整
- [x] **战斗模块职责收敛为「演算 + 结算」（战斗服务 v1.7）**：
  - 新增组装/流程侧 `org.example.integration.WildEncounter`：`levelAround(int[, Random])` 与
   `randomWild(int, BattleDataPort[, Random])`，把**野生遭遇生成**移出战斗模块；
  - 消除战斗模块内最后一处 `Math.random()`：遭遇侧与引擎侧的随机源统一为可注入的
   `java.util.Random`（遭遇侧另有 `ThreadLocalRandom` 缺省重载），测试可完全复现；
  - `BattleServices` 去掉 `wildLevelAround`/`randomWild`（含恒返回空的旧签名），收敛为**纯工厂**
   （仅 8 个 `newBattle`/`newTrainerBattle` 重载）；
  - 调用方同步：`MainController#startRandomBattle()` 与 `demo.BattleConsole#oneWildBattle()` 改调
   `WildEncounter`；数据来源不变（仍经 `BattleDataPort` 取野生池与生成个体）；
  - 新增单测 `src/test/java/org/example/integration/WildEncounterTest.java`（**5 用例**）：
   等级浮动 200 次采样均在 `[等级-2, 等级+2]` 内、不低于下限、可注入随机源复现、
   随机遭遇经端口取池与生成个体、空池返回空；`BattleDataPortTest` 相应精简为 6 用例；
   `mvn -o test` **79/79 通过**
- [x] 文档同步（续）：
  - `docs/接口文档_战斗服务.md` → **v1.7**：§1 补「只做演算与结算」目标，§2 模块结构与类关系图
   去掉遭遇助手并标注模块外归属，§4 工厂表删除遭遇助手行并注明职责边界，§5 示例改用
   `WildEncounter`，§11.3 记端口默认实现的已知取舍，**新增 §12 遭遇生成（模块外）**
   （迁移对照/行为/内聚性收益/调用点/测试覆盖）
  - `docs/测试用例草稿.md`：COMBAT-26/27 改写为遭遇生成与解耦约束（含无 `Math.random()`），
   新增 COMBAT-28/29（等级浮动范围与钳制、遭遇生成可注入随机源）
  - `docs/接口文档_UI模块.md` → **v0.1.7**：依据升级为战斗服务 v1.7，§1.2 战斗系统行与
   外部依赖表改注 `WildEncounter`
  - `docs/适配说明_宝可梦技能队伍接口v1.md`：新增 §七 战斗模块职责收敛（迁出内容/新位置/随机源/
   数据来源/约束/调用点）
- [x] 道具目标选择（战斗服务 v1.8）：
  - 接口：`BattleService#useItem(Item, int partyIndex)` 作用于队伍任意精灵（含替补）；
   原 `useItem(Item)` 降为 `default` 便捷重载（等价于出战精灵下标），既有调用点零改动
  - 引擎：`BattleEngine#useItem(Item,int)` 重排分支（`POKE_BALL` 前置且忽略目标；越界目标提示不消耗；
   `HEAL` 对倒下精灵无效、`CURE` 对倒下精灵仍有效；`else` 分支改为不消耗回合直接返回）
  - 界面：`BattleView#showBagMenu` 由占位改为真实渲染（3 列道具格 + 右卡说明，超出滚动），
   新增 `showTargetMenu`（复用抽出的 `renderPartyGrid` 六格布局，灰格不可点可悬停）；
   `BattleController` 改为「选道具 → 选目标精灵」两步，可用性判定改为「队伍中是否存在合法目标」
  - demo：`demo.BattleConsole#doItem` 同步为道具后追加目标选择
  - 单测：`BattleEngineStatusTest` 新增 6 用例（替补用药 / 单参重载 / 倒下不可回复但可解除 /
   越界不消耗 / 使用后敌方仍行动），`mvn -o test` **85/85 通过**
  - 文档：战斗服务 → **v1.8**（§3.2 新增重载与目标规则表、类关系图、§5 示例、§10.6 目标规则、
   §10.8 测试覆盖 38 用例、§11.5）；UI 模块 → **v0.1.8**（§3.3 两步交互、§4.1 契约表、
   补记 v0.1.6/v0.1.7 版本行）；测试用例草稿新增 COMBAT-30~34；
   适配说明新增 §八 道具目标选择
- [x] 成长判定外移（战斗服务 v1.9）：战斗模块**不负责**经验增加 / 升级 / 学招 / 进化
  - 端口：新增 `org.example.battle.BattleGrowthPort`（`settle(survivors, defeated)` 申报结算、
    `resolveLearn(choice, forgetSlotIndex)` 转发学招抉择、`Settlement` record、`none()` 空端口）
  - 外部模块：新增 `org.example.growth.GrowthService`（经验 = 被击败精灵六维种族值总和 × 等级 / 5、
    单只下限 30；逐级升级、到级学招、进化，技能与种族经 `BattleDataPort` 查询）
  - 引擎：删除 `awardExpAndSettle` / `expGain` / `expSum` / `tryLearnAt` / `tryEvolve`，
    改为 `settleGrowth()` 申报（只搬运日志行 + 收拢挂起抉择）；`decideLearn(int)` 改为
    「队列出入队 + 转发 `resolveLearn`」；移除 `Species` / `Stats` 依赖
  - 工厂/组装：`BattleServices` 新增 4 个成长重载（旧重载内部等价 `BattleGrowthPort.none()`）；
    `PokemonBattleAdapter#battleGrowthPort(BattleDataPort)`；`MainController`、`demo.BattleConsole`
    改为注入数据端口 + 成长端口
  - 单测：新增 `GrowthServiceTest` 13 用例；`BattleDataPortTest` 成长用例改为注入 `GrowthService`
    并补「未注入成长端口 → 等级不变、不学招、不进化」对照；`mvn -o test` **98/98 通过**
  - 文档：战斗服务 → **v1.9**（§1 设计目标、§2 模块结构与类关系图、§3.1 `PLAYER_WIN`、
   新增 §3.5 学习抉择、§4 工厂表 4 个成长重载、§5 示例、§6 状态机、§11 端口用途、
   新增 §13 成长端口）；UI 模块 → **v0.1.9**；测试用例草稿新增 COMBAT-35~39 与 GROW-09/10；
   适配说明新增 §九 成长判定外移
- [x] **对接文档**（面向「外部导入数据的模块」的开发者）：新增 `docs/对接文档_战斗模块数据对接.md` **v1.0**（10 章）
  - §一 角色与边界（职责三列表 + 边界图）；§二 对外接口清单（`BattleDataPort` 4 方法逐个说明
   「用途/谁在调用/何时调用/返回约定」、`BattleGrowthPort` 与 `Settlement`/`none()`、非端口对象）；
   §三 战斗模块已有能力（`BattleService` 方法表、伤害/先手/异常/天气场地/道具/捕捉/训练师轮战等演算规则、
   12 个工厂重载、「不会做的事」5 条）；§四 使用方法（三步接入、最小示例、学招抉择循环、训练师战、
   自定义数据端口与自定义成长规则实现）；§五 需要的数据（`Move`/`Species`/个体构造契约表、
   **9 条一致性硬约束 C1~C9**、现有 CSV 列定义参考）；§六 交接清单 D-01~D-09 + 启动期数据自检代码骨架；
   §七 已知风险 R-01~R-07；§八 缺数据降级行为矩阵；§九 验收自测清单；§十 相关文档索引
  - `docs/接口文档_战斗服务.md` 头部补 v1.9a 变更行，文末新增 **§14** 指向该对接文档（含章节对照表）
- [x] **对接文档中的实测缺陷 R-01**：用 `jshell --class-path target/classes` 探针实测发现
  `species.csv` 实际列序（`...,baseExp,captureRate,category,description`）与旧注册表
  `org.example.data.GameData#applySpeciesRow` 期望列序（`...,catchRate,wild,evolvesTo,evolveLevel,moves,learns`）
  不一致 → `species("bulbasaur").getMoveIds() = ["种子宝可梦"]`、`moveLearnedAt(...) = null`、
  `createPokemon("bulbasaur", 5)` 得到 **0 个技能槽**；旧注册表的学招/进化**全线静默失效**
  （另发现 R-02：`org.example.data.GameData` 与 `org.example.pokemon.infrastructure.GameData`
  两套注册表并存，id 系不同）。已登记为风险与验收项，未在本轮修数据
- [x] 合并 `origin/dev`（**肉鸽系统**，21 文件 / +1347 / -7），无冲突
  - 新增：`GameSession`、`FloorData`、`FloorEventGenerator`、`Option`/`OptionGenerator`/`OptionType`、
   `PokemonInstance`、`RogueTurnManager`、`RunData`、`MusicPlayer`、`view/RogueFloorView`、
   `view/StartView`、`resources/audio/bgm_start.mp3`，以及 `PokemonBattleAdapterTest`(5)、`RogueTurnManagerTest`(5)
  - 修改：`pom.xml`（+`javafx-media`）、`App`、`MainController`（+206，肉鸽楼层流程与 WILD/ENEMY/BOSS 战斗入口）、
   `config/AppConfig`、`model/ElementType`、`view/MainView`
  - 合并后 `mvn -q compile` 通过；`mvn -o test` → **108/108 通过**（原 98 + dev 的 10 个新用例）
- [x] **组装层收口（合并后一致性修正）**：dev 的 3 个肉鸽战斗入口（`startRogueWildBattle` /
  `startRogueEnemyBattle` / `startRogueBossBattle`）只注入了数据端口、**漏注入成长端口**
  （表现为「肉鸽战斗能打，但胜利后没有经验/升级/学招/进化」且不报错）
  - `MainController` 抽出两个私有组装入口 `newWildBattle(Player, Pokemon)` /
   `newTrainerBattle(Player, Trainer)`，内部一次性创建 `dataPort` 并注入
   `PokemonBattleAdapter.battleGrowthPort(dataPort)`；4 个调用点（随机战 + 肉鸽 WILD/ENEMY/BOSS）全部改走入口
  - 组装层自此**单一组装点**，新增战斗入口不会再漏端口
- [x] **成长机制（捕捉驱动的个体值成长，战斗服务 v1.10）**：玩家多次捕捉同族精灵后，
  **之后遇见的所有精灵个体值提升**（种族值不变），并对外提供**图鉴数据接口**供局外查询
  - 成长模块新增三个类（`org.example.growth`）：
    - `IvGrowthRule`：纯规则。`MAX_IV = 31`、`CAPTURES_PER_STEP = 2`；
      `speciesBonus(captureCount) = min(31, captureCount / 2)`、`apply(iv, bonus) = min(31, iv + bonus)`
    - `SpeciesGrowthRecord`：不可变的单族图鉴条目（`captureCount` / `battleCount` / 派生 `ivBonus`
      / `isBlank` / `equals`）
    - `GrowthProgress`：**图鉴数据接口**（可变进度表 + 进程级单例 `instance()`）。
      查询：`record(id)` / `dexEntries()`（按 id 排序，含只参战未捕捉的族）/
      `captureCount` / `battleCount` / `ivBonus` / `globalIvBonus()`（各族之和，封顶 31）；
      写入：`recordCapture` / `recordBattle` / `clear`
  - 端口与引擎（捕捉申报）：`BattleGrowthPort` 新增 **`default void onCaptured(String speciesId)`**，
    `default` 空实现使 `none()` 与既有实现无需改动；`BattleEngine#tryCapture` 在野生精灵
    **成功入队之后**申报一次（不追加日志行，避免与原有捕捉文案重复）；训练师轮战不可捕捉故不触发
  - 成长模块落地：`GrowthService` 持有 `GrowthProgress`（新构造 `GrowthService(BattleDataPort, GrowthProgress)`，
    旧单参构造委托单例），`settle` 先按参战精灵累计**对战次数**、`onCaptured` 累计**捕捉次数**，
    新增 `getProgress()`；空 / 空白物种 id 的申报被忽略，不污染图鉴
  - 加成落地（新体系）：`Stats#randomIv()` 降为 `randomIv(0)` 的委托，新增
    **`Stats#randomIv(int bonus)`**（每项 `min(31, 随机 0~31 + bonus)`）；
    `PokemonServiceImpl` 持有 `GrowthProgress` 并抽出私有 `newPokemon(...)`，三条创建入口
    （`createPokemon` ×2、`createWildPokemon`）全部经它生成 —— 保证「加成对之后遇到的所有精灵生效」口径唯一
  - **战斗模型公式改为计入个体值**（这是让机制真正影响战力的关键）：
    `model.Pokemon#computeStats(species, level, ivs)` 采用标准公式
    `(2×种族值 + 个体值) × 等级 / 100`（HP 另加 `等级 + 10`，其余五项 `+5`）；
    `create` 新增 4 参重载显式接收 `ivs`（3 参重载按 `Stats.randomIv()` 随机），
    `applyStatsOnLevelUp` 沿用个体持有的 `ivs`（升级不让个体值漂移），新增 `ZERO_IV` 缺省值
  - 接缝修正：`PokemonBattleAdapter#toBattlePokemon` **原先丢弃个体值并重新随机**（旧行为），
    现经新增的 `toBattleStats(Stats)` 显式搬运；`battleGrowthPort` / `createWildPokemon`
    各补一个接收 `GrowthProgress` 的重载便于隔离测试
  - 会话与组装：`GameSession` 持有并暴露 `growthProgress`（新增 2 参构造供测试），
    `MainController#growthProgress()` 统一取会话进度；`newWildBattle` / `newTrainerBattle`
    由 `static` 改实例方法，3 个 `createWildPokemon` 调用点（野生 / 训练师队 / Boss）全部传入 ——
    捕捉记录在战斗内产生、立刻影响下一场遭遇
  - **本地永久存档（「本地单机永久提升」要求的落地）**：原先进度只存进程内存，关掉程序即丢。
    项目**没有存档系统**（`StartView` 的「继续游戏」仍是预留入口，`model/PokemonState` 只是个未被
    使用的 Serializable 快照类），因此本机制**自带极简存档**：
    - 新增 `GrowthProgressStore`（`org.example.growth`）：`defaultFile()` =
      `<用户主目录>/.pokerouge/growth-progress.txt`（放用户主目录而非工程目录，
      重新构建 / 切分支 / 清 `target` 都不丢档）；换 `at(Path)` 即可改存别处
    - 存档为 UTF-8 可读文本（含格式头与说明注释，每行 `物种id=捕捉次数,对战次数`，按 id 排序），
      便于调试与手工清档
    - `GrowthProgress#instance()` 改为**懒加载** `Holder`，首次访问时经 `loadProgress()` 从文件载入
      并 `attachSaver(this::write)` 绑定自动保存；`recordCapture` / `recordBattle` / `clear` 后立即落盘
    - **原子写**（先写 `.tmp` 再 `ATOMIC_MOVE`，不支持时退化为 `REPLACE_EXISTING`），
      避免写一半崩溃损坏存档
    - 边界：文件不存在 / 损坏 → 按新档处理；单行格式错误只跳过该行不整档报废；
      写入失败（目录只读 / 被占用）只打日志，本局进度仅存内存，**不抛异常、不中断战斗**；
      上限由 `IvGrowthRule#MAX_IV` 在**规则层**强制，故手工把次数改到数千游戏内仍封顶 31
    - `new GrowthProgress()` 默认构造不绑 saver → 纯内存、永不碰盘；`PokemonBattleAdapterTest` 与
      `PokemonSystemValidator` 已改为注入独立实例，避免测试读到开发者本机真实存档而随机失败
  - 单测（**新增 38 用例，`mvn -o test` → 146/146 通过**）：
    `GrowthProgressTest`(11)：空记录不写入、捕捉递增、每 2 次记 1 点、单族与全局封顶、
    对战次数独立、图鉴排序与对战-only 族、跨轮累积、清空、空 id 拒收、加成截断；
    `GrowthProgressStoreTest`(**14**，全部 `@TempDir` 隔离)：无文件时空档、计数往返一致、
    **进度变化立即落盘且跨「重启」恢复**、多族与全局加成跨重启保持、存档可读且按物种排序、
    注释与空行忽略、损坏行跳过不整档报废、**超量次数仍受上限约束**、空记录不写入、
    父目录自动创建、`clear()` 同时清空文件、写入失败不抛异常、未绑 saver 不触碰磁盘、
    默认路径位于用户主目录；
    `GrowthServiceTest`(+5 → 18)：捕捉申报累计、空 id 忽略、结算累计对战、未参战不写入、局外可查；
    `PokemonServiceImplGrowthTest`(6)：无进度时 IV 合法、捕捉后各项 ≥ 加成、满加成必满个体、
    **种族值不变**、满个体面板高于零个体、服务暴露进度；
    `PokemonBattleAdapterTest`(+2 → 7)：个体值跨系统搬运、满加成经接缝传导为满个体
  - **真实跨进程冒烟验证**（不是单测，用 `jshell --class-path target/classes`）：进程 A 写 6 次捕捉
    + 1 次对战 → 进程 B（全新进程）读回 `加成=3 / 捕捉=6 / 对战=1` 完全一致，存档文件内容合法
    UTF-8；验证后已清理本机存档，开发者从干净档开始
  - 文档：战斗服务 → **v1.10**（§13.1 端口表补 `onCaptured`、§13.2 补捕捉申报流程、
    §13.3 补图鉴进度与「本地永久存档」规则、§13.5 测试覆盖更新（146/146）、
    **新增 §13.6 个体值与属性演算**、**新增 §13.7 成长进度的本地永久存档**）；
    UI 模块 → **v0.1.10**（**新增 §4.6 成长 / 图鉴数据接口**：三个取数入口 +
    `GrowthProgress` 查询方法表 + `SpeciesGrowthRecord` 字段）；
    适配说明新增 **§十**（并订正 §三 第 6 条与 §五「IV 未接入战斗结算」的旧取舍）；
    测试用例草稿新增 COMBAT-40 与 GROW-11~17，并注明 GROW-08（EV）仍未实现

- **存档系统（新需求：「单机 4 个存档，未作战时可存档」）**：
  - 需求澄清（2 轮）：图鉴成长归属 → **每个存档位各自独立**；存档触发 → **手动 + 自动**
  - **新增 `org.example.save` 8 个类**：
    - `SaveSlot`：固定 4 个档位枚举，`id()` = `slot1~slot4` **同时是磁盘目录名**（一经发布不可改），
     `displayName()` = 「存档 N」，`all()/count()/ofId(String)`
    - `SaveData`：顶层 record + 6 个嵌套 record（`PokemonData/MoveData/ItemData/RunRecord/OptionData/IvData`），
     `FORMAT_VERSION = 1`；紧凑构造器做 null 归一（`status`/`speciesId` 归为空串 —— 这是修 bug 时加的，
     否则 `null` 写出成空字段再读回会变成 `""`，往返不一致）
    - `SaveCodec`：纯文本编解码（项目**无** JSON 库，pom 只有 javafx + junit）。
     格式 `键|值1|值2` 每行一条，键 `VERSION/SAVED_AT/PLAYER/ACTIVE/SEGMENT/MAP/RUN/PP/MOVE/ITEM/OPTION/BOSS`；
     `\|` `\\` `\n` 转义（`\r` 丢弃）；`#` 注释；**未知键跳过**；缺 `VERSION` 行或版本过高抛
     `SaveFormatException`；数值非法**带行号**；`MOVE` 行按下标归并、越界行忽略
    - `SaveFormatException` / `SaveSummary`（档位摘要 + `describe()` 一行式文本）
    - `SaveStore`：文件布局与读写。`defaultRoot()` = `<user.home>/.pokerouge/saves`，
     每档 `slotN/{save.txt, growth-progress.txt}`；**原子写**（`.tmp` + `ATOMIC_MOVE`，退化 `REPLACE_EXISTING`，
     **带 3 次 × 20ms 重试**以躲开 Windows 索引器/杀毒软件的瞬时占用，避免对玩家弹假的「保存失败」）；
     `statuses()`/`status()` 返回 `SlotStatus(slot, exists, readable, summary)` + `usable()`/`empty()`；
     `delete()` 同时清进度与成长两份文件
    - `PokemonMapper`：`model.Pokemon ⇄ SaveData.PokemonData`，种族与技能**按 id 重查**
     （优先 `pokemon.infrastructure.GameData`，回退 `PokemonBattleAdapter.battleDataPort()`），
     越界值一律裁剪（等级 `[1, MAX_LEVEL]`、HP `[0, maxHp]`、PP `[0, maxPp]`）
    - `SaveManager`：`snapshot` / `save`（**失败抛异常**，UI 提示）/ `autoSave`（**失败只记日志返回 false**，
     绝不打断游戏）/ `load` / `newGame` / `defaultManager`
  - **既有文件改动 5 处**：`MoveSlot` 新增 `MoveSlot(Move, int pp)`；`Pokemon` 新增
    `restore(species, level, ivs, slots, exp, status, sleepTurns, badlyPoisonCounter, confusionTurns, currentHp)`
    （既有 API **无 setter**，还原残血/异常/剩余 PP 必须走这个入口；属性仍由 `computeStats` 现算）；
    `PokemonBattleAdapter` 的 `toBattlePokemon/toBattleSpecies/toBattleMove` 由 `private` 提为 `public`
    （读档必须复用同一接缝，**无行为变更**）；`GameSession` 补 `setSegment/getMapBackground/setMapBackground`；
    `GrowthProgressStore.fileName()` 提为 `public`（供存档复用同一文件名）
  - **UI 接线**：新增 `SaveSlotView`（4 档位一屏，`Purpose.NEW_GAME / CONTINUE / SAVE` 三用途复用，
    **视图自身不做 IO**，选择结果经回调交回控制器）；`StartView` 增「继续游戏」回调（无存档时置灰 + tooltip）；
    `MainView.Actions` 增 `onSaveGame()` 与「保存游戏」按钮、标题栏显示当前档位名；
    `MainController` 增 `saveManager`/`activeSlot`/`battleInProgress` 三个字段与 `enterBattle(...)`
    统一入口 —— **战斗中置位并拒绝手动存档，这就是「未作战时才可存档」的落点**；
    自动存档时机：回到主菜单、每次楼层事件推进后、BOSS 胜利进下一层前、新游戏建好会话后
  - **「保存游戏」可另存到其他档位**（验收反馈后修正）：原先 `manualSave()` 在已有档位时
    **直接写当前档、没有选择入口**（`chooseSlotToSave()` 只有 `activeSlot == null` 才走到），
    且选档页保存成功后仍停在选档页。现改为**每次手动保存都经过选档页**（当前档标注「（当前）」，
    按钮「保存到当前档」/「保存并切换到此」），选定后**该档位成为当前档位**（标题栏随之更新、
    之后的自动存档都写它），保存完回主菜单；覆盖确认改为**只在覆盖自己以外的已有档位时**才弹
    （写回当前档是常规保存，不再打扰）
  - **`growth` 的归属修订**：图鉴成长改为**每档位一份**（`SaveStore#loadGrowth/createGrowth` 经
    `GrowthProgressStore#at(Path)` 指向档位目录），换档位等于换一份个体值成长；
    `GrowthProgress#instance()` 的全局文件不再被新档位使用 —— 这是「每个存档独立」要求的直接落地
  - 单测（**新增 55 用例 → `mvn -o test` → 201/201 通过**，全部 `@TempDir` 隔离，不碰本机存档）：
    `SaveCodecTest`(15)：往返一致、可选字段为 null、特殊字符转义后行数不变、未知键跳过、
    注释空行忽略、缺版本行/版本过高/空内容/数值非法带行号、技能按下标归并、越界技能行忽略、
    摘要容错与兜底、文件头、定界符工具；
    `SaveStoreTest`(14)：档位与目录命名稳定、写出读回、空档、任一档可继续、**无残留临时文件**、
    覆盖保存、自动建目录、档位互不影响、状态列表、损坏档宽松展示而严格读档抛错、删除清两份文件、
    **图鉴成长按档位独立**、无成长文件、默认路径；
    `PokemonMapperTest`(10)：状态完整往返（个体值/残血/异常计数/PP）、个体值往返、HP 裁剪、PP 裁剪、
    未知物种返回空、未知技能丢弃、等级裁剪、缺失字段默认值、种族同源、队伍顺序；
    `SaveManagerTest`(16)：完整闭环、中途状态保留、楼层选项与 BOSS 往返、结束状态、空档、损坏抛错、
    物种全失效返回空、自动存档成功/失败不抛、手动存档失败抛错、**新游戏清空档位**、
    **图鉴成长按档位独立**、档位互不影响、快照带时间、默认编排器
  - 文档：**新增 `docs/接口文档_存档系统.md` v1.0**（需求范围 / 磁盘布局 / `save.txt` 格式与逐键字段表 /
    公开 API / 界面接线与流转图 / 版本兼容矩阵 / 已知限制 / 验收测试清单）；
    UI 模块 → **v0.1.11**（§3.2 页面总表增存档位选择页、**新增 §3.5 存档位选择页**、
    **新增 §4.7 存档系统接口**、§4.6 说明成长记录已改为按档位存放）；
    适配说明新增 **§十一**（并订正 §十 中「项目无存档系统」与「进度归属为进程级单例」两处过期说明）；
    测试用例草稿新增 **E 组 SAVE-01~12**（含覆盖确认、战斗中拒绝存档、档位独立、损坏容错、前向兼容）

  - **需求三（路线节点 / 行动点改版）第一步落地**（严格按《需求文档》§4 全表实现）：
    - **节点类型**：`OptionType` 由 4 值扩至 **8 值**（路人 / 野外精灵 / 医院 / 商店 / 特殊事件 /
      道馆战 / 四天王连打 / 冠军战），并补 `isResident()` / `isMandatory()` / `getApCost()` /
      `defeatOnlyCostsGold()` 等判定；旧 `ENEMY` / `RANDOM` / `BOSS` 三值删除
    - **行动点（AP）**：新增 `RouteConfig` 集中所有可调数值（总段数 5、第 1 段 8 点且每段 +2、
      商店 55% / 特殊事件 30% 出现概率、野外免单 30%、每段节点数上限 5、商店每段 +1 件且 +15% 价、
      自回血 1/5）；`RogueTurnManager` 重写为纯规则状态机（`consumeNode` 校验并扣点、
      `advanceAfterNode` 在 AP 耗尽时触发道馆、`resolveMandatoryVictory/Defeat` 处理必然节点链与
      「可失败一次」；行动点不足时拒绝进入且状态不变）
    - **金币与商店**：`RunData` 增金币并发奖励 / 扣惩罚（不足时扣到 0 不为负）；
      新增 `ShopStock`（12 件商品按段解锁）与 `ShopView`（余额显示、买不起禁用）
    - **必然节点链**：道馆 →（末段）四天王连打 → 冠军战 → 通关；道馆 / 四天王可失败一次（大量扣金币，
      重试再败即结束），冠军不可失败；均**不占行动点**
    - **界面**：`RogueFloorView` 重写为「第 N / 5 段 · 阶段名」+「行动点 X / Max　金币 N 🪙」，
      已走过节点置灰、必然节点阶段改为必然节点卡片与「挑战道馆」入口；`MainView` 标题栏加金币
      （尚未开始远征时隐藏）
    - **存档升 v2**：`FORMAT_VERSION` 1 → 2，`RUN` 行由 3 字段改为 **7 字段**（行动点 / 上限 / 阶段 /
      重试 / 金币 / 结束 / 通关），`OPTION`·`BOSS` 各增「**已走过**」字段，
      `SaveSummary` 的 `currentFloor` 拆为 `ap` + `gold`；**v1 旧档仍可读**（旧「点数」迁移为行动点）
    - **测试**：新增 `RouteConfigTest`(9) / `NodeGeneratorTest`(8) / `ShopStockTest`(10)，
      完全重写 `RogueTurnManagerTest`(21)；全量由 201 → **246 个，Failures 0 / Errors 0**
    - 文档：`接口文档_存档系统.md` → **v2.0**；`接口文档_UI模块.md` → **v0.1.12**
      （新增 §3.6 路线地图页与商店页、§4.8 肉鸽路线只读查询）；`需求文档.md` 新增
      **§4.5 首版落地参数与实现进度**并细化 §七 / §九；`测试用例草稿.md` 新增 **F 组
      ROUTE / AP / NODE / GOLD / SHOP / MAND 共 21 条**，E 组补 SAVE-17/18 与 v2 字段说明

  - **需求三（火箭队剧情线与神兽偶遇）第二步落地**（严格按《需求文档》§五 规格表实现）：
    - **节点类型**：`OptionType` 由 8 值扩至 **12 值**，新增 `ROCKET`（火箭队出没）/`ROCKET_CAPTURE`
      （火箭队抓捕神兽）/`LEGENDARY`（神兽偶遇）/`ROCKET_INVASION`（首领侵略战）；补
      `defeatEndsRun()`（火箭队三节点战败即结束）与 `Option.allowsCapture()`（野外 + 神兽可捕捉）
    - **数值集中**：`RouteConfig` 增后期判定（第 3 段起）、火箭队 35% / 抓捕 60% / 神兽 30% 出现概率、
      四种节点的行动点（2 / 2 / 1 / 0）、胜利金币、掉落概率（高级球 60%、大师球必得）、
      等级加成与队伍规模公式
    - **生成规则**：`NodeGenerator#rollSpecialEvent` 按优先级占用**唯一的特殊事件槽位** ——
      待触发的 0 点神兽 → 后期剧情线抓捕 → 后期神兽 → 火箭队 → 通用特殊事件；节点数仍截到上限 5
    - **剧情线状态机**：`RunData` 增 5 个标记（线已开 / 首领已败 / 已遇神兽 / 待触发神兽 / 侵略战已触发）；
      `RogueTurnManager#markStorylineOnEnter` 在**进入节点**时打标记；击败火箭队首领后
      `resolveRocketBossVictory()` 把 0 点神兽追加进本段可选节点；冠军战胜利时按「是否已击败首领」
      三分支（追加首领侵略战 / 直接通关 / 未碰剧情线直接通关）
    - **控制器接线**：`MainController` 增 3 个开战入口（火箭队队员 / 火箭队首领 / 神兽）、
      `applyNodeReward` 结算道具掉落与大师球、`grantItem` 入背包；`runEndReason` 让战败文案说明
      是哪个节点不可失败
    - **存档升 v2.1**：新增**可选** `STORY` 行（5 个 `0|1` 标记，全 `false` 不写、缺行按未开启读回），
      **不提升 `FORMAT_VERSION`**，旧 v2 与 v1 文件均可读
    - **测试**：新增 `RocketStorylineTest`(18) / `SaveStoryCodecTest`(5)，`NodeGeneratorTest` 8 → 9
      （补「火箭队判定落空时退回通用特殊事件」）；全量由 246 → **270 个，Failures 0 / Errors 0**
    - 文档：`接口文档_存档系统.md` → **v2.1**；`接口文档_UI模块.md` → **v0.1.13**；
      `需求文档.md` §4.5 补「火箭队剧情与神兽偶遇的首版参数」表；`测试用例草稿.md` 新增
      **G 组 RISK / STORY / LEGEND 共 19 条**

### 二、今日遇到的问题与处置

| 问题 | 处置方式 | 状态 |
| --- | --- | --- |
| 训练师战中敌方面板空白 | `BattleController` 原用 `engine.getWild()` 渲染，改为统一 `foeActive()`；`getWild()` 仅野生遭遇非空 | ✅ 已解决 |
| 换宠当回合新精灵是否会立刻出手 | `useMove` 在 `resolveRoundEnd()` 之前判断出战精灵是否倒下，换上的精灵当回合不行动，已加断言固化 | ✅ 已解决 |
| 本机 git 启用了提交签名但无私钥，commit 失败 | 提交统一加 `--no-gpg-sign`（不改动全局 git 配置） | ✅ 已解决 |
| 训练师战暂无游戏内入口 | v1.4 时 `newTrainerBattle` 尚无调用方；本轮合并 `origin/dev` 后肉鸽 **ENEMY 事件**（拦路训练家）已走 `newTrainerBattle`，入口补齐 | ✅ 已解决 |
| 麻痹精灵「无法行动」时仍被扣了 PP | 原实现先 `MoveSlot.use()` 再判定行动；改为 `playerAct` 中**先 `canAct` 再消耗 PP**（`use()` 在 PP 耗尽时返回 false 且不消耗，判定必须前置），并加断言固化 | ✅ 已解决 |
| 解毒药无法解除剧毒（原版应同时解除中毒与剧毒） | 原 `Item.curesSpec` 只支持单个状态；改为按 `[&#124;,\s]+` 分词的多项范围，并把 `i_antidote` 调整为 `POISON&#124;BADLY_POISON`（内建注册与 CSV 同步） | ✅ 已解决 |
| 「只带变化类效果」的招式无法施加异常 | `executeMove` 原在 `MoveEffect.NONE` 时提前跳过；改为「有效果 **或** 带 `inflicts`」都进入结算 | ✅ 已解决 |
| 战斗结束后仍继续推进回合末结算 | `finishRound` 开头增加 `Status.ONGOING` 判定，非进行中直接返回 | ✅ 已解决 |
| 战斗模块直接依赖数据注册表 `GameData`（内建数据 + 直连数据源） | 抽出只读端口 `BattleDataPort`，引擎/工厂改为经端口查询，数据侧提供 `GameDataBattleDataPort` 适配器并在组装层注入；旧签名保留（缺省走空端口 `BattleDataPorts.none()`），`mvn -o test` 76/76 无回归 | ✅ 已解决 |
| 战斗模块内的 `Math.random()` 不可注入，遭遇生成也不属于演算/结算 | 把 `wildLevelAround`/`randomWild` 迁至组装侧 `WildEncounter`，随机源改为可注入的 `java.util.Random`（缺省 `ThreadLocalRandom`）；`BattleServices` 收敛为纯工厂，调用方同步改造，`mvn -o test` 79/79 通过 | ✅ 已解决 |
| 道具只能作用于出战精灵，无法给替补用药 | 新增 `useItem(Item, int partyIndex)` 目标选择重载，原单参方法降为 `default` 委托（既有调用点零改动）；明确「倒下精灵不可回复但可解除异常」「越界不消耗回合」等目标规则；UI 改为两步菜单，`mvn -o test` 85/85 通过 | ✅ 已解决 |
| 战斗模块仍承担经验增加、升级、学招与进化规则，超出「只消费外部数据的引擎」定位 | 抽出申报式端口 `BattleGrowthPort`（`settle` / `resolveLearn` / `none()`），外部成长模块实现 `GrowthService`；引擎删除 `awardExpAndSettle`/`expGain`/`expSum`/`tryLearnAt`/`tryEvolve`，仅把 `survivors`+`defeated` 与挂起学招抉择申报出去；旧工厂重载等价 `none()` 保持兼容，`mvn -o test` 98/98 通过 | ✅ 已解决 |
| 成长规则在 `PokemonServiceImpl#gainExp` 与 `GrowthService` 各有一份 | 本轮先交付端口化与外部实现；两处公式口径统一列入明日计划，避免规则双源 | ⏳ 待收敛 |
| 肉鸽战斗入口（合并 `dev` 后新增）只注入数据端口、漏注入成长端口 | 3 个入口（WILD/ENEMY/BOSS）原写法仅 `BattleServices.newBattle/…(player, x, PokemonBattleAdapter.battleDataPort())`，表现为「战斗能打但胜利后无经验/升级/学招/进化」且**不报错**；`MainController` 抽出 `newWildBattle`/`newTrainerBattle` 统一注入双端口，4 个调用点全部改走入口，`mvn -o test` 108/108 无回归 | ✅ 已解决 |
| `species.csv` 列序与旧注册表解析期望不一致，学招/进化**静默失效** | 实测：`createPokemon("bulbasaur", 5)` 得到 0 个技能槽、`moveLearnedAt(1/16/32)` 全为 `null`；列序应为 `…evolutionLevel,evolutionTarget,baseExp,captureRate,category,description`，旧解析按 `…catchRate,wild,evolvesTo,evolveLevel,moves,learns` 读 → 全线错位。已写入对接文档 §七 R-01 与 §六 D 段验收项；**数据侧修列序还是改解析器待定** | ⏳ 待确认 |
| 两套数据注册表并存（内建 id `s_fire_cat` 系 vs CSV id `bulbasaur` 系） | `org.example.data.GameData` 与 `org.example.pokemon.infrastructure.GameData` 各自注册，`wildPool()` 返回内建 id 而 `findSpecies` 按 CSV id 查 → 需在数据模块定稿时收敛为一套（对接文档 §七 R-02） | ⏳ 待确认 |

| 需求要求「本地单机**永久**提升」，但原实现是进程级单例，关掉程序即丢 | 项目**无存档系统**（`StartView`「继续游戏」仅预留入口），故让成长机制**自带极简存档**：新增 `GrowthProgressStore`（`<用户主目录>/.pokerouge/growth-progress.txt`、UTF-8 可读文本、原子写、异常只打日志），`GrowthProgress#instance()` 懒加载并从文件载入 + 绑定自动保存；测试全部 `@TempDir` 隔离，并以 `jshell` 跨进程冒烟验证 | ✅ 已解决 |
| 加载进程级单例的测试会读到开发者**本机真实存档**，导致结果随机 | `new GrowthProgress()` 默认不绑 saver（纯内存）；`PokemonBattleAdapterTest` 与 `PokemonSystemValidator` 改为注入独立实例；store 相关断言全部在 `@TempDir` 内 | ✅ 已解决 |
| `GrowthProgressStoreTest#清空进度同时清空存档文件` **偶发失败**（单独跑必过，全量跑偶尔红） | 定位为 Windows 上 `Files.move(ATOMIC_MOVE)` 被索引器/杀毒软件瞬时占用，替换失败后按「写入失败只打日志」降级 → 旧内容残留。`GrowthProgressStore#write` 与 `SaveStore#move` 均补**有界重试**（3 次 × 20ms）并清理失败临时文件；连续全量 `mvn -o test` 恢复稳定 **201/201 通过** | ✅ 已解决 |
| 「保存游戏」**无法保存到其他存档位**：已有档位时点按钮直接静默写回当前档，玩家没有选择入口；且选档页保存成功后仍停在选档页 | 验收反馈「保存存档有问题，设置成可以保存到其他存档」后修正：`manualSave()` 取消「已选档位就直接写」的短路，**每次都进选档页**；选定档位后 `activeSlot = slot`（该档成为当前档，标题栏与后续自动存档都跟着它），保存成功 `showMainMenu()` 回主菜单再弹提示；覆盖确认收窄为「覆盖自己以外的已有档位」才弹 | ✅ 已解决 |

| 存档 v2 快照把「尚未开始远征」写成了第 1 段（`SaveManagerTest.快照带存档时间` 由 0 变 1） | 根因是 `GameSession#getSegment()` 会把 `<1` 抬到 `RunData.FIRST_SEGMENT`。改为快照直接用 `RunData#getSegment()`、读档用 `RunData#setSegment(Math.max(0, …))` 绕开会抬值的门面方法，并把这条例外写进存档文档 §4.4 | ✅ 已解决 |
| 存档格式升 v2 后 `SaveCodecTest.未知键被跳过` 仍把输入写死 `VERSION\|1` | 断言随格式版本演进应当自动跟随，改为 `"VERSION\|" + SaveData.FORMAT_VERSION`，避免下次升版再翻一次测试 | ✅ 已解决 |
| v1 旧档的 `RUN` 行与 v2 语义不同（第 1 字段是楼层而非行动点），摘要读出 `11` 而非 `7` | `SaveCodec.parseSummary` 不能只看位置，必须**按版本区分**：`RUN` 字段数 ≤ 4 判为 v1，行动点取第 2 字段（旧「点数」），否则取第 1 字段、金币取第 5 字段 | ✅ 已解决 |
| v1 存档里的节点类型名（`ENEMY` / `RANDOM` / `BOSS`）在 8 值枚举中已不存在 | 解析时丢弃该节点而非整档报废，其余字段照常读回；同理 `phase` 无法识别时退化为 `EXPLORING` —— 已写入版本兼容矩阵 | ✅ 已解决 |
| 第二步落地后 `NodeGeneratorTest.随机节点按概率出现且不超过节点数上限` **由绿转红** | 该用例用「必中」伪随机源断言特殊事件槽位应产生 `SPECIAL`，但第二步后槽位优先级被火箭队抢占（后期+剧情线已开时会改成 `ROCKET_CAPTURE`）。**先补可控伪随机源**（新增 `scripted(int...)` 按序返回给定的 roll 值，替代原先只能全体命中 / 全体落空的假源），再补一条「火箭队判定落空时退回通用特殊事件」的用例，把「槽位只有一个、按优先级占用」的规则固化；`NodeGeneratorTest` 9/9、全量 270/270 | ✅ 已解决 |

### 三、AI 使用记录

| 时间 | 提示词/用途 | AI 产出 | 核对结果 |
| --- | --- | --- | --- |
| 2026-09-10 | "为战斗系统增加内容：与训练家战斗时，在己方或对方所有精灵倒下后才退出战斗" | `Trainer` 模型 + `BattleEngine` 训练师轮战 + `BattleServices.newTrainerBattle` | ✅ 人工核对判定分支与回合消耗；`mvn -o test` 36/36 通过 |
| 2026-09-10 | "审查并补齐现有实现（敌方渲染 + 单测 + 文档）" | `BattleController` 改用 `foeActive()`；`BattleEngineTrainerTest` 9 用例；战斗服务文档 v1.4 | ✅ 敌方面板显示正确；断言覆盖整队全灭与回合规则 |
| 2026-09-10 | "从仓库拉取更新" | `git fetch` + `git merge origin/dev`（18 文件，无冲突）；`mvn -o compile` / `mvn -o test` 通过 | ✅ 合并后构建与测试全绿 |
| 2026-09-10 | "参照原版宝可梦，为战斗模块增加异常状态（如麻痹，中毒等）" | `StatusCondition` 规则中心 + `Pokemon` 状态 API + `BattleEngine` 行动/持续伤害结算 + `Move.inflicts` 与 CSV 列扩展 + `ItemCategory.CURE` 解除道具 + 界面状态徽章 + `BattleEngineStatusTest` 32 用例 + 战斗服务文档 v1.5 | ✅ 人工核对三类缺陷（麻痹扣 PP、解毒药不治剧毒、变化招不施异常）已修复；`mvn -o test` 68/68 通过 |
| 2026-09-10 | "我想要这样的架构：战斗模块仅是消费，其他道具等等数据从其他模块导入" | 端口 + 外部实现注入：新增 `BattleDataPort`/`BattleDataPorts`，`BattleEngine`/`BattleServices` 去除数据依赖并补端口重载，数据侧新增 `GameDataBattleDataPort` 适配器，组装层注入（`PokemonBattleAdapter#battleDataPort`），新增 `BattleDataPortTest` 8 用例 + 战斗服务文档 v1.6 §11 | ✅ 人工核对解耦约束（`org.example.battle` 包内无内建数据与数据模块引用）与向后兼容（旧签名保留，`mvn -o test` 76/76 通过） |
| 2026-09-10 | "结构是否符合这样：战斗模块的数据都从外部引入，而它充当引擎，负责战斗演算过程还有结算" | 核查后整改两处偏差：新增 `WildEncounter`（组装侧）承接遭遇生成，随机源改为可注入；`BattleServices` 收敛为纯工厂，`MainController`/`BattleConsole` 同步；新增 `WildEncounterTest` 5 用例 + 战斗服务文档 v1.7 §12 | ✅ 静态核查包内已无 `Math.random()` 与遭遇生成；`mvn -o test` 79/79 通过 |
| 2026-09-10 | "战斗模块是否知道所有道具种类，是否有对应使用接口，是否能产生对应效果" | 全量核查后交付结论：战斗模块不按 id 认识道具，只按 3 个 `ItemCategory` 分流，效果均已实现；列出 7 项缺口（无目标选择 / 无复活与 PP 回复类别 / 混乱不可解除 / 扩展需改代码 / 4 个道具无获取途径 / 双 `ItemCategory` 并存） | ✅ 静态核查 `org.example.battle` 包内无道具 id 字面量与注册表 |
| 2026-09-10 | "那么就 1"（补「道具目标选择」缺口） | 接口新增 `useItem(Item,int)`、原方法降为 `default`；引擎重排分支与目标规则；`BattleView#showBagMenu` 占位改真实渲染 + 新增 `showTargetMenu`；`BattleController` 两步菜单；demo 同步；`BattleEngineStatusTest` 新增 6 用例；文档升级（战斗服务 v1.8 / UI v0.1.8 / 用例 COMBAT-30~34 / 适配说明 §八 / 日报） | ✅ 人工核对目标规则（越界不消耗、倒下不可回复但可解除、球忽略目标）与向后兼容；`mvn -o test` 85/85 通过 |
| 2026-09-10 | "战斗模块不要负责升级，进化，而是结算后传给对应模块让它检查" | 新增端口 `BattleGrowthPort`（申报式 `settle`/`resolveLearn`/`none()`）与外部实现 `GrowthService`（经验 = 六维种族值总和 × 等级 / 5、单只下限 30、逐级升级/学招/进化）；引擎剥除 5 个成长方法并改 `settleGrowth()` 申报、`decideLearn` 转发；`BattleServices` 补 4 个成长重载、`PokemonBattleAdapter#battleGrowthPort`、`MainController`/`BattleConsole` 注入；新增 `GrowthServiceTest` 13 用例并改写 `BattleDataPortTest` 4 个成长用例 | ✅ 人工核对「引擎侧已无经验/升级/进化字面逻辑」与默认无成长（旧重载走 `none()`）；`mvn -o test` **98/98 通过**；文档升级（战斗服务 v1.9 / UI v0.1.9 / 用例 COMBAT-35~39 + GROW-09/10 / 适配说明 §九） |
| 2026-09-10 | "我需要你做一个对接文档：为了方便外部导入数据的那个模块的开发者，列出接口与其用途，还有已有功能，并说明使用方法以及需要的数据，以及需要那个模块完成的功能" | 新增 `docs/对接文档_战斗模块数据对接.md` **v1.0**（10 章）：§二 接口清单（`BattleDataPort` 4 方法逐个说明 + `BattleGrowthPort`）、§三 已有能力（接口表 / 演算规则 / 12 个工厂重载 / 不会做的事）、§四 使用方法（三步接入 + 5 类示例 + 学招抉择循环）、§五 数据契约与 **9 条一致性硬约束 C1~C9**、§六 交接清单 D-01~D-09 + 启动期自检骨架、§七 风险 R-01~R-07、§八 缺数据降级矩阵、§九 验收自测清单；`接口文档_战斗服务.md` 补 v1.9a 头部行与 §14 索引 | ✅ 用 `jshell --class-path target/classes` 探针实测校准文档中的数据契约，据此发现并记录 R-01（`species.csv` 列序错位导致学招/进化静默失效）与 R-02（双注册表） |
| 2026-09-10 | "推送" | 提交 `79d9ed7 docs: 新增面向外部数据模块的对接文档` 并推送 `feature/battle` | ✅ 推送后工作区干净、与远端同步 |
| 2026-09-10 | "拉取更新" | `git fetch --all --prune` + `git merge origin/dev`（**肉鸽系统** 21 文件 / +1347 / -7，无冲突）；`mvn -q compile` 通过、`mvn -o test` **108/108 通过**；随后核查发现肉鸽 3 个战斗入口漏注入成长端口并整改（抽出 `MainController#newWildBattle` / `#newTrainerBattle` 统一组装） | ✅ 合并无冲突、构建与测试全绿；漏注入问题已修复并复核 4 个调用点 |
| 2026-09-10 | "完成一个成长机制，根据玩家多次使用相同的精灵通关，提升后续他遇见的所有精灵的个体值" | 经 4 轮澄清定稿口径（**累计捕捉次数**驱动、只改个体值不改种族值、图鉴局外可查、战斗公式须计入个体值）后落地：新增 `growth` 模块三件套（`IvGrowthRule` 纯规则 / `SpeciesGrowthRecord` 图鉴条目 / `GrowthProgress` 图鉴数据接口 + 进程级单例）；`BattleGrowthPort` 补 `default onCaptured`，引擎在捕捉成功处申报；`GrowthService` 记录捕捉/对战次数；`PokemonServiceImpl` 经统一 `newPokemon` 叠加全局加成；`model.Pokemon#computeStats` 改标准IV公式、接缝 `toBattlePokemon` 补搬个体值；`GameSession`/`MainController` 串通单例；**新增 24 用例 → `mvn -o test` 132/132 通过**；战斗服务文档 v1.10 | ✅ 人工核对四条口径：捕捉 2 次 = +1 点、加成封顶 31、种族值完全不变、捕捉记录立即影响下一场遭遇；跨系统个体值搬运用「满加成 → 满个体」端到端断言锁定 |
| 2026-09-10 | "个体值加成是本地单机永久提升的成长机制，但有提升上限"（补充要求：**永久** + **有上限**） | 核查发现上限已满足（`IvGrowthRule.MAX_IV` 在规则层强制），但「永久」未满足 —— 原实现是进程级单例，**关掉程序即丢**。项目无存档系统（`StartView`「继续游戏」仅预留入口），故新增 `GrowthProgressStore` 自带极简存档：存档位于 `<用户主目录>/.pokerouge/growth-progress.txt`、UTF-8 可读文本、原子写、读写异常只打日志；`GrowthProgress#instance()` 改懒加载 `Holder` 并 `attachSaver` 自动落盘；新增 `GrowthProgressStoreTest` **14 用例**（全 `@TempDir`）；`PokemonBattleAdapterTest` / `PokemonSystemValidator` 改注入独立实例以隔离本机存档；**`mvn -o test` → 146/146 通过**；战斗服务文档补 §13.7 | ✅ 人工核对：上限不受手改存档影响（有专门断言）；并用 `jshell` 做**真实跨进程冒烟** —— 进程 A 写 6 次捕捉 + 1 次对战，进程 B（全新进程）读回加成/捕捉/对战完全一致，确认「关掉程序再打开仍保留」成立；冒烟后已清理本机存档 |
| 2026-09-10 | 按《需求文档》§4 全表重做「路线节点 / 行动点」 | `OptionType` 4 → 8 值；新增 `RouteConfig` 集中数值、`NodeGenerator`、`ShopStock`/`ShopView`；重写 `RunData`/`RogueTurnManager`/`RogueFloorView` 与 `MainController` 肉鸽段落；存档升 **v2**（`RUN` 7 字段、`OPTION`/`BOSS` 增「已走过」、`SaveSummary` 拆 `ap`+`gold`，v1 旧档可读）；测试重写并新增 `RouteConfigTest`/`NodeGeneratorTest`/`ShopStockTest`；**246/246 通过**；文档同步（存档 v2.0 / UI v0.1.12 / 用例 F 组 / 需求 §4.5） | ✅ 人工核对 AP 校验（不足拒绝且状态不变）、必然节点不占点、可失败一次与不可失败分支、v1 旧档迁移；`mvn -o test` 246/246 通过 |
| 2026-09-10 | "继续"（推进《需求文档》§五 火箭队剧情线与神兽偶遇） | `OptionType` 8 → **12 值**（`ROCKET`/`ROCKET_CAPTURE`/`LEGENDARY`/`ROCKET_INVASION`）并补 `defeatEndsRun()`；`RouteConfig` 增后期判定与四种节点的概率/行动点/金币/掉落/强度公式；`NodeGenerator#rollSpecialEvent` 按优先级占用唯一特殊事件槽位；`RunData` 增 5 个剧情线标记，`RogueTurnManager` 增 `markStorylineOnEnter`/`resolveRocketBossVictory`/`clearRun` 与冠军后三分支；`MainController` 增 3 个开战入口、战利品掉落与 `runEndReason` 文案；存档新增**可选** `STORY` 行（**不升格式版本**）；**新增 `RocketStorylineTest`(18) + `SaveStoryCodecTest`(5)，270/270 通过**；文档同步（存档 v2.1 / UI v0.1.13 / 用例 G 组） | ✅ 人工核对三分支（进过火箭队不抓捕→冠军后追加侵略战；已击败首领→直接通关；未碰剧情线→直接通关）、神兽每局一次与 0 点强触发、火箭队战败即结束不耗重试；`mvn -o test` 270/270 通过 |

### 四、明日计划

- [x] 与流程/肉鸽系统对接**训练师战入口**（道馆战、四天王、Boss 冠军战节点调用 `newTrainerBattle`；
  肉鸽路线节点已接入，**必然节点链的道馆 / 四天王 / 冠军战也已接入** `startMandatoryBattle`）
- [x] **需求三第二步：特殊事件进阶规则（已落地）** —— 火箭队节点（各时期均可能）与神兽偶遇（仅后期），
  含「火箭队抓捕神兽并击败首领后必然出现的神兽节点」（**0 行动点**）与高收益高风险分支；
  规格严格按《需求文档》§5.1 / §5.2 / §5.3 三分支实现，首版参数见 §4.5
- [ ] **技能机的数据形态**：现有 `ItemCategory` 只有回复 / 解毒 / 精灵球，商店暂不售技能机；
  需先确定是给 `ItemCategory` 加类别还是另设学习机制
- [ ] **必然节点通关后是否享受节点自回血**：当前首版**不享受**（只发金币），已记入需求文档 §九 第 11 项
- [ ] **数值调平衡**：段数（暂定 5）、AP 上限曲线（8 起步每段 +2）、金币奖惩、
  商店物价（每段 +15%）、各节点敌人数值全部集中在 `RouteConfig`，待实测后调参
- [ ] 训练师战界面：对手名与对方剩余精灵展示、逃跑/投球按钮置灰或提示
- [ ] 按数值定稿补充训练师队伍强度与胜利奖励（金币/经验）
- [ ] **图鉴界面**：当前仅提供数据接口（`GameSession#getGrowthProgress()`），
  待补 JavaFX 图鉴/成长面板展示各族捕捉次数、对战次数与个体值加成
- [x] **数值调参（已定稿）**：个体值加成速率为「每 2 次捕捉 +1 点」（`IvGrowthRule.CAPTURES_PER_STEP = 2`），
  全局加成口径为「各族之和、封顶 31」（`IvGrowthRule.MAX_IV = 31`，已满足「有提升上限」要求）；
  经需求方确认**维持现状**，后续如需调整只改 `IvGrowthRule` 一处
- [x] **存档目录归属（已落地）**：统一存档系统已交付，`<用户主目录>/.pokerouge/saves/slotN/` 下每档
  持有 `save.txt`（进度快照）与 `growth-progress.txt`（图鉴成长）；成长模块经
  `GrowthProgressStore#at(Path)` 交由 `SaveStore` 托管，成长规则与战斗模块**无需改动**
- [ ] **图鉴 / 成长面板界面的存档位展示**：面板当前只显示当前档位的成长（`GameSession#getGrowthProgress()`），
  待确认是否需要在面板里提示「成长仅对本存档位生效」
- [ ] 异常状态扩展：特性联动（如「静电」「免疫」）、携带道具自动解除（如木子果）、
  对战界面异常状态图标化（当前为文字徽章）
- [ ] 成长规则收敛：把 `org.example.growth.GrowthService` 与宝可梦系统
  `PokemonServiceImpl#gainExp` 的经验口径合并为一处，避免双源；确认 `Pokemon#addExp`
  与 `gainExp` 的升级回血差异是否统一
- [ ] **数据一致性（对接文档 §七 R-01/R-02）**：修正 `species.csv` 列序与注册表解析的错位
  （当前学招/进化静默失效，`createPokemon` 返回 0 技能），并收敛 `org.example.data.GameData`
  与 `org.example.pokemon.infrastructure.GameData` 两套注册表
- [ ] 确认肉鸽战斗的成长口径：路人 / 野外精灵 / 道馆 / 四天王 / 冠军是否统一走 `GrowthService`（当前是），
  节点奖励（金币已落地；道具 / 额外经验）是否需新增端口

### 五、风险与求助

- [x] 训练师战（道馆 / 四天王 / 冠军）的失败惩罚与重试次数归**流程系统**决定 —— 已按《需求文档》
  §4.3 落地：道馆与四天王**可失败一次**（大量扣金币，重试再败即结束），冠军**不可失败**；
  `RogueTurnManager#resolveMandatoryDefeat()` 返回布尔作为战败回执
- [ ] 各节点训练师队伍的生成规则（等级 / 数量 / 技能）已在 `RouteConfig` 给出按段递增的初值，
  待肉鸽系统与宝可梦系统确认最终的队伍组成与技能池
- [x] **特殊事件的内容空缺（已补齐）**：火箭队队员 / 火箭队首领（抓捕神兽）/ 神兽偶遇 / 首领侵略战
  四种节点的触发条件、战斗配置与奖励均已定义并落地（数值集中在 `RouteConfig`）；仍待确认的是
  「后期」起点（暂定第 3 段）与各概率的平衡取值
