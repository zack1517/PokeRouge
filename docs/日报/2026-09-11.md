# 日报

## 日期：2026-09-11　|　提交人：[待确认，肉鸽流程 / UI 负责人]

### 一、今日完成

- [x] **修复「打赢道馆后不进入下一段」**（试玩反馈）
  - 根因：`GameSession#resolveRogueMandatoryVictory()` / `RogueTurnManager#resolveMandatoryVictory()`
    早已实现并在 javadoc 里写明「由控制器调用」，但**全仓没有任何调用点**——控制器漏接线。
    `MainController#rogueBattleFinished` 的胜利分支只发了金币就调 `finishNodeStep()`，
    而 `RogueTurnManager#advanceAfterNode()` 在必然阶段会直接 `return false`（`phase != EXPLORING`），
    于是阶段永远停在 `GYM`，`afterRogueStep()` 下一拍又开一次道馆战（对手每次重新随机，
    表现为「同段道馆反复打、段落数不涨」）
  - 修复：必然节点（道馆 / 四天王 / 冠军 / 首领侵略战）胜利改走
    `GameSession#resolveRogueMandatoryVictory()`，**金币与阶段流转（下一段 / 四天王 / 冠军 / 通关）
    一次结算**；控制器不再单独调 `awardRogueWinGold`，避免重复发钱
  - 顺带修正 `GameSession#enterRogueSegment(int)` 的地图背景重抽判定：原实现把 `getSegment() != segment`
    写在 `enterSegment(segment)` **之后**，比较永远相等、背景从不重抽；改为先取 `previous` 再比较。
    `resolveRogueMandatoryVictory()` 也在段号变化时补上重抽
- [x] **路线节点改为「每走完一个节点整批刷新」**（试玩反馈：「节点刷新是每过一个节点就要刷新」，
  已用 `ask_user` 确认需求方要的就是这个行为）
  - `RogueTurnManager` 抽出 `generatePlan(int)`（开段与刷新共用同一套生成规则），新增 `refreshRoute()`：
    按当前段号与剧情线 / 神兽状态重抽一批节点替换旧列表，**AP、段号、阶段、金币与剧情线标记都不重置**
  - `GameSession#refreshRogueRoute()` 暴露给控制器；`RogueTurnManager#resolveNode()`（非战斗节点）也补了刷新
  - `MainController#finishNodeStep()` 改为 `finishNodeStep(boolean refreshRoute)`：
    **刷新必须发生在 `advanceRogueNode()` 之前**——否则 AP 归零时旧列表里的 0 点节点（免单野外 / 特殊事件）
    会让 `RunData#hasSelectableOption()` 仍为 true，道馆永远触发不出来
  - 必然节点路径传 `false`：此时列表已由 `enterSegment()` / `enterPhase()` 重新生成，再刷一次属于覆盖新列表
- [x] 单测：`RogueTurnManagerTest` 26 → **29 例**，新增刷新相关 3 例
  （AP / 段号 / 阶段 / 金币不变且旧节点被替换、常驻三类必在且不超过 `MAX_ROUTE_NODES`、
  待触发的神兽偶遇 0 点且刷新后仍在）；`mvn -o test` **278/278 通过**
- [x] 文档同步：
  - `docs/需求文档.md`：§4.1 新增「每走完一个路线节点整批重新随机生成」条目并改写常驻节点条目
    （明确「同一批内一次性」只约束非常驻节点）；§4.2 表前补引用块（随机概率是**每批**重新判定、
    常驻必占 3 槽位）；§4.5 参数表与实现进度表各补一行
  - `docs/接口文档_UI模块.md` → **v0.1.15**：§3.6 补刷新说明及其对「已走过 N 次」标签的影响；
    §4.8 新增 `GameSession#refreshRogueRoute()`（标注为写操作，仅 `finishNodeStep(true)` 调用），
    并把误写的 `RunData#getRouteOptions()` 更正为 `getAvailableOptions()`；版本记录补 v0.1.15
  - `docs/测试用例草稿.md`：§F 计数 54 → **57**；NODE-01b~01e 按「同段内刷新」改写，
    新增 NODE-01g（每步刷新）/ NODE-01h（道馆胜利进下一段）
- [x] 代码已推到 `origin/feature/save-flow`（`dev` 与远端分叉，按需求方要求先只推自己的提交，未做合并）
- [x] **界面流转与存档读取补全**（需求：「存档机制不够完善，我要能够读取存档，以及不要直接退出游戏，
  而是到初始主界面，以及肉鸽结束就在初始主界面选择新游戏」）
  - `MainView`：动作栏改为「进入层内事件 / 保存游戏 / **读取存档** / **返回主界面**」，
    `Actions` 新增 `onLoadGame()`；`MainController#onExit` 由「二次确认后 `Platform.exit()`」
    改为 `returnToStartScreen()`（不退出进程，窗口关闭的二次确认保留给 `bindStageEvents`）
  - 新增 `MainController#showLoadSelection()`（战斗守卫 → 空会话守卫 → `autoSave()` →
    `SaveSlotView(CONTINUE, ...)`）；`finishRogueRun()` / `ensureRogueBattleReady()` 的全灭分支
    与 `returnToStartScreen()` 统一收敛到同一出口（先 `autoSave()` 再置空字段再回启动页）
  - 顺带修掉一处**死档**：`loadFromSlot()` 顶部加守卫，已结束的一轮（`isRogueRunFinished()`）
    拒绝载入并提示「本轮已结束」，避免读进去后立即被流程判定再次结束
  - 单测：`SaveManagerTest` 的往返用例补 `assertTrue(restored.isRogueRunFinished())`；`mvn -o test` **278/278 通过**
  - 文档：`接口文档_存档系统.md` → **v2.3**、`接口文档_UI模块.md` → **v0.1.16**、
    用例草稿新增 SAVE-19~24、`QA问题表.md` 第 19/20 行
- [x] **经验改为「每击倒一只即时发放」并接入种族经验值**（需求：「每次打死一个精灵都可以获得经验，
  经验多少和等级相关，还有精灵种族值」；经 `ask_user` 确认两点都要改）
  - 模型：`org.example.model.Species` 新增 `baseExpYield`（带参构造器 + `getBaseExpYield()`），
    10 参构造器保留并委托（`baseExpYield = 0`），既有调用方零改动
  - 接缝：`PokemonBattleAdapter#toBattleSpecies` 搬运新体系 `getBaseExpYield()`（对齐 `species.csv`
    的 `baseExp` 列，如妙蛙种子 64 / 妙蛙草 142 / 妙蛙花 236）；游戏主流程全部经此转换故真实生效
  - 公式：`GrowthService` 经验折算改为 **`baseExpYield × 对方等级 / 7`**（单只不低于 30），
    种族未提供 baseExp 时回退「六维种族值总和 × 等级 / 5」
  - 时机：`BattleEngine#settleGrowth(Pokemon defeated)` 改为**每击倒一只申报一次**（野生倒下 /
    训练师每只倒下，且在派出下一只之前），**战败不回收**已发放经验；新增整场一次获胜申报
    `BattleGrowthPort#onBattleWon(survivors)`（`default` 空实现），图鉴对战次数改由它按**场次**累计
  - UI 联动：`BattleController#render` 调整为「**挂起学招抉择优先** → 结局 → 战斗菜单」，
    因为经验 / 学招现在可能在战斗进行中产生
  - 单测：`BattleEngineTrainerTest` +4（逐只申报、野生即时、战败不回收、无端口降级）、
    `GrowthServiceTest` 重写经验折算 6 例 + 图鉴 2 例、`PokemonBattleAdapterTest` +1；`mvn -o test` **287/287 通过**
  - 文档：`接口文档_战斗服务.md` → **v1.11**（§3.4/§3.5/§5/§13.1~13.5）、
    `对接文档_战斗模块数据对接.md` → **v1.1**（§2.2/§4.6/§5.1/§六 D-05/§7 R-01/§九）、
    `适配说明_宝可梦技能队伍接口v1.md` §十二、`接口文档_UI模块.md` → **v0.1.17**、
    用例草稿 COMBAT-35/36/39 改写 + 新增 COMBAT-41/42、GROW-01/09/10/15 改写
- [x] **修复「打败路人一只精灵看不到获得经验」**（试玩反馈：「exp，怎么没有能实现在打败路人一只精灵的
  情况下就能获得经验呢」）
  - 复现：临时诊断用例驱动「我方 Lv.41 vs 路人训练家 3 只」轮战，逐只击倒后 **经验确实逐只入账**
    （0 → 117 → 217），但战斗日志里只有「… 倒下了！」与「… 派出了 …！」，**一行经验提示都没有**
  - 根因：`GrowthService#grow` 只在**升级时**才写日志（`if (gainedLevels <= 0) return;`），
    而本作升级曲线为 `expToNextLevel = 3n²+3n+1`（41 级需 5167 点），单只对手只给约 100 点，
    升级极少发生 —— 于是「经验涨了但界面毫无变化」，被误判为「没有获得经验」。**接线本身正确**
    （`MainController` 三个路人 / 火箭队 / 必然节点入口都注入了 `battleGrowthPort`，
    引擎在 `switchToNextHealthy()` 之前逐只 `settle`，`survivors()` 与 `player.getParty()` 同一对象图）
  - 修复：`GrowthService#grow` 改为**每次结算都追加可见反馈** ——
    「`名字 获得了 N 点经验！（当前/升级所需）`」；升级时该行在前、随后逐行「`升到了 Lv.N！`」；
    满级（`Pokemon#MAX_LEVEL`）不再产生该行。**即便当次不足以升级也能看到经验入账**
  - 单测：`GrowthServiceTest` +2（未升级时返回带进度的经验日志、升级时经验行在升级行之前）、
    `BattleEngineTrainerTest` +1（注入真实 `GrowthService`，断言「击倒一只后日志出现获得经验」且经验入账）；
    诊断用例用完即删；`mvn -o test` **290/290 通过**
  - 文档：`接口文档_战斗服务.md` → **v1.12**（版本记录 + §13.3 规则表新增「经验可见反馈」+ §13.5 用例数）、
    `适配说明_宝可梦技能队伍接口v1.md` §十二、用例草稿 GROW-01 改写 + 新增 GROW-18
- [x] **清理测试对玩家真实存档的污染 + 修复根因（测试隔离缺陷）**
  - 现象：通读存档文本时发现 `C:\Users\lenovo\.pokerouge\growth-progress.txt` 出现
    `mine_sp=0,270` / `mine_evo=0,6` —— **测试用的假物种 id**，时间戳正对应跑测试的时间
  - 处置：先把该文件清理为仅注释（用户级默认档；`saves/slot1/growth-progress.txt` 的
    `bulbasaur=0,1` 是真实数据，未动）；游戏内影响为零（假 id 不匹配任何真实物种）
  - 根因：`BattleDataPortTest` 的两例（`升级学招经由成长模块查询技能`、`升级进化经由成长模块查询种族`）
    用了**单参** `new GrowthService(port)` → 回落到 `GrowthProgress#instance()`，而它绑定
    `<用户主目录>/.pokerouge/growth-progress.txt` 且 `attachSaver` **接管落盘**；两例都打到
    `PLAYER_WIN` → `reportBattleWon` → `recordBattle("mine_sp"/"mine_evo")` **每跑一次测试 +1**
  - 修复：`GrowthServiceTest`（26 处）与 `BattleDataPortTest`（2 处）全部改为注入纯内存
    `new GrowthProgress()` 的两参形式；给 `GrowthService(BattleDataPort)` 与
    `PokemonBattleAdapter#battleGrowthPort(BattleDataPort)` 补 `@apiNote` 警示
  - 单测：`mvn -o test` **290/290 通过**；**回归验证**：跑完测试后真实存档 mtime 不变、内容仍为两行注释
  - 文档：`接口文档_战斗服务.md` → **v1.13**（版本记录 + §13.5 测试隔离约定 + §13.7 测试隔离行 / 与存档系统关系更新）、
    用例草稿顶部补隔离约定
- [x] **将 `dev` 拉到远端最新并合并 `feature/save-flow`**（需求：「把项目下载为 dev 最新版」，澄清为云端→本地）
  - `git fetch --prune` 后发现远端已删除 `origin/feature/flow-save-dev`、`origin/feature/rouge`；
   本地 `dev` 落后 31 个提交，确认无本地独有提交后 `merge --ff-only` 快进到 `72d007c`
  - 但 `dev` **不含**测试隔离修复，在 dev 上跑测试仍会污染真实存档 —— 经 `ask_user` 确认后
   把 `feature/save-flow` 合并进 `dev`（带入 `8b83746` + `4916a6c`，12 个文件），
   唯一冲突为 `接口文档_战斗服务.md` 版本行（取 v1.13）
  - 合并后 `mvn -o -B test` 仍失败 3 个测试类；用独立 worktree 在**纯净 `origin/dev`** 上复现同样失败，
   确认属 dev 既有失败（`BattleEngineHeldItemTest` / `NodeGeneratorTest` / `SaveSystemTest`，**非本次合并引入**）
  - `dev` 现为 `91989f5`（ahead 3），按需求方要求**暂不推送**
- [x] **商店扩充固定倍率球种 + 修正道具名不一致**（需求：「把商店选项拓展」；经 `ask_user` 定下
  第一阶段范围「球类上架 + 固定倍率球，条件倍率球后续再补」）
  - 数据：`items.csv` 与 `GameData#registerBuiltinItems` 同步新增 **纪念球 ×3 / 贵重球 ×3 /
   狩猎球 ×4.5 / 竞赛球 ×4.5**。纪念球与贵重球在正作中就是「基准倍率」的收藏球（与精灵球同效），
   因此保持 ×3 不作增强，商店里与精灵球同价
  - 商店：`ShopStock.CANDIDATES` 12 → **16 条**，按段解锁（纪念球 / 狩猎球第 2 段、竞赛球第 3 段、
   贵重球第 4 段、大师球仍第 5 段），售价走既有的「基础价 × (1 + 15% × (段数−1))」通胀
  - **修历史 bug**：`ShopStock` 自存了一份中文名，`i_awakening` 在商店叫「醒神药」、
   在 `items.csv` / `GameData` 里叫「醒睡药」。现改为**统一从道具注册表取名**
   （`GameData.instance().item(id).getName()`），只在注册表查不到该 id 时退回兜底名，从结构上消除名字漂移
  - 单测：`ShopStockTest` 10 → **13 例**：新增「新增球种按解锁段位进入商品池」、
   「球类商品倍率来自道具注册表」、「商品展示名与道具注册表一致」（80 组随机源逐一比对），
   并把「只上架已解锁的商品」强化为「第 1 段采样集合恰等于解锁集合」
  - 全量 `mvn -o -B test` **334 例、7 例失败**；在 HEAD 基线 worktree 上复现同 7 例，
   确认失败集合与本次改动**无关**（均为随机伤害导致的既有失败）；真实存档仍零污染
  - 文档：`接口文档_战斗服务.md` → **v1.14**、`对接文档_战斗模块数据对接.md` §3.2 球倍率清单改写、
   `接口文档_UI模块.md` §ShopView 补「展示名统一取注册表」、用例草稿 F 组计数 57 → **60** +
   新增 SHOP-04/05/06
  - **未做（留作后续）**：条件倍率球（等级球 / 月亮球 / 诱饵球 / 友友球 / 甜蜜球 / 速度球 /
   沉重球 / 巢穴球 / 先机球 / 梦境球 / 究极球）—— `Item` 目前只有「固定倍率 + 必定捕获 + 解除范围」
   三个语义槽，还需给 `Species` 补体重字段、给捕捉入口补「回合数 / 场地」上下文；
   携带物（约 60 种）与树果（约 30 种）同样需要扩 `HeldItemEffect` 与新增树果机制，尚未开始
  - **范围澄清（需求方明确答复）**：树果里的「努力值降低」类 —— 榴石果（HP−10）/ 藻根果（攻击−10）/
    比巴果（防御−10）/ 哈密果（特攻−10）/ 萄葡果（特防−10）/ 茄番果（速度−10）**这 6 种一律不加入**
    （洗努力值与亲密度均为本作尚未实现的系统），已记入 `QA问题表.md` 第 23 行

#### （七）批次①：18 件低成本可携带装备 + 装备落存档（新增）

- **背景**：需求方贴出完整道具对照表（约 50 件携带物 + 约 30 种树果），确认「全都要」，并定下
  **①低成本携带物 → ②树果机制 → ④招式标记 / 会心 / 畏缩 → ③能力等级系统**的实施顺序
- **代码**：
  - `HeldItemEffect` 枚举 **6 → 19** 种；`HeldItem` 新增 9 个语义化参数解析入口，统一「`|` 分段」约定
  - `equipment.csv` **8 → 26 行**，`GameData#registerBuiltinEquipment` 同步扩到 26 件并新增
    `equipment(id)` / `allEquipment()` 查询入口
  - `BattleEngine` 完成全部结算接线（伤害 / 速度 / 先后手 / 回合末 / 保命 / 天气延长），
    `Pokemon#effectiveSpeed()` 成为速度比较的唯一入口
  - 单测：`BattleEngineHeldItemTest` 10 → **32 例**、`HeldItemTest` **8 例**，全绿
- **顺带修掉 3 个真实缺陷**：
  1. **装备完全不落存档**（`org.example.save` 全链路无装备字段，肉鸽发的装备读档后全部消失）
     → `PP` 行末新增第 15 字段 `heldItemId`；**不升 `FORMAT_VERSION`**，旧档缺字段由 `field()` 越界
     兜底按空串读回，天然向前兼容
  2. **补齐技能后重建队伍时静默丢弃装备** → 删除 `SaveData.PokemonData` 的「省略装备」便捷构造器，
     改为必填参数，让编译器兜住同类遗漏（症状极具欺骗性：写出的文本里有装备、读回来却没有）
  3. **黑色铁球速度减半静默失效** —— `HeldItem#doubleParam()` 遇多段参数 `0.5|GROUND` 整体解析失败
     返回兜底 1.0 → 改为「整体解析失败则退化取首段」
- **测试基座修复**：`Pokemon.create(species, level, pool)` 隐式随机个体值，使「有装备 / 无装备」两组
  的 HP / 攻 / 防 / 速面板都不同，伤害比、回血量、最大 HP 比例全部不可比 —— 这才是 dev 基线上
  6 例既有失败的真实根因（此前误判为「随机伤害浮动」）。测试改用固定 31 个体值，生产代码保留
  随机个体值语义
- **验证**：`mvn -o -B test` **363 例，仅 2 例失败**，且用 `git worktree` 在基线 `0f958de` 上双向复现
  确认**均为改动前既有**（`NodeGeneratorTest` 1 例；`com.bao01.save.SaveSystemTest` 偶发）。真实存档
  零污染（3 份文件 mtime 未变）
- **文档**：`接口文档_战斗服务.md` v1.14 → **v1.15**（§15 全节重写）、`接口文档_存档系统.md`
  v2.3 → **v2.4**、`对接文档_战斗模块数据对接.md` v1.1 → **v1.2**、`测试用例草稿.md` 新增
  COMBAT-43~48、`QA问题表.md` 第 24/25 条
- **未做（后续批次）**：树果机制（约 29 种，批次②）、招式标记 / 会心 / 畏缩（批次④）、
  能力等级系统（批次③）、商店出售携带物 —— 均已排入 todo

### 二、今日遇到的问题与处置

| 问题 | 处置方式 | 状态 |
| --- | --- | --- |
| 打赢第 1 段道馆后不进入第 2 段，道馆战反复触发 | 定位为「接口写好了但控制器漏调用」：必然节点胜利改走 `resolveRogueMandatoryVictory()`，并明确金币只发一次 | 已解决 |
| 同段内节点列表一直不变，与需求方预期不符 | `ask_user` 确认要「每走完一个节点重新随机生成本段剩余节点」→ 新增 `refreshRoute()` 并在 `finishNodeStep` 里刷新 | 已解决 |
| 刷新若发生在 `advanceRogueNode()` 之后，AP 归零时道馆可能不触发 | 固定调用顺序：刷新 → 推进 → `afterRogueStep()` | 已解决 |
| `enterRogueSegment()` 地图背景从不重抽 | 把 `getSegment()` 比较提前到 `enterSegment()` 之前 | 已解决 |
| 本地 `dev` 与 `origin/dev` 分叉（ahead 4 / behind 18） | 按需求方要求仅推 `feature/save-flow`，`dev` 暂不动 | 待需求方决定是否合并 |
| 战败后仍能「继续游戏」，读进去立刻又被判失败（死档循环） | `loadFromSlot()` 顶部加守卫：`isRogueRunFinished()` 的档拒绝载入并提示「本轮已结束」；单测补 `assertTrue(restored.isRogueRunFinished())` 防止回归 | 已解决 |
| 把 `settle` 改为「逐只申报」后 `结算时按参战精灵累计对战次数` 断言失败（expected 1 but 0） | 属预期影响：对战次数随申报从 `settle` 迁到 `onBattleWon`。改由 `GrowthService#onBattleWon` 调 `recordBattles`，并重写该组用例为「获胜申报累计 / 击倒结算不累计 / 未参战不写入」三例 | 已解决 |
| 经验改为逐只即时结算后，经验与升级可能发生在**战斗进行中**，此时若技能栏已满会挂起学招抉择 | 原 `BattleController#render` 只在 `!engine.isOngoing()` 时消费抉择，会导致「抉择无处消费、技能悄悄丢失」。改为**抉择优先**：队列非空即弹学招菜单并 return，处理完再回战斗菜单或结局；UI 文档补渲染顺序 | 已解决 |
| `species.csv` 的 `baseExp` 在内建注册表路径上读不到（`GameData#applySpeciesRow` 列序错位，R-01） | 本次未修列序（超出范围）：仅加 javadoc 说明该路径 `baseExpYield = 0` 会回退旧公式。游戏主流程走 `PokemonBattleAdapter`，baseExp 真实生效；已在对接文档 §7 R-01 记录 | 记录待修 |
| 「打败路人一只精灵没有获得经验」（实为**已入账但无可见反馈**） | 用临时诊断用例复现并确认经验 0 → 117 → 217 确实逐只入账、接线无误；根因是 `GrowthService#grow` 只在升级时写日志。改为**每次结算都追加**「获得了 N 点经验！（当前/升级所需）」战斗日志，未升级也能看到入账 | 已解决 |
| 跑测试污染开发机**真实存档**（`<用户主目录>/.pokerouge/growth-progress.txt` 出现假物种 `mine_sp=0,270`） | 清理污染数据 + 定位根因：测试走 `new GrowthService(port)` 单参会回落到绑定真实存档的单例 `GrowthProgress#instance()`，且 `BattleDataPortTest` 两例打到获胜分支即落盘。改为全测试注入纯内存 `new GrowthProgress()`，两个单参入口补 `@apiNote` 警示 | 已解决（含回归验证：测试后真实存档 mtime/内容不变） |
| 合并 `feature/save-flow` 进 `dev` 后全量测试仍失败 | 用独立 worktree 在**纯净 `origin/dev`** 上跑同一套用例，复现同样的失败集合 → 确认是 dev 既有失败（既有用例用真实随机源、断言写死数值），与本次合并无关；已在验收记录中标注 | 记录待他人修 |
| 商店里「醒神药」与背包 / 数据表里的「醒睡药」名字不一致 | 根因是 `ShopStock` 自己维护了一份中文名数组。改为统一从道具注册表取名并可加兜底，另加用例逐一比对展示名与注册表，防止再次漂移 | 已解决 |
| 商店球类只覆盖 4 种，条件倍率球暂缺 | 条件倍率球需要 `Item` 支持条件倍率、`Species` 支持体重、捕捉入口支持「回合数 / 场地」，属跨模块改动；按需求方确认的**分阶段**方案本版只做固定倍率球，条件倍率球与携带物 / 树果留作后续 | 记录待做 |
| 纪念球 / 贵重球与精灵球同为 ×3，在商店里属「同效同价」的道具 | 忠实还原正作（这两球本就是基准倍率的收藏球），未擅自加数值；已在代码注释、对接文档与日报中写明，若需求方希望它们有专属卖点需另行设计 | 待需求方确认 |
| 肉鸽奖励发的携带装备**读档后全部消失** | 根因是 `org.example.save` 全链路没有装备字段（写档 `render` 与读档 `parse` 都只有 14 个精灵字段）。给 `PP` 行末加第 15 字段 `heldItemId`，`SaveData` / `SaveCodec` / `PokemonMapper` 三处打通；旧档缺字段由 `field(parts, index)` 越界兜底读成空串，故**不升 `FORMAT_VERSION`** | 已解决 |
| 装备写进存档文本了，读回来却是「未携带」 | `SaveCodec#parse` 末尾「补齐技能后重建队伍」走了 `PokemonData` 的「省略装备」便捷构造器，把 `heldItemId` 静默重置为空串。删除该构造器改必填参数（编译期兜住），另补 5 个回归用例 | 已解决 |
| 黑色铁球「速度降低 50%」完全不生效（实测速度 115 而非 58） | `HeldItem#doubleParam()` 只对 `DAMAGE_TYPE` 取 `|` 后段，其余效果整体 `Double.parseDouble(param)`；黑铁球 param 是 `0.5\|GROUND` → 解析异常 → 返回兜底 1.0。改为「整体解析失败则退化取首段」，并逐个核对全部 9 个调用点的参数形态 | 已解决 |
| `BattleEngineHeldItemTest` 在 dev 基线上就有 6 例失败，一度以为是「随机伤害浮动」，加 `new Random(42)` 也治不好 | 真正根因是 `Pokemon.create(species, level, pool)` 内部调用 `Stats.randomIv()`，每次构建面板都不同 —— 比对「有装备 / 无装备」两组时连 HP / 攻 / 防 / 速都不可比。测试改用固定 31 个体值（生产代码保留随机个体值语义） | 已解决 |
| `com.bao01.save.SaveSystemTest` 断言「capture → restore → capture 稳定」偶发失败 | 旧演示体系 `PartyEntry` 只存 `(speciesId, level, currentHp)`，**不存个体值**，`restore` 重算面板后 `maxHp` 变化使残血换算落点漂移。用 worktree 在基线 `0f958de` 上双向复现（基线 6 次 2 次通过 / 工作树 6 次 3 次通过），确认为**改动前既有**的偶发；正式体系存了个体值无此问题 | 记录待他人修 |
| 讲究系锁定招式后，若该招式 PP 耗尽将永远无法出招 | 本作**没有「挣扎」兜底机制**，会形成死锁。`choiceAllows()` 改为在锁定招式 `exhausted()` 时**自动解禁**（允许换招）—— 属实现方自主决策，**未经需求方确认**，已记入 `QA问题表.md` 第 24 条 | 待需求方确认 |

### 三、AI 使用记录

| 时间 | 提示词/用途 | AI 产出 | 核对结果 |
| --- | --- | --- | --- |
| 上午 | 「打赢了第一段道馆为什么不进去下一段，而且节点刷新是每过一个节点就要刷新」 | 用 `grep` 确认 `resolveRogueMandatoryVictory` 全仓无调用点、定位 `advanceAfterNode()` 在必然阶段直接 return；给出控制器接线修复 + 刷新实现 | 已核对：`MainController` 6 处 `finishNodeStep` 调用点参数正确；全量测试 278/278 通过 |
| 上午 | 「推送到远端」「开一个分支 feature/save-flow」 | 发现 `dev` 与远端分叉，改为在 `feature/save-flow` 上推送 | 已核对：`origin/feature/save-flow` 建立跟踪 |
| 上午 | 「存档机制不够完善，我要能够读取存档，以及不要直接退出游戏，而是到初始主界面，以及肉鸽结束就在初始主界面选择新游戏」 | 补齐读取存档入口、把「退出游戏」改为回启动页、一轮结束回启动页；另发现并修掉「已结束的一轮仍可载入」的死档 | 已核对：`MainController` 三个出口统一走 `returnToStartScreen()`；`mvn -o test` 278/278 通过 |
| 上午 | 「每次打死一个精灵都可以获得经验，经验多少和等级相关，还有精灵种族值」 | 经 `ask_user` 确认「即时结算」与「用种族经验值」都要改；把经验折算改为 `baseExpYield × 等级 / 7` 并让引擎逐只申报，另新增获胜申报供图鉴按场次计数 | 已核对：`species.csv` 的 `baseExp` 经适配器真实生效；逐只申报/战败不回收/无端口降级均有新增用例；`mvn -o test` 287/287 通过 |
| 上午 | 「exp，怎么没有能实现在打败路人一只精灵的情况下就能获得经验呢」 | 先全链路核查接线（入口注入、`resolveRoundEnd` 申报时机、`survivors()` 对象图）均正确，再用临时诊断用例跑「Lv.41 vs 路人 3 只」实测：经验 0 → 117 → 217 确实逐只入账，但日志只有「倒下了！」。定位为 `grow` 仅在升级时写日志 → 改为每次结算都追加经验反馈行 | 已核对：诊断日志修复后出现「小水龟 获得了 108 点经验！（108/5167）」；新增 3 例回归用例；诊断用例已删除；`mvn -o test` 290/290 通过 |
| 下午 | 「我在那看看存档文本」/「需要删了，优化检查一下代码有没有问题，没问题就上传」 | 用 `[System.IO.File]::ReadAllText(...UTF8Encoding($false))` 读出两份 `save.txt` 与两份 `growth-progress.txt` 全文并逐行解释（发现 slot1 是 HP=0 + `gameOver=1` 的死档），顺带识别出测试污染。清理污染后 `grep` 测试目录定位到单参构造器回落单例的根因，批量替换 28 处调用点并加 Javadoc 警示 | 已核对：4 个文件无编译错误；`mvn -o test` 290/290 通过；跑测试后真实存档 mtime 与内容**不变**（零污染回归验证） |
| 下午 | 「把项目下载为 dev 最新版」（澄清：云端→本地，不是上传） | 用 `git fetch --prune` + `merge --ff-only` 把本地 `dev` 追到 `origin/dev`(72d007c)；发现 dev 缺测试隔离修复，经 `ask_user` 确认后合并 `feature/save-flow`，用 worktree 在纯净 dev 上复现既有失败以自证无新增破坏 | 已核对：`git merge-base --is-ancestor`、`git diff --name-only origin/dev` 确认未误改战斗/节点生成代码；合并后跑测试真实存档 mtime 未变 |
| 下午 | 「现在把商店选项拓展」+「给我拉个表现在有哪些道具在游戏内」 | 先盘点游戏内道具全表（12 消耗品 + 8 装备）与商店定价 / 上架机制，指出 3 个既有问题（商店硬编码名与数据表不一致、两处 `ItemCategory` 不同步、装备无法购买）；经 `ask_user` 分阶段后落地「固定倍率球上架」 | 已核对：`ShopStockTest` 13/13 通过；全量 334 例中失败 7 例与 HEAD 基线完全一致；真实存档零污染 |
| 下午 | 贴出完整道具对照表（约 50 件携带物 + 约 30 种树果）+「这些树果不用加入」（努力值降低类 6 种）+ 经 `ask_user` 选定排期 | 精读引擎能力边界后输出四批分类表（标出本作无对应系统而不实现的条目），按 ①→②→④→③ 落地批次①：18 件装备 + 存档字段 + 全部结算接线；过程中自查出「装备完全不落存档」「重建队伍静默丢装备」「黑色铁球速度失效」3 个真实缺陷并修复；用 worktree 隔离验证 2 例失败为基线既有 | 已核对：`mvn -o -B test` 363 例仅 2 例失败（worktree 双向复现确认改动前既有）；`SaveCodecTest`/`PokemonMapperTest`/`HeldItemTest`/`BattleEngineHeldItemTest` 全绿；真实存档 3 份文件 mtime 未变 |

### 四、明日计划

- [ ] **常驻节点可重复进入 + 每步刷新的难度平衡**：医院只要 AP 够就能反复全队满血，路人 / 野外可反复
  刷金币与捕获次数，难度上限完全由 AP 额度决定 —— 实测后考虑是否限制同段内次数或提高常驻节点消耗
- [ ] 节点刷新后的**节奏体感**：当前每次刷新必出 3 个常驻节点 + 最多 2 个随机节点，
  实测是否出现「想要的商店刷不出来」的挫败感；必要时考虑保底机制
- [ ] 技能机数据形态（`ItemCategory` 加类别，还是另设学习机制）
- [ ] **批次②：树果机制 + 约 29 种树果**（异常治疗 7 / HP 回复 6 / PP 回复 1 / 属性减伤 16）——
     需要新增「携带树果」的触发通道（回合末 / 受击 / 危机时刻）与消耗语义
- [ ] **批次④：招式标记 + 会心 + 畏缩**（拳击手套 / 凸凸头盔 / 防尘护目镜 / 锐利之爪 / 王者之证 /
     节拍器）—— 需要给 `Move` 补「接触 / 声音 / 拳 / 粉末 / 连续攻击」标记，并新增会心与畏缩判定
- [ ] **批次③：能力等级系统**（弱点保险 / 打空保险 / 爽喉喷雾 / 四场地种子 / 五危机树果 /
     亚开果 / 香罗果 / 清净坠饰 / 突击背心）—— 需要新增 stat stages（−6~+6）并改造伤害与速度入口
- [ ] **商店出售携带物**：装备目前只能从肉鸽 REWARD 事件随机获得，`ShopStock.CANDIDATES` 尚不含装备
- [ ] 必然节点（道馆 / 四天王 / 冠军）通关后是否享受 1/5 自回血 —— 当前**不享受**，待定
- [ ] 图鉴 / 成长面板 JavaFX 界面
- [ ] `dev` 与 `origin/dev` 的分叉处理（是否合并、何时合并）

### 五、风险与求助

- [ ] **请需求方确认**：本次两道修复提交在 `feature/save-flow`（已推送），`dev` 仍与远端分叉
  （ahead 4 / behind 18），是否需要一个合并 / 强推方案
- [ ] `RouteConfig` 里的刷新相关数值（`MAX_ROUTE_NODES = 5`、常驻必占 3 槽）偏紧，
  随机节点的出现概率被压缩，实测后可能需要放宽上限
- [ ] 「节点刷新」这一改动会**同时放大**高风险与高收益节点的期望值（火箭队 / 神兽偶遇可被反复刷到），
  剧情线触发频率可能超出设计预期
- [ ] **请需求方确认**：讲究系（讲究眼镜 / 讲究围巾）锁定招式后，**若该招式 PP 耗尽，本作会自动解禁
  允许换招** —— 这是为绕开「本作没有挣扎机制」的死锁而做的自主决策（`QA问题表.md` 第 24 条）。
  若希望忠实还原正作「PP 耗尽即只能挣扎」，需要先给本作补上挣扎兜底
- [ ] **请需求方确认**：黑色铁球的「地面化」采用「属性免疫由 0 倍抬到 1 倍」的**近似实现**
  （原作语义是「去掉飞行属性带来的地面免疫」）。对双属性宝可梦的伤害计算等价，但若后续要做
  「属性面板 / 特性展示」，两者需要对同一份规则口径
- [ ] 讲究系「+50%」目前用**装备内联乘算**实现（讲究眼镜走伤害乘区、讲究围巾走 `effectiveSpeed()`），
  而**不是**特攻 / 速度的能力等级提升。批次③ 能力等级系统落地后效果一致，但会与「能力等级上限 +6」
  的规则脱钩，届时需决定是否迁移
- [ ] 装备暂**无跨精灵唯一性校验**（`Player#equip` 已有「同一只精灵换穿自动脱下」，但允许两只精灵
  携带同一件装备）。若打算做成「一件装备只能装一只」，需要在队伍层面加约束