# 日报

## 日期：2026-09-11　|　提交人：[待确认，肉鸽流程 / UI 负责人]

### 一、今日完成

- [x] **修复「打赢道馆后不进入下一段」**（试玩反馈）
  - 根因：`GameSession#resolveRogueMandatoryVictory()` / `RogueTurnManager#resolveMandatoryVictory()`
    早已实现并在 javadoc 里写明「由控制器调用」，但**全仓没有任何调用点**——控制器漏接线。
    `MainController#rogueBattleFinished` 的胜利分支只发了金币就调 `finishNodeStep()`，
    而 `RogueTurnManager#advanceAfterNode()` 在必然阶段会直接 `return false`（`phase != EXPLORING`），
    于是阶段永远停在 `GYM`，`afterRogueStep()` 下一拍又开一次道馆战（对手每次重新随机，
    表现为「同段道馆反复打、段落数不涨」）
  - 修复：必然节点（道馆 / 四天王 / 冠军 / 首领侵略战）胜利改走
    `GameSession#resolveRogueMandatoryVictory()`，**金币与阶段流转（下一段 / 四天王 / 冠军 / 通关）
    一次结算**；控制器不再单独调 `awardRogueWinGold`，避免重复发钱
  - 顺带修正 `GameSession#enterRogueSegment(int)` 的地图背景重抽判定：原实现把 `getSegment() != segment`
    写在 `enterSegment(segment)` **之后**，比较永远相等、背景从不重抽；改为先取 `previous` 再比较。
    `resolveRogueMandatoryVictory()` 也在段号变化时补上重抽
- [x] **路线节点改为「每走完一个节点整批刷新」**（试玩反馈：「节点刷新是每过一个节点就要刷新」，
  已用 `ask_user` 确认需求方要的就是这个行为）
  - `RogueTurnManager` 抽出 `generatePlan(int)`（开段与刷新共用同一套生成规则），新增 `refreshRoute()`：
    按当前段号与剧情线 / 神兽状态重抽一批节点替换旧列表，**AP、段号、阶段、金币与剧情线标记都不重置**
  - `GameSession#refreshRogueRoute()` 暴露给控制器；`RogueTurnManager#resolveNode()`（非战斗节点）也补了刷新
  - `MainController#finishNodeStep()` 改为 `finishNodeStep(boolean refreshRoute)`：
    **刷新必须发生在 `advanceRogueNode()` 之前**——否则 AP 归零时旧列表里的 0 点节点（免单野外 / 特殊事件）
    会让 `RunData#hasSelectableOption()` 仍为 true，道馆永远触发不出来
  - 必然节点路径传 `false`：此时列表已由 `enterSegment()` / `enterPhase()` 重新生成，再刷一次属于覆盖新列表
- [x] 单测：`RogueTurnManagerTest` 26 → **29 例**，新增刷新相关 3 例
  （AP / 段号 / 阶段 / 金币不变且旧节点被替换、常驻三类必在且不超过 `MAX_ROUTE_NODES`、
  待触发的神兽偶遇 0 点且刷新后仍在）；`mvn -o test` **278/278 通过**
- [x] 文档同步：
  - `docs/需求文档.md`：§4.1 新增「每走完一个路线节点整批重新随机生成」条目并改写常驻节点条目
    （明确「同一批内一次性」只约束非常驻节点）；§4.2 表前补引用块（随机概率是**每批**重新判定、
    常驻必占 3 槽位）；§4.5 参数表与实现进度表各补一行
  - `docs/接口文档_UI模块.md` → **v0.1.15**：§3.6 补刷新说明及其对「已走过 N 次」标签的影响；
    §4.8 新增 `GameSession#refreshRogueRoute()`（标注为写操作，仅 `finishNodeStep(true)` 调用），
    并把误写的 `RunData#getRouteOptions()` 更正为 `getAvailableOptions()`；版本记录补 v0.1.15
  - `docs/测试用例草稿.md`：§F 计数 54 → **57**；NODE-01b~01e 按「同段内刷新」改写，
    新增 NODE-01g（每步刷新）/ NODE-01h（道馆胜利进下一段）
- [x] 代码已推到 `origin/feature/save-flow`（`dev` 与远端分叉，按需求方要求先只推自己的提交，未做合并）

### 二、今日遇到的问题与处置

| 问题 | 处置方式 | 状态 |
| --- | --- | --- |
| 打赢第 1 段道馆后不进入第 2 段，道馆战反复触发 | 定位为「接口写好了但控制器漏调用」：必然节点胜利改走 `resolveRogueMandatoryVictory()`，并明确金币只发一次 | 已解决 |
| 同段内节点列表一直不变，与需求方预期不符 | `ask_user` 确认要「每走完一个节点重新随机生成本段剩余节点」→ 新增 `refreshRoute()` 并在 `finishNodeStep` 里刷新 | 已解决 |
| 刷新若发生在 `advanceRogueNode()` 之后，AP 归零时道馆可能不触发 | 固定调用顺序：刷新 → 推进 → `afterRogueStep()` | 已解决 |
| `enterRogueSegment()` 地图背景从不重抽 | 把 `getSegment()` 比较提前到 `enterSegment()` 之前 | 已解决 |
| 本地 `dev` 与 `origin/dev` 分叉（ahead 4 / behind 18） | 按需求方要求仅推 `feature/save-flow`，`dev` 暂不动 | 待需求方决定是否合并 |

### 三、AI 使用记录

| 时间 | 提示词/用途 | AI 产出 | 核对结果 |
| --- | --- | --- | --- |
| 上午 | 「打赢了第一段道馆为什么不进去下一段，而且节点刷新是每过一个节点就要刷新」 | 用 `grep` 确认 `resolveRogueMandatoryVictory` 全仓无调用点、定位 `advanceAfterNode()` 在必然阶段直接 return；给出控制器接线修复 + 刷新实现 | 已核对：`MainController` 6 处 `finishNodeStep` 调用点参数正确；全量测试 278/278 通过 |
| 上午 | 「推送到远端」「开一个分支 feature/save-flow」 | 发现 `dev` 与远端分叉，改为在 `feature/save-flow` 上推送 | 已核对：`origin/feature/save-flow` 建立跟踪 |

### 四、明日计划

- [ ] **常驻节点可重复进入 + 每步刷新的难度平衡**：医院只要 AP 够就能反复全队满血，路人 / 野外可反复
  刷金币与捕获次数，难度上限完全由 AP 额度决定 —— 实测后考虑是否限制同段内次数或提高常驻节点消耗
- [ ] 节点刷新后的**节奏体感**：当前每次刷新必出 3 个常驻节点 + 最多 2 个随机节点，
  实测是否出现「想要的商店刷不出来」的挫败感；必要时考虑保底机制
- [ ] 技能机数据形态（`ItemCategory` 加类别，还是另设学习机制）
- [ ] 必然节点（道馆 / 四天王 / 冠军）通关后是否享受 1/5 自回血 —— 当前**不享受**，待定
- [ ] 图鉴 / 成长面板 JavaFX 界面
- [ ] `dev` 与 `origin/dev` 的分叉处理（是否合并、何时合并）

### 五、风险与求助

- [ ] **请需求方确认**：本次两道修复提交在 `feature/save-flow`（已推送），`dev` 仍与远端分叉
  （ahead 4 / behind 18），是否需要一个合并 / 强推方案
- [ ] `RouteConfig` 里的刷新相关数值（`MAX_ROUTE_NODES = 5`、常驻必占 3 槽）偏紧，
  随机节点的出现概率被压缩，实测后可能需要放宽上限
- [ ] 「节点刷新」这一改动会**同时放大**高风险与高收益节点的期望值（火箭队 / 神兽偶遇可被反复刷到），
  剧情线触发频率可能超出设计预期

---

## 附加记录：`feature/battle` 分支（战斗系统）　|　日期：2026-09-11

### 一、今日完成

- [x] **己方精灵倒下时改为「由玩家选择接下来上场的精灵」**（试玩反馈：原来倒下后引擎自动换宠，
  玩家没有选择权，界面也无法表达「该派谁上场」）
  - 方案：**引擎层硬门禁 + 界面层选择面板**两层
    - `BattleEngine` 新增 `awaitingReplacement` 状态与 `isAwaitingReplacement()`；
      `resolveRoundEnd()` 的玩家倒下分支不再调 `Player.switchToNextHealthy()`，改为
      `awaitingReplacement = true` + 日志「请选择接下来上场的精灵！」
    - 新增 `chooseReplacement(int partyIndex)`：派出选中精灵、`clearVolatileState()` 使其从中立状态开始、
      追加「你派出了 X！」并压入一条玩家方 `SEND_OUT` 演出事件；**不消耗回合、敌方不行动**
    - 把原来的 `requireOngoing()` 升级为 `requirePlayerAction()`（同时校验 `awaitingReplacement`），
      4 个行动方法（`useMove` / `useItem` ×2 / `tryRun` / `switchActive`）全部经它把关 ——
      等待补位期间一律抛 `IllegalStateException`，**任何调用方（JavaFX 界面 / 控制台 demo / 测试）
      都无法绕过选择直接出招**
    - 判负条件由 `switchToNextHealthy() == null` 改为 `player.hasHealthyPokemon()`
      （语义等价且不再改动 `activeIndex`）
  - `BattleService` 契约补 `isAwaitingReplacement()` / `chooseReplacement(int)` 声明与 javadoc，
    并为 4 个行动方法补 `@throws` 说明；`playerActive()` 语义改为「补位等待期间为刚倒下的那只，非 null」
  - UI 接线：`BattleView.Actions` 新增 `onReplacementSelected(int)`，
    新增 `showReplacementMenu(party, activeIndex, onPick)`（复用队伍 3×2 六格，
    `renderPartyGrid` 支持 `onBack == null` 时不渲染返回键）；
    `BattleController.render()` 在 `isOngoing()` 之后**优先**判断等待补位并弹出面板（不可返回主菜单）；
    `BattleConsole` 主循环插入 `doReplacement(BattleService)` 与界面行为对齐
  - **敌方（训练师）倒下后仍自动派下一只**，行为不变（需求方反馈只针对己方）
- [x] 单测：新建 `BattleEngineReplacementTest`（**8 例**）覆盖硬门禁（等待期间四种行动全部抛异常且状态不变）、
  补位语义（满血上场 / 恢复行动 / 从中立状态开始 / 产生玩家方 `SEND_OUT`）、
  非法选择（倒下精灵 / 越界下标不生效且保持等待）、队伍全倒时直接判负
- [x] 既有用例改写 4 处（原断言「自动换宠」）：`BattleEngineTrainerTest` ×2（含「最后一只倒下才判败」中
  补插 `chooseReplacement`）、`BattleEventTest` ×1、`BattleEventHpSnapshotTest` ×1；
  `BattleAnimationSmokeTest.NoopActions` 补空实现
- [x] 测试结果：本模块全绿 —— `BattleEngineReplacementTest` 8 / `BattleEngineTrainerTest` 9 /
  `BattleEventTest` 11 / `BattleEventHpSnapshotTest` 7 / `BattleEngineStatusTest` 38 /
  `BattleEngineStatStageTest` 20 / `BattleEngineSpecialStatusMoveTest` 20（合计 **105/105**）；
  全量 `mvn -o test` 375 例，9 个失败**全部为既有**（见下）
- [x] 文档同步：
  - `docs/接口文档_战斗服务.md` → **v1.14**：新增 **§3.3a 己方倒下后的补位**
    （情形表 + `chooseReplacement` 语义表 + 界面接线）、新增 **§19 倒下后的玩家补位**
    （动机 / 分层设计 / 行为细节 / 测试覆盖 + 用例改写对照表）；
    §3.1 `PLAYER_LOSE` 行、§3.2 行动方法前置条件与 `useMove` 副作用、§3.3 查询表
    （`playerActive()` 改说明 + 新增 `isAwaitingReplacement()` 行 + 表格补 `chooseReplacement`）、
    §3.4 训练师轮战对比表（新增「己方倒下后」行）、§6 状态机（新增补位等待子状态与己方/敌方分支）、
    §16/§18 相关措辞（`SEND_OUT` 产生时机、`clearVolatileState` 调用点、两份动手写示例）同步修订
  - `docs/接口文档_UI模块.md` → **v0.1.16**：§3.3 新增「己方倒下后的补位面板」界面要求
    （优先于主菜单渲染、复用队伍六格、**无返回键**、选中后 `chooseReplacement`、非法选择保持可重试）；
    §4.1 契约表补两个新方法并修订行动方法与 `playerActive()` 说明（不再为 null）；
    §6.4 / §6.6 去除「自动换宠」旧描述；版本记录补 v0.1.16
  - `docs/测试用例草稿.md`：COMBAT-09 由「自动换上下一个健康精灵」改写为「弹补位面板由玩家选择」；
    文件头补 COMBAT-41~45 的引用块；新增 **COMBAT-41~45** 五条（硬门禁 / 补位语义 / 非法选择 /
    判负边界 / 界面补位面板）
  - `docs/需求文档.md`：§4.4 新增「战斗中的补位」条目（玩家选择、挂起期间禁止行动、满血并从中立状态登场、
    不消耗回合、全倒才判负）；§4.5 实现进度表补一行

### 二、今日遇到的问题与处置

| 问题 | 处置方式 | 状态 |
| --- | --- | --- |
| 只在界面层拦截「必须先选替补」可被控制台 demo / 测试绕过（`status` 仍是 `ONGOING`） | 把约束放进引擎：`requirePlayerAction()` 统一把关，等待期间所有行动方法抛 `IllegalStateException` | 已解决 |
| 旧测试断言「倒下后自动换宠」 | 逐条改写为「先断言等待状态 → 再 `chooseReplacement` → 断言恢复」，共 4 处 | 已解决 |
| 新测试首轮 4 处失败：断言用了 `Pokemon.getName()` 当 id | `getName()` 返回 `species.getName()`（测试里物种 id 与名称同为 `mine_b_sp`），按实际返回值修正断言 | 已解决 |
| 新测试用「混乱」验证「上场后从中立状态开始」不成立 | `clearVolatileState()` 按设计**不清除混乱**（倒下时由 `takeDamage` 另行清除），改用 `setSeeded(true)` / `isSeeded()` 验证 | 已解决 |
| 全量测试出现 `BattleEngineHeldItemTest` 失败 | 用 `git stash` 跑基线对比：**基线与改动后都会失败且失败集合每次不同**（6~7 例浮动），属既有 flaky（该系列依赖 `new Random(42)` 的固定序列，且受本地 `growth-progress.txt` 的全局个体值加成影响），与本次改动无关 | 结论：不计入本次验收 |
| 全量测试的 `SaveSystemTest` 失败（1~2/73 条） | 单独跑通过、全量跑失败 → 判定为**测试顺序/全局状态污染**导致的既有问题；`git stash` 基线同样失败 | 结论：既有问题，非本次引入 |
| `NodeGeneratorTest` 1 例失败 | 基线与改动后均失败 | 结论：既有问题 |

### 三、AI 使用记录

| 时间 | 提示词/用途 | AI 产出 | 核对结果 |
| --- | --- | --- | --- |
| 下午 | 「当己方精灵倒下时，要让玩家选择接下来上场的精灵」 | 定位 `resolveRoundEnd()` 的自动换宠分支；给出「引擎硬门禁 + 界面选择面板」方案并完成 `BattleEngine` / `BattleService` / `BattleView` / `BattleController` / `BattleConsole` 五处改动与测试 | 已核对：`mvn -o -q compile` 通过；目标测试 105/105 通过 |
| 下午 | （工具调用中断后）「卡住了」 | 用 `git status --short` 核对改动完整性、重新编译确认无丢失，继续完成文档同步 | 已核对：改动完整、无遗留 `git stash` |
| 下午 | 排查全量测试的 8~9 个失败 | 用 `git stash` 跑基线对比，确认 `BattleEngineHeldItemTest` / `NodeGeneratorTest` / `SaveSystemTest` 的失败在基线**同样存在**且计数浮动 | 已核对：基线 367 例 8 失败（含 `SaveSystemTest`），改动后 375 例 9 失败，失败集合一致 |

### 四、明日计划

- [ ] 与需求方确认：**训练师（敌方）**倒下后是否也需要玩家选择派出精灵，还是保持自动（当前保持自动）
- [ ] 补位面板的键鼠操作细节（是否允许方向键 + 回车、是否需要二次确认）
- [ ] 观察 `BattleEngineHeldItemTest` 的 flaky 波动是否值得单独修（固定随机源 + 隔离本地成长存档）
- [ ] `dev` / `feature/battle` 的合并时机（本次改动尚未推送）

### 五、风险与求助

- [ ] **请需求方确认**：等待补位期间引擎抛出 `IllegalStateException` 属硬约束，界面必须先判
  `isAwaitingReplacement()`；若后续新增战斗入口（如自动战斗）需要显式处理该状态，否则会直接抛错
- [ ] `BattleEngineHeldItemTest` 的 flaky 会持续干扰「全量测试是否全绿」的判定，建议单独排期修复
