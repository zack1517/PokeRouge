# 日报

## 日期：2026-09-12　|　提交人：[待确认，肉鸽流程 / UI 负责人]

### 一、今日完成

- [x] **批次④：招式标记 + 会心 + 畏缩 + 连招增伤**（按 09-11 排期 ①→②→④→③ 推进）
  - **新增招式标记体系** `org.example.model.MoveFlag`（`CONTACT` 接触 / `PUNCH` 拳 / `POWDER` 粉末）。
    此前引擎只能看到招式的属性 / 类别 / 威力，无法区分「这招是不是接触类」，
    凸凸头盔、拳击手套、防尘护目镜三件装备因此**无法实现**。
  - `org.example.model.Move`：新增 `flags` 字段与 **12 参全量构造器**（末位 `Set<MoveFlag>`），
    **旧 11 参构造器保留**并委托为 `Set.of()`（既有调用方零改动）；
    新增 `getFlags()/hasFlag()/isContact()/isPunch()/isPowder()`。
  - `moves.csv`：表头 11 → **12 列**（末尾新增 `flags`，`|` 分隔，留空表示无标记），
    **数据行数不变仍为 36**；`GameData#applyMoveRow` 用 `c.length > 11` 判断，
    因此 v1.16 的 11 列旧表**仍能加载**。当前标注 12 条：`CONTACT` 9 条、`PUNCH` 3 条、`POWDER` 2 条。
  - **同版新增两套通用战斗机制**（不只是装备）：
    - **会心**：基础等级 0 → **1/24**，1 级 **1/8**、2 级 **1/2**、≥3 级**必定**（`CRIT_DENOMINATORS = {24,8,2}` /
      `CRIT_ALWAYS_STAGE = 3`），伤害 ×**1.5**，日志追加「**，击中要害！**」，由 `CRIT_BOOST` 提供等级。
    - **畏缩**：新增挥发性状态 `Pokemon#flinched`（`isFlinched()/setFlinch()/clearFlinch()`），
      命中后无法行动（日志「XX 畏缩了，无法行动！」），**回合末与换人时清除**。
- [x] **6 件批次④ 装备**（`equipment.csv` 56 → **62 件**，`HeldItemEffect` 23 → **29 种**）：
  - 拳击手套 `PUNCH_BOOST 1.1`：拳类招式威力 ×1.1，且携带者使出拳类招式时**该招式不再视为接触**（免吃凸凸头盔反伤）。
  - 凸凸头盔 `CONTACT_PUNISH 0.1667`：被**接触类**招式命中时反伤攻击方 `max(1, 最大HP × 0.1667)`（约 1/6），**不消耗**。
  - 防尘护目镜 `POWDER_IMMUNE`（**空 param**）：免疫粉末类招式（**含变化类的催眠粉 / 毒粉**，直接落空）+ 免疫沙暴 / 冰雹回合末削血。
  - 锐利之爪 `CRIT_BOOST 1`：会心等级 0 → 1（1/24 → 1/8）。
  - 王者之证 `FLINCH_CHANCE 10`：造成伤害后 10% 概率使目标畏缩，**变化类招式不触发**。
  - 节拍器 `CONSECUTIVE_BOOST 0.2`：连续使用同一招式每层 ×`1 + 0.2 × (次数 − 1)`，**封顶 ×2.0**，**换招归零**。
- [x] **粉末免疫刻意放在 `executeMove` 入口（而非 `performAttack`）**
  - `executeMove` 对 `isStatus()` 招式会**先走 `tryInflict` 分支**再考虑进伤害路径。
    若把粉末判定写在 `performAttack` 里，**催眠粉 / 毒粉这两个变化类粉末招式会永远漏掉**。
    顺带**删掉**原先 `performAttack` 内那段永不生效的死代码检查。
- [x] 单测：
  - `BattleEngineHeldItemTest` 46 → **60 例**（新增 14 例批次④：拳类增伤 / 非拳类无效、凸凸头盔接触反伤 /
    非接触不反伤 / 被拳套豁免、锐利之爪会心伤害 ×1.5 / 会心概率 1/24 → 1/8、王者之证触发畏缩 /
    变化类不触发、节拍器逐层增伤封顶两倍 / 未携带不增伤、防尘护目镜免疫粉末且不消耗 / 只免疫粉末 / 免疫沙暴回合末伤害）。
  - `HeldItemTest` 13 → **14 例**：6 件装备的 effectType 与 param 断言（含 `POWDER_IMMUNE` 的**空参数**）；
    注册总数断言 56 → **62**（`equipment.csv 应注册 32 件装备 + 30 种树果`）。
  - **新增 `MoveFlagTest` 10 例**：`parseFlags` 的多标记 / 单标记 / 空白与大小写不敏感 / `null` 与空串与
    `"||"` 返回空集 / 无法识别片段忽略（含「前缀相同的错误名称」不误匹配）/ 去重 / 返回集合不可变；
    `Move` 的旧构造器等价无标记、12 参构造器写入标记、`null`/空集归一、**防御性拷贝**。
  - `mvn -o -B test` 基线 **382** → **407** 例，**仅 2 例失败且均为改动前既有**
    （`com.bao01.save.SaveSystemTest` 偶发、`org.example.model.NodeGeneratorTest` 1 例），**新增失败 0 例**。
- [x] 文档同步（5 份）：
  - `docs/接口文档_战斗服务.md` → **v1.17**：新增 §15.1.1「招式标记 `MoveFlag`」（枚举 + `parseFlags` 容错语义 +
    `Move` 新旧构造器对照表）、§15.6「招式标记数据列」（新旧表头对照 + 12 条标注清单 + 标注原则）；
    §15.2 效果一览 23 → 29 种（新增「招式标记与判定类」6 件表格）；§15.3 结算点补 6 行
    （含 `POWDER_IMMUNE` 在 `executeMove` **入口**、会心/畏缩的判定位置）+ 畏缩清除时机 + 七处语义决策引用块；
    §15.5 测试覆盖 46 → 60 / 13 → 14 并新增 `MoveFlagTest`（10）；回归基线 382 → 407。
  - `docs/对接文档_战斗模块数据对接.md` → **v1.4**：§3.2 伤害公式补「携带装备倍率」与「会心 ×1.5」、
    新增畏缩条目；§5.1 `Move` 契约补 `flags` 可选参数与字段表；§5.3 `moves.csv` 列定义补第 12 列
    （含「拳类必须同时标 `CONTACT`」与当前 12 条标注清单）、`equipment.csv` 56 → 62 并新增
    批次④ 6 种 `effectType` 对照表；§7 **R-07 修订**（原文称「引擎不做暴击」，v1.17 起会心已实现）。
  - `docs/QA问题表.md`：新增第 **30~36** 条（批次④ 七处实现方自主决策，均标「待确认」）。
  - `docs/测试用例草稿.md`：新增 **COMBAT-53~66**（14 条，对应新增的 14 个引擎用例）+ 顶部批次④ 说明块
    （含「概率类断言必须打散随机种子」的注意事项）；v1.15 / v1.16 的旧计数改标注为「当时」。
  - `docs/日报/2026-09-12.md`（本文件）。

### 二、今日遇到的问题与处置

| 问题 | 处置方式 | 状态 |
| --- | --- | --- |
| 变化类粉末招式（催眠粉 / 毒粉）若把免疫判定写在 `performAttack` 里会**永远不生效** | 把 `POWDER_IMMUNE` 判定移到 `executeMove` **入口**（`isStatus()` 分支之前），并删掉 `performAttack` 里那段永不触发的死代码 | 已解决 |
| 概率类用例（会心 1/24、畏缩 10%）直接 `new Random(i)` 循环时 `nextInt(8)` 恒为常数，一度怀疑实现有 bug | 根因是 `java.util.Random` 小种子的首个 `next(31)` 高位**近似线性**。改用「打散种子」（`i * 0x9E3779B97F4A7C15L + 0x9E3779B97F4A7C15L`）或「遍历种子取第一个会触发者」的确定性写法；已把该坑写入测试用例草稿与接口文档 §15.5 | 已解决 |
| 批次④ 用例比对「有装备 / 无装备」时伤害比不稳定 | 沿用 09-11 的结论：`Pokemon.create(species, level, movePool)` 每次都会 `Stats.randomIv()`，必须用固定 `FIXED_IVS = Stats(31,31,31,31,31,31)` 创建精灵，否则连 HP / 攻 / 防 / 速都不可比 | 已解决 |
| `equipment.csv` 的 `POWDER_IMMUNE` 参数留空，担心被加载器整行跳过 | 加载器只要求「**5 列**」而 `param` 恒为第 4 列，写出空列即可（`...,POWDER_IMMUNE,,说明`）。已在对接文档 §5.3 显式写明该陷阱，并在 `HeldItemTest` 断言其 param 为空 | 已解决 |
| 凸凸头盔反伤可能「救不回来地」打死攻击方，一度不确定是否要给保命装备兜底 | 反伤不打在 `survivedByHeldItem` 路径上（该钩子只在「被招式命中」时调用），因此**反伤不受气势披带 / 气势头带保护、可以击倒攻击方**，与正作一致。已记入 `QA问题表.md` 第 31 条 | 待需求方确认 |
| 节拍器的连招增伤是否需要做成「能力等级」 | 批次③ 能力等级系统**尚未落地**，本版按讲究系先例用**装备内联乘算**（记录为引擎内存态、键为精灵 `uuid`、不落存档）。已记入 `QA问题表.md` 第 36 条，批次③ 完成后需复核是否迁移 | 待需求方确认（依赖批次③） |
| 畏缩在「速度更慢者施加」时感觉毫无作用 | 属正作语义：畏缩在回合末与换人时清除，**只对「本回合尚未行动」者生效**。已在文档写明并记入 `QA问题表.md` 第 32 条 | 待需求方确认 |
| 防尘护目镜「免疫沙暴 / 冰雹」是需求方对照表里没写、但正作有、且 `HeldItemEffect` javadoc 早已声明的行为 | 按 javadoc 补齐实现（两条都免疫），并在 `executeMove` 播报「XX 借助防尘护目镜，粉末类招式没有命中！」、在 `weatherChip` 播报「XX 借助防尘护目镜，不受沙暴影响！」。已记入 `QA问题表.md` 第 33 条 | 待需求方确认 |
| 全量测试出现 2 例失败，需确认是否为本次引入 | 与 09-11 基线（382 例 / 2 例失败）**同一集合**：`com.bao01.save.SaveSystemTest`（旧演示体系不存个体值的偶发，见 QA 第 25 条）与 `org.example.model.NodeGeneratorTest` 1 例。本次新增 25 例全绿，**无新增失败** | 记录待他人修（既有） |

### 三、AI 使用记录

| 时间 | 提示词/用途 | AI 产出 | 核对结果 |
| --- | --- | --- | --- |
| 上午 | 「继续」（按 09-11 排期推进批次④） | 落地批次④：新增 `MoveFlag` 枚举与 `Move` 标记字段（旧构造器保留）、`moves.csv` 第 12 列、6 件装备与 29 种 `HeldItemEffect`、**会心系统**（1/24 · 1/8 · 1/2 · 必定，×1.5）与 **畏缩**挥发性状态 | 已核对：`mvn -o -B test "-Dtest=BattleEngineHeldItemTest,HeldItemTest"` 74 例全绿（60 + 14） |
| 上午 | （自查）粉末免疫接线位置 | 复查 `executeMove` 的分支顺序，发现粉末判定若留在 `performAttack` 则**变化类粉末招式永远漏掉**；改到 `executeMove` 入口并删除死代码 | 已核对：新增「防尘护目镜免疫粉末类招式」「只免疫粉末类招式」两例覆盖变化类与伤害类两条路径 |
| 上午 | （自查）概率类用例结果异常 | 用临时探针确认「朴素 `new Random(i)` 循环」下会心比例恒为 0、打散种子后为 17/400（≈1/24）、锐利之爪 46/400（≈1/8），定位为 `java.util.Random` 小种子高位的线性相关而非实现缺陷 | 已核对：临时探针用例用完即删；改写的概率断言稳定通过；该注意事项已写入 `测试用例草稿.md` 与接口文档 §15.5 |
| 下午 | （自查）测试是否污染开发机真实存档 | 检查 `C:\Users\lenovo\.pokerouge\`：3 份文件（`growth-progress.txt`、`saves\slot1\save.txt`、`saves\slot2\save.txt`）mtime 均为 2026-09-11 及更早，**零污染** | 已核对：跑完全量测试后再次比对 mtime，未变 |
| 下午 | 「继续」（推进批次④ 文档与提交） | 补齐 `MoveFlagTest`（10 例）后同步 5 份文档：接口文档 v1.17（§15.1.1 / §15.6 / §15.2 / §15.3 / §15.5）、对接文档 v1.4（§3.2 / §5.1 / §5.3 / R-07）、QA 第 30~36 条、用例草稿 COMBAT-53~66、本日报 | 已核对：文档中的每个数字都与 `moves.csv` / `equipment.csv` / surefire 实际输出逐一对齐（36 条招式 / 62 件装备 / 12 条标注 / 407 例）；全量 `mvn -o -B test` 407 例仅 2 例既有失败 |

### 四、明日计划

- [ ] **批次③：能力等级系统**（弱点保险 / 打空保险 / 爽喉喷雾 / 四场地种子 / 五危机树果 /
      亚开果 / 香罗果 / 清净坠饰 / 突击背心）—— 需要新增 stat stages（−6~+6）并改造伤害与速度入口；
      落地后需复核两处「用内联乘算代替能力等级」的既有实现（讲究系、**节拍器**）是否迁移
- [ ] **商店出售携带物**：装备目前只能从肉鸽 REWARD 事件随机获得，`ShopStock.CANDIDATES` 尚不含装备
- [ ] 常驻节点可重复进入 + 每步刷新的难度平衡：医院只要 AP 够就能反复全队满血，路人 / 野外可反复
      刷金币与捕获次数，难度上限完全由 AP 额度决定 —— 实测后考虑是否限制同段内次数或提高常驻节点消耗
- [ ] 节点刷新后的**节奏体感**：当前每次刷新必出 3 个常驻节点 + 最多 2 个随机节点，
      实测是否出现「想要的商店刷不出来」的挫败感；必要时考虑保底机制
- [ ] 技能机数据形态（`ItemCategory` 加类别，还是另设学习机制）
- [ ] 剩余招式的标记补全：当前只标注了 12 条（`CONTACT` 9 / `PUNCH` 3 / `POWDER` 2），
      其余物理招式是否算接触（如双刃头锤、岩石爆击等）需按正作逐条核对；
      「蘑菇孢子」尚未进 `moves.csv`
- [ ] 必然节点（道馆 / 四天王 / 冠军）通关后是否享受 1/5 自回血 —— 当前**不享受**，待定
- [ ] 图鉴 / 成长面板 JavaFX 界面
- [ ] `dev` 与 `origin/dev` 的分叉处理（是否合并、何时合并）；批次 ①②④ 三个提交目前均**未推送**

### 五、风险与求助

- [ ] **请需求方确认**：批次④ 有七处实现方自主决策（`QA问题表.md` 第 30~36 条）——
      ① **会心与畏缩只在「招式命中并造成伤害到未倒下的目标」之后判定**（变化类、0 倍招式都不参与）；
      ② 凸凸头盔反伤**不消耗装备、不受保命装备保护、可以击倒攻击方**；
      ③ 畏缩**只在「目标本回合尚未行动」时实际生效**（回合末与换人时清除）；
      ④ 防尘护目镜**同时免疫沙暴 / 冰雹回合末削血**（需求方对照表只写了免疫粉末招式）；
      ⑤ 粉末免疫判定放在 `executeMove` **入口**（否则变化类粉末招式永远漏掉）；
      ⑥ 拳类招式**同时标注 `CONTACT`**（即不持拳套时仍会被凸凸头盔反伤）；
      ⑦ 节拍器用**装备内联乘算**而非能力等级（与讲究系同一处理方式，批次③ 后需复核）
- [ ] **会心是全局机制变更**：本版把会心做成了「所有攻击都会按 1/24 判定」，**不只是锐利之爪的附属效果**。
      这会同时影响既有的全部伤害预期与正作的伤害口径，请确认这是预期范围；
      若要关闭基础会心（只让锐利之爪生效），把默认等级改为「负值即不判定」即可，属一行改动
- [ ] **招式标记需要数据侧逐条核对**：`CONTACT` / `PUNCH` / `POWDER` 目前只标注了 12 条招式，
      其余物理招式**默认无标记**（= 不会触发凸凸头盔、不吃拳套增伤）。这意味着在补全之前，
      凸凸头盔的强度**低于正作**（很多接触招式不反伤），请需求方确认是否要按正作全量补标
- [ ] 讲究系「+50%」与节拍器「+20%/层」目前都用**装备内联乘算**实现，而**不是**能力等级提升。
      批次③ 落地后效果可能一致，但会与「能力等级上限 +6」的规则脱钩，届时需决定是否迁移
- [ ] 装备暂**无跨精灵唯一性校验**（`Player#equip` 已有「同一只精灵换穿自动脱下」，但允许两只精灵
      携带同一件装备）。若打算做成「一件装备只能装一只」，需要在队伍层面加约束
- [ ] **请需求方确认**：本轮批次 ①②④ 三个提交**均未推送**（`dev` 与 `origin/dev` 仍分叉），
      是否需要一个合并 / 强推方案
- [ ] `RouteConfig` 里的刷新相关数值（`MAX_ROUTE_NODES = 5`、常驻必占 3 槽）偏紧，
      随机节点的出现概率被压缩，实测后可能需要放宽上限
- [ ] 旧验收体系 `com.bao01.save.SaveSystemTest` 的偶发失败仍未处理（`QA问题表.md` 第 25 条②），
      影响「全量测试是否全绿」的判断，建议尽早清理或标注为预期失败
