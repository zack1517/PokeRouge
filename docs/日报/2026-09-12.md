# 日报

## 日期：2026-09-12　|　提交人：[待确认，肉鸽流程 / UI 负责人]

### 一、今日完成

- [x] **批次④：招式标记 + 会心 + 畏缩 + 连招增伤**（按 09-11 排期 ①→②→④→③ 推进）
  - **新增招式标记体系** `org.example.model.MoveFlag`（`CONTACT` 接触 / `PUNCH` 拳 / `POWDER` 粉末）。
    此前引擎只能看到招式的属性 / 类别 / 威力，无法区分「这招是不是接触类」，
    凸凸头盔、拳击手套、防尘护目镜三件装备因此**无法实现**。
  - `org.example.model.Move`：新增 `flags` 字段与 **12 参全量构造器**（末位 `Set<MoveFlag>`），
    **旧 11 参构造器保留**并委托为 `Set.of()`（既有调用方零改动）；
    新增 `getFlags()/hasFlag()/isContact()/isPunch()/isPowder()`。
  - `moves.csv`：表头 11 → **12 列**（末尾新增 `flags`，`|` 分隔，留空表示无标记），
    **数据行数不变仍为 36**；`GameData#applyMoveRow` 用 `c.length > 11` 判断，
    因此 v1.16 的 11 列旧表**仍能加载**。当前标注 12 条：`CONTACT` 9 条、`PUNCH` 3 条、`POWDER` 2 条。
  - **同版新增两套通用战斗机制**（不只是装备）：
    - **会心**：基础等级 0 → **1/24**，1 级 **1/8**、2 级 **1/2**、≥3 级**必定**（`CRIT_DENOMINATORS = {24,8,2}` /
      `CRIT_ALWAYS_STAGE = 3`），伤害 ×**1.5**，日志追加「**，击中要害！**」，由 `CRIT_BOOST` 提供等级。
    - **畏缩**：新增挥发性状态 `Pokemon#flinched`（`isFlinched()/setFlinch()/clearFlinch()`），
      命中后无法行动（日志「XX 畏缩了，无法行动！」），**回合末与换人时清除**。
- [x] **6 件批次④ 装备**（`equipment.csv` 56 → **62 件**，`HeldItemEffect` 23 → **29 种**）：
  - 拳击手套 `PUNCH_BOOST 1.1`：拳类招式威力 ×1.1，且携带者使出拳类招式时**该招式不再视为接触**（免吃凸凸头盔反伤）。
  - 凸凸头盔 `CONTACT_PUNISH 0.1667`：被**接触类**招式命中时反伤攻击方 `max(1, 最大HP × 0.1667)`（约 1/6），**不消耗**。
  - 防尘护目镜 `POWDER_IMMUNE`（**空 param**）：免疫粉末类招式（**含变化类的催眠粉 / 毒粉**，直接落空）+ 免疫沙暴 / 冰雹回合末削血。
  - 锐利之爪 `CRIT_BOOST 1`：会心等级 0 → 1（1/24 → 1/8）。
  - 王者之证 `FLINCH_CHANCE 10`：造成伤害后 10% 概率使目标畏缩，**变化类招式不触发**。
  - 节拍器 `CONSECUTIVE_BOOST 0.2`：连续使用同一招式每层 ×`1 + 0.2 × (次数 − 1)`，**封顶 ×2.0**，**换招归零**。
- [x] **粉末免疫刻意放在 `executeMove` 入口（而非 `performAttack`）**
  - `executeMove` 对 `isStatus()` 招式会**先走 `tryInflict` 分支**再考虑进伤害路径。
    若把粉末判定写在 `performAttack` 里，**催眠粉 / 毒粉这两个变化类粉末招式会永远漏掉**。
    顺带**删掉**原先 `performAttack` 内那段永不生效的死代码检查。
- [x] 单测：
  - `BattleEngineHeldItemTest` 46 → **60 例**（新增 14 例批次④：拳类增伤 / 非拳类无效、凸凸头盔接触反伤 /
    非接触不反伤 / 被拳套豁免、锐利之爪会心伤害 ×1.5 / 会心概率 1/24 → 1/8、王者之证触发畏缩 /
    变化类不触发、节拍器逐层增伤封顶两倍 / 未携带不增伤、防尘护目镜免疫粉末且不消耗 / 只免疫粉末 / 免疫沙暴回合末伤害）。
  - `HeldItemTest` 13 → **14 例**：6 件装备的 effectType 与 param 断言（含 `POWDER_IMMUNE` 的**空参数**）；
    注册总数断言 56 → **62**（`equipment.csv 应注册 32 件装备 + 30 种树果`）。
  - **新增 `MoveFlagTest` 10 例**：`parseFlags` 的多标记 / 单标记 / 空白与大小写不敏感 / `null` 与空串与
    `"||"` 返回空集 / 无法识别片段忽略（含「前缀相同的错误名称」不误匹配）/ 去重 / 返回集合不可变；
    `Move` 的旧构造器等价无标记、12 参构造器写入标记、`null`/空集归一、**防御性拷贝**。
  - `mvn -o -B test` 基线 **382** → **407** 例，**仅 2 例失败且均为改动前既有**
    （`com.bao01.save.SaveSystemTest` 偶发、`org.example.model.NodeGeneratorTest` 1 例），**新增失败 0 例**。
- [x] 文档同步（5 份）：
  - `docs/接口文档_战斗服务.md` → **v1.17**：新增 §15.1.1「招式标记 `MoveFlag`」（枚举 + `parseFlags` 容错语义 +
    `Move` 新旧构造器对照表）、§15.6「招式标记数据列」（新旧表头对照 + 12 条标注清单 + 标注原则）；
    §15.2 效果一览 23 → 29 种（新增「招式标记与判定类」6 件表格）；§15.3 结算点补 6 行
    （含 `POWDER_IMMUNE` 在 `executeMove` **入口**、会心/畏缩的判定位置）+ 畏缩清除时机 + 七处语义决策引用块；
    §15.5 测试覆盖 46 → 60 / 13 → 14 并新增 `MoveFlagTest`（10）；回归基线 382 → 407。
  - `docs/对接文档_战斗模块数据对接.md` → **v1.4**：§3.2 伤害公式补「携带装备倍率」与「会心 ×1.5」、
    新增畏缩条目；§5.1 `Move` 契约补 `flags` 可选参数与字段表；§5.3 `moves.csv` 列定义补第 12 列
    （含「拳类必须同时标 `CONTACT`」与当前 12 条标注清单）、`equipment.csv` 56 → 62 并新增
    批次④ 6 种 `effectType` 对照表；§7 **R-07 修订**（原文称「引擎不做暴击」，v1.17 起会心已实现）。
  - `docs/QA问题表.md`：新增第 **30~36** 条（批次④ 七处实现方自主决策，均标「待确认」）。
  - `docs/测试用例草稿.md`：新增 **COMBAT-53~66**（14 条，对应新增的 14 个引擎用例）+ 顶部批次④ 说明块
    （含「概率类断言必须打散随机种子」的注意事项）；v1.15 / v1.16 的旧计数改标注为「当时」。
  - `docs/日报/2026-09-12.md`（本文件）。

- [x] **批次⑤：能力等级系统 + 命中判定 + 场地种子 + 属性反应**
  （按 09-11 排期 ①→②→④→③ 推进，原定「批次③」因与④ 的顺序互换，此处记作⑤）
  - **能力等级系统**（`Pokemon`）：新增 `statStages[]` 与 `MAX_STAT_STAGE = 6`、
    `stageMultiplier(level)`（±1~±6 按原版分数表 1.5/0.67 · 2/0.5 · 2.5/0.4 · 3/0.33 · 3.5/0.29 · 4/0.25）、
    `getStatStage(stat)`、`changeStatStage(stat, delta)`（返回**实际**变化量并截断到 ±6）、
    `clearStatStages()`；**五个 `effective*` 面板全部乘上等级倍率**。
    `org.example.model.StatModifier` 新增 `getDisplayName()`（中文名，供日志）与
    `static parse(String)`（失败返回 `null`、不抛异常）。
  - **招式能力等级变化**：新建 `org.example.model.MoveStatChange`
    （`record(StatModifier stat, int delta, boolean self)` + 容错 `parse(String)`：`;` 分段、
    `能力项|等级|SELF`，`null`/空白 → 空列表、脏片段静默跳过、拒绝 `HP` 与 `delta == 0`、返回不可变列表）。
    `Move` 新增 `statChanges` 字段与 **13 参全量构造器**，**11 参与 12 参构造器均保留**并委托；
    新增 `getStatChanges()/hasStatChanges()`。**叫声 / 摇尾巴 / 硬邦邦 / 高速移动因此真正生效**
    （此前只播报、无任何影响）。
  - **命中判定**（同版新增的通用机制）：`Move` 构造器归一化 `accuracy`（`< 0 → -1`、`> 100 → 100`）；
    `rollHit` 在 `accuracy < 0 || accuracy >= 100` 时**直接返回命中且不消耗随机数**，
    `accuracy == 0` 仍掷骰（因此永不命中），`1~99` 按百分比掷骰。未命中时播报并触发打空保险。
  - **`moves.csv` 11 → 13 列**：第 12 列 `flags` 新增 `SOUND` 取值、第 13 列 `statChanges`
    （`;` 分段、`|` 分列）。**数据行数不变仍为 36**；`GameData#applyMoveRow` 用
    `c.length > 11` / `c.length > 12` 判断，因此 v1.16 的 11 列表与 v1.17 的 12 列表**都仍能加载**。
    新增标注：`SOUND` **1 条**（叫声）、`statChanges` **4 条**（叫声 `ATTACK|-1` / 摇尾巴 `DEFENSE|-1` /
    硬邦邦 `DEFENSE|1|SELF` / 高速移动 `SPEED|2|SELF`）。
  - **15 件批次⑤ 装备**（`equipment.csv` 62 → **77 件**，`HeldItemEffect` 29 → **37 种**）：
    - **能力等级联动 7 种 / 14 件**：讲究头带 `CHOICE ATTACK|1.5`（**`CHOICE` 的修正项新增 `ATTACK`**）/
      突击背心 `ASSAULT_VEST 1.5` / 清净坠饰 `CLEAR_AMULET`（空 param）/ 弱点保险 `WEAKNESS_POLICY 2` /
      打空保险 `BLUNDER_POLICY 2` / 爽喉喷雾 `THROAT_SPRAY 1` /
      种子四件 `TERRAIN_SEED 场地|能力项|等级`（电气 `ELECTRIC|DEFENSE|1`、超能 `PSYCHIC|SP_DEFENSE|1`、
      薄雾 `MISTY|SP_DEFENSE|1`、青草 `GRASSY|DEFENSE|1`）。
    - **属性反应 4 件** `TYPE_REACTION 属性|能力项|等级`：球根 `WATER|SP_ATTACK|1`、
      充电电池 `ELECTRIC|ATTACK|1`、光苔 `WATER|SP_DEFENSE|1`、雪球 `ICE|ATTACK|1`。
    - **天气免疫 1 件**：万能伞 `UTILITY_UMBRELLA`（空 param）。
    `HeldItem` 新增 5 个参数访问器（`seedTerrainParam()` / `reactionTypeParam()` / `statParam()` /
    `statLevels()` / `statLevelsParam()`）。
  - **`BattleEngine` 接线**：`useMove` 入口新增 `assaultVestAllows` gate（与 `choiceAllows` 相邻，
    **均不消耗 PP、不推进回合**）、`pickFoeMove` 过滤变化招、`switchActive` 新增
    `clearStatStages()` + `applyTerrainSeed(target, terrain)`、`setTerrain` → `applyTerrainSeeds()`（双方）、
    `applyStatChange(target, stat, delta, source)` 作为**能力等级唯一漏斗**（含 `CLEAR_AMULET` 守卫）、
    `applyWeaknessPolicy` / `applyBlunderPolicy` / `applyThroatSpray` / `applyTypeReaction`、
    `computeDamage` 中 `ASSAULT_VEST` 特防加成与 `UTILITY_UMBRELLA` 天气倍率豁免、`weatherChip` 削血免疫。
  - **实现方自主决策 9 处**（`QA问题表.md` 第 37~45 条）：能力等级封顶 ±6 且日志按**实际**变化量播报、
    清净坠饰只拦**对手造成的**下降、突击背心禁招放在 `useMove` 入口（不消耗 PP）且敌方 AI 过滤变化招、
    突击背心特防加成**只在特殊招式路径**（物理与 `selfHit` 都不参与）、万能伞**只抵天气不抵场地**、
    万能伞与防尘护目镜**共用**天气削血免疫的播报与分支、场地种子在**场地开启与换人上场两处**触发、
    弱点保险只在 `effectiveness > 1.0` 时触发、**必中招式不消耗随机数**（零回归的关键）。
  - **9 件装备已由需求方裁决「从需求中移除」**（`QA问题表.md` 第 46 条，**不实现、不建 CSV 行**）：
    紧缠勾爪 / 机变骰子 / 逃脱按键 / 避难背包 / 红牌 / 美丽空壳 / 厚底靴 / 红线 / 特性护具 ——
    分别依赖「无法逃走」状态、多段攻击段数、战斗中强制换人链路、入场危害地形、能力下降反弹、
    特性替换等本作尚未建立的大系统；已确认**不立项**，相关底层系统不再列入排期。
  - [x] 单测：
    - `BattleEngineHeldItemTest` 60 → **89 例**（新增 29 例批次⑤）。
    - `HeldItemTest` 14 → **16 例**：注册总数 62 → **77**（`47 件装备 + 30 种树果`）、
      15 件新装备的 effectType / param 断言、`Stat.parse` 三个分支（v1.20 合并后由 `StatModifier` 改为 `Stat`，见 §六）。
    - `MoveFlagTest` 10 → **11 例**：新增 `SOUND` 标记用例。
    - **新增 `StatChangeTest` 8 例**（v1.20 由 `MoveStatChangeTest` 改名）、**新增 `PokemonStatStageTest` 7 例**。
    - `mvn -o -B test` 基线 407 → **454 例**，**仅 1 例失败且为改动前既有**
      （`org.example.model.NodeGeneratorTest`，2 例跳过），**新增失败 0 例**。
      合并远程 `dev` 后（见 §六）全量基线为 **547 例 / 0 失败 / 3 跳过 / BUILD SUCCESS**。
      （既有偶发失败 `com.bao01.save.SaveSystemTest` 本版全量跑**未复现**。）
  - [x] 文档同步（5 份）：
    - `docs/接口文档_战斗服务.md` → **v1.18**：§15 标题与计数（37 种 / 77 件）、param 访问器表新增 3 行、
      §15.1.1 加 `SOUND`、**新增 §15.1.2 `MoveStatChange`**、§15.2 计数与新增 3 个表
      （能力等级联动 / 天气免疫 / `CHOICE` 加讲究头带）、§15.3 新增 9 行结算点 + 九处语义决策块 +
      9 件未实现装备说明、**新增 §15.3.1 `executeMove` 结算顺序与命中率语义表**、§15.5 测试覆盖全部重写、
      §15.6 重写为 `flags` / `statChanges` 双列。
    - `docs/对接文档_战斗模块数据对接.md` → **v1.5**：§3.2 新增能力等级与命中判定条目、`HeldItem` 计数
      62 → 77；§5.1 `Move` 契约升为 **13 参**并补 `statChanges` 字段与 `accuracy` 新语义、
      新增 `SOUND`；§5.2 硬约束新增 **C10~C13**；§5.3 `moves.csv` 补第 13 列与命中率约定、
      `equipment.csv` 62 → 77 并新增批次⑤ 8 种 `effectType` 表 + 9 件未实现清单；
      §7 **R-07 修订**（命中判定已实现，`priority` 仍未消费）、新增 **R-08**（9 件装备）与 **R-09**（装备双源）；
      §8 降级矩阵新增 3 行；§10 引用版本号更新。
    - `docs/QA问题表.md`：新增第 **37~46** 条（批次⑤ 九处实现方自主决策 + 9 件无法实现装备清单）。
    - `docs/测试用例草稿.md`：新增 **COMBAT-67~88**（22 条）+ 顶部批次⑤ 说明块。
    - `docs/日报/2026-09-12.md`（本文件）。
  - [x] 提交并**推送 `dev`**（本批次为 09-11 排期的最后一批，用户已授权「完成再上传」）。

### 二、今日遇到的问题与处置

| 问题 | 处置方式 | 状态 |
| --- | --- | --- |
| 变化类粉末招式（催眠粉 / 毒粉）若把免疫判定写在 `performAttack` 里会**永远不生效** | 把 `POWDER_IMMUNE` 判定移到 `executeMove` **入口**（`isStatus()` 分支之前），并删掉 `performAttack` 里那段永不触发的死代码 | 已解决 |
| 概率类用例（会心 1/24、畏缩 10%）直接 `new Random(i)` 循环时 `nextInt(8)` 恒为常数，一度怀疑实现有 bug | 根因是 `java.util.Random` 小种子的首个 `next(31)` 高位**近似线性**。改用「打散种子」（`i * 0x9E3779B97F4A7C15L + 0x9E3779B97F4A7C15L`）或「遍历种子取第一个会触发者」的确定性写法；已把该坑写入测试用例草稿与接口文档 §15.5 | 已解决 |
| 批次④ 用例比对「有装备 / 无装备」时伤害比不稳定 | 沿用 09-11 的结论：`Pokemon.create(species, level, movePool)` 每次都会 `Stats.randomIv()`，必须用固定 `FIXED_IVS = Stats(31,31,31,31,31,31)` 创建精灵，否则连 HP / 攻 / 防 / 速都不可比 | 已解决 |
| `equipment.csv` 的 `POWDER_IMMUNE` 参数留空，担心被加载器整行跳过 | 加载器只要求「**5 列**」而 `param` 恒为第 4 列，写出空列即可（`...,POWDER_IMMUNE,,说明`）。已在对接文档 §5.3 显式写明该陷阱，并在 `HeldItemTest` 断言其 param 为空 | 已解决 |
| 凸凸头盔反伤可能「救不回来地」打死攻击方，一度不确定是否要给保命装备兜底 | 反伤不打在 `survivedByHeldItem` 路径上（该钩子只在「被招式命中」时调用），因此**反伤不受气势披带 / 气势头带保护、可以击倒攻击方**，与正作一致。已记入 `QA问题表.md` 第 31 条 | 待需求方确认 |
| 节拍器的连招增伤是否需要做成「能力等级」 | 批次③ 能力等级系统**尚未落地**，本版按讲究系先例用**装备内联乘算**（记录为引擎内存态、键为精灵 `uuid`、不落存档）。已记入 `QA问题表.md` 第 36 条，批次③ 完成后需复核是否迁移 | 待需求方确认（依赖批次③） |
| 畏缩在「速度更慢者施加」时感觉毫无作用 | 属正作语义：畏缩在回合末与换人时清除，**只对「本回合尚未行动」者生效**。已在文档写明并记入 `QA问题表.md` 第 32 条 | 待需求方确认 |
| 防尘护目镜「免疫沙暴 / 冰雹」是需求方对照表里没写、但正作有、且 `HeldItemEffect` javadoc 早已声明的行为 | 按 javadoc 补齐实现（两条都免疫），并在 `executeMove` 播报「XX 借助防尘护目镜，粉末类招式没有命中！」、在 `weatherChip` 播报「XX 借助防尘护目镜，不受沙暴影响！」。已记入 `QA问题表.md` 第 33 条 | 待需求方确认 |
| 全量测试出现 2 例失败，需确认是否为本次引入 | 与 09-11 基线（382 例 / 2 例失败）**同一集合**：`com.bao01.save.SaveSystemTest`（旧演示体系不存个体值的偶发，见 QA 第 25 条）与 `org.example.model.NodeGeneratorTest` 1 例。本次新增 25 例全绿，**无新增失败** | 记录待他人修（既有） |
| 批次⑤ 加命中判定后，既有 407 例「同种子对拍」用例是否会因多掷一次骰子而全部失效 | 约定**必中招式不消耗随机数**：`rollHit` 在 `accuracy < 0 \|\| accuracy >= 100` 时直接返回命中、不调用 `random`。因既有 `moves.csv` 中绝大多数招式 `accuracy = 100`，随机序列与 v1.17 **完全一致**，既有对拍用例**无一需要改动**（已记入 `QA问题表.md` 第 45 条） | 已解决 |
| 场地种子若只在「场地开启」时判定，则「场地已开、种子后上场」永远不触发 | 补第二处触发点：`switchActive` 对**换上的精灵**判定（`applyTerrainSeed(target, terrain)`）。已在对接文档 §5.3 与接口文档 §15.3 写明两处触发点 | 已解决 |
| 万能伞的「只抵天气不抵场地」若要测出差异，需构造「天气 + 场地同时生效」，用例一度算错 | 用例改为**只在晴天（无场地）下比对同一夹具的两次调用**（除装备外完全一致、随机序列相同，故可直接比 `1.5` 倍）；**注意不能**把「有伞」的结果与另一套夹具（不同随机序列）的伤害比对，那样 325 vs 361 会得到假失败 | 已解决 |
| 写用例时误用 `MoveSlot#getRemainingPp()`，编译不过 | `MoveSlot` 的实际 API 是 `getCurrentPp()`（可用 PP），无 `getRemainingPp()`。已改正并记入本日报 | 已解决 |
| 「必中招式不受命中判定影响」用例初次失败 | 原因是用例里用了 **GHOST 属性招式**打 NORMAL 防守方 → 属性**免疫**（不是命中问题）。改用 NORMAL 招式后通过 | 已解决 |
| 「换上的精灵在场地已开启时立即触发种子」用例初次失败 | 原因是用例用了 GRASS 系种子但场地开的是 ELECTRIC → 不匹配。改用 `TERRAIN_SEED_ELECTRIC` 后通过 | 已解决 |
| 弱电/突击背心等新装备的「触发并消耗但不提升能力」风险 | `TERRAIN_SEED` / `TYPE_REACTION` 的 param 必须写满 3 段，只写 2 段时第 3 段（等级）读作 **0** → 装备会**静默失效**。已在对接文档 §5.3 与硬约束 **C12** 显式写明 | 已解决（文档已警示） |
| 批次⑤ 的「15 件装备」是否真的都实现了 | 逐条比对需求方 58 行清单：**可实现的 15 件全部落地**，另 **9 件**因依赖未建立的大系统而**无法实现**（已单列并在 `QA问题表.md` 第 46 条说明受阻原因），合计 24 件落在本轮范围内 | 已核对；**9 件后经需求方裁决从需求中移除**（同表第 46 条，2026-09-12） |

### 三、AI 使用记录

| 时间 | 提示词/用途 | AI 产出 | 核对结果 |
| --- | --- | --- | --- |
| 上午 | 「继续」（按 09-11 排期推进批次④） | 落地批次④：新增 `MoveFlag` 枚举与 `Move` 标记字段（旧构造器保留）、`moves.csv` 第 12 列、6 件装备与 29 种 `HeldItemEffect`、**会心系统**（1/24 · 1/8 · 1/2 · 必定，×1.5）与 **畏缩**挥发性状态 | 已核对：`mvn -o -B test "-Dtest=BattleEngineHeldItemTest,HeldItemTest"` 74 例全绿（60 + 14） |
| 上午 | （自查）粉末免疫接线位置 | 复查 `executeMove` 的分支顺序，发现粉末判定若留在 `performAttack` 则**变化类粉末招式永远漏掉**；改到 `executeMove` 入口并删除死代码 | 已核对：新增「防尘护目镜免疫粉末类招式」「只免疫粉末类招式」两例覆盖变化类与伤害类两条路径 |
| 上午 | （自查）概率类用例结果异常 | 用临时探针确认「朴素 `new Random(i)` 循环」下会心比例恒为 0、打散种子后为 17/400（≈1/24）、锐利之爪 46/400（≈1/8），定位为 `java.util.Random` 小种子高位的线性相关而非实现缺陷 | 已核对：临时探针用例用完即删；改写的概率断言稳定通过；该注意事项已写入 `测试用例草稿.md` 与接口文档 §15.5 |
| 下午 | （自查）测试是否污染开发机真实存档 | 检查 `C:\Users\lenovo\.pokerouge\`：3 份文件（`growth-progress.txt`、`saves\slot1\save.txt`、`saves\slot2\save.txt`）mtime 均为 2026-09-11 及更早，**零污染** | 已核对：跑完全量测试后再次比对 mtime，未变 |
| 下午 | 「继续」（推进批次④ 文档与提交） | 补齐 `MoveFlagTest`（10 例）后同步 5 份文档：接口文档 v1.17（§15.1.1 / §15.6 / §15.2 / §15.3 / §15.5）、对接文档 v1.4（§3.2 / §5.1 / §5.3 / R-07）、QA 第 30~36 条、用例草稿 COMBAT-53~66、本日报 | 已核对：文档中的每个数字都与 `moves.csv` / `equipment.csv` / surefire 实际输出逐一对齐（36 条招式 / 62 件装备 / 12 条标注 / 407 例）；全量 `mvn -o -B test` 407 例仅 2 例既有失败 |
| 下午 | 「现在道具完善成什么样了」 | 逐一清点 `HeldItemEffect` 枚举、`equipment.csv` 行数与 `HeldItemTest` 断言，报出当前口径（29 种 `effectType` / 62 件装备）；顺带**修正文档里的 CSV 行数笔误**（`items.csv` 13 → 16 行） | 已核对：清点数与 `HeldItemTest` 的注册总数断言完全一致 |
| 下午 | 用户提供**完整 58 行道具清单** + 「把这些完成再上传」 | ① **能力探测**：逐条比对引擎现有钩子，判定 9 件依赖未建立的大系统、**无法实现**；② 落地批次⑤：新建 `MoveStatChange` record、`Pokemon.statStages[]` 与 `changeStatStage`、`Move.statChanges` + `MoveFlag.SOUND`、8 个新 `HeldItemEffect`、5 个参数访问器；③ `GameData` 解析 `moves.csv` 第 13 列、`moves.csv` 扩为 13 列、`equipment.csv` +15 行（62 → 77）；④ `BattleEngine` 接入九处结算点（`executeMove` 入口 `rollHit` 与粉末 gate、`useMove` 突击背心禁招、`switchActive` 种子、`setTerrain` 种子、`computeDamage` 突击背心与万能伞、`weatherChip` 天气免疫、`applyStatChange` 唯一漏斗） | 已核对：定向 `BattleEngineHeldItemTest` **89/89**、四个模型测试类 **42/42**；全量 `mvn -o -B test` 454 例、**仅 1 例改动前既有失败**、新增失败 0 |
| 下午 | （自查）批次⑤ 是否会破坏既有 407 例对拍用例 | 设计并验证**必中招式不消耗随机数**（`accuracy < 0 \|\| accuracy >= 100` 直接返回命中）：因既有招式 `accuracy` 多为 100，随机序列与 v1.17 完全一致，既有用例**零改动** | 已核对：全量测试未改动任何既有用例即通过 |
| 下午 | （自查）批次⑤ 用例首轮 4 处失败 | 逐条定位：GHOST 招式打 NORMAL 防守方是**属性免疫**而非命中问题（改 NORMAL）、种子与场地属性不匹配（改 `TERRAIN_SEED_ELECTRIC`）、伤害比对跨了两套随机序列（改为同一夹具比 `1.5` 倍）、`MoveSlot` 无 `getRemainingPp()`（改 `getCurrentPp()`） | 已核对：4 处修正后 `BattleEngineHeldItemTest` 89/89 |
| 晚间 | （自查）批次⑤ 文档同步 | 同步 5 份文档：接口文档 **v1.18**（§15 计数 37 种 / 77 件、新增 §15.1.2 `MoveStatChange`、**新增 §15.3.1 `executeMove` 顺序与命中率语义表**、§15.5 / §15.6 重写）、对接文档 **v1.5**（§5.1 `Move` 升至 13 参 + `accuracy` 新语义、硬约束 **C10~C13**、**R-07 修订 + 新增 R-08 / R-09**）、QA 第 37~46 条、用例草稿 COMBAT-67~88、本日报 | 已核对：文档数字与 `moves.csv` 13 列 / `equipment.csv` 77 行 / surefire 454 例逐一对齐 |

### 四、明日计划

- [x] ~~**批次③：能力等级系统**~~ —— 已在**批次⑤**落地（`Pokemon.statStages[-6,+6]`、`applyStatChange` 唯一漏斗、
      `MoveStatChange` 与 `moves.csv` 第 13 列）；弱点保险 / 打空保险 / 爽喉喷雾 / 四场地种子 /
      亚开果 / 香罗果 / 清净坠饰 / 突击背心 **全部实现**，能力等级封顶 ±6
- [ ] **批次⑥ 候选①（既有内联乘算的复核）**：讲究系「+50%」与**节拍器**「+20%/层」目前仍用
      装备内联乘算而非能力等级提升，需决定是否迁移到 `applyStatChange` 以受 ±6 封顶约束
- [ ] **批次⑥ 候选②（命中判定的后续消费）**：`Move.accuracy` 已于批次⑤ 生效，但 **`priority` 仍未被消费**
      （见对接文档 R-07）——下一批次需接入先制出招顺序，并补先制类装备（如先制之爪）
- [ ] **批次⑥ 候选③（招式标记补全）**：当前只标注 13 条（`CONTACT` 9 / `PUNCH` 3 / `POWDER` 2 / `SOUND` 1），
      其余物理招式是否算接触（如双刃头锤、岩石爆击等）需按正作逐条核对；「蘑菇孢子」尚未进 `moves.csv`
- [ ] **装备双源问题（对接文档 R-09）**：`equipment.csv` 与 `GameData.registerBuiltinEquipment()`
      两处注册，需在批次⑥ 前决定是否统一
- [ ] **商店出售携带物**：装备目前只能从肉鸽 REWARD 事件随机获得，`ShopStock.CANDIDATES` 尚不含装备
- [ ] 常驻节点可重复进入 + 每步刷新的难度平衡：医院只要 AP 够就能反复全队满血，路人 / 野外可反复
      刷金币与捕获次数，难度上限完全由 AP 额度决定 —— 实测后考虑是否限制同段内次数或提高常驻节点消耗
- [ ] 节点刷新后的**节奏体感**：当前每次刷新必出 3 个常驻节点 + 最多 2 个随机节点，
      实测是否出现「想要的商店刷不出来」的挫败感；必要时考虑保底机制
- [ ] 技能机数据形态（`ItemCategory` 加类别，还是另设学习机制）
- [ ] 必然节点（道馆 / 四天王 / 冠军）通关后是否享受 1/5 自回血 —— 当前**不享受**，待定
- [ ] 图鉴 / 成长面板 JavaFX 界面
- [x] `dev` 与 `origin/dev` 的分叉处理：推送时发现远程已领先 **30 个提交**，本批次已**合并远程 `dev`**（保留对方的能力等级 / 命中率 / 先制度实现，本地 15 件装备改搭其契约），见 §六

### 五、风险与求助

- [ ] **请需求方确认**：批次④ 有七处实现方自主决策（`QA问题表.md` 第 30~36 条）——
      ① **会心与畏缩只在「招式命中并造成伤害到未倒下的目标」之后判定**（变化类、0 倍招式都不参与）；
      ② 凸凸头盔反伤**不消耗装备、不受保命装备保护、可以击倒攻击方**；
      ③ 畏缩**只在「目标本回合尚未行动」时实际生效**（回合末与换人时清除）；
      ④ 防尘护目镜**同时免疫沙暴 / 冰雹回合末削血**（需求方对照表只写了免疫粉末招式）；
      ⑤ 粉末免疫判定放在 `executeMove` **入口**（否则变化类粉末招式永远漏掉）；
      ⑥ 拳类招式**同时标注 `CONTACT`**（即不持拳套时仍会被凸凸头盔反伤）；
      ⑦ 节拍器用**装备内联乘算**而非能力等级（与讲究系同一处理方式，批次③ 后需复核）
- [ ] **会心是全局机制变更**：本版把会心做成了「所有攻击都会按 1/24 判定」，**不只是锐利之爪的附属效果**。
      这会同时影响既有的全部伤害预期与正作的伤害口径，请确认这是预期范围；
      若要关闭基础会心（只让锐利之爪生效），把默认等级改为「负值即不判定」即可，属一行改动
- [ ] **招式标记需要数据侧逐条核对**：`CONTACT` / `PUNCH` / `POWDER` / `SOUND` 目前只标注了 13 条招式，
      其余物理招式**默认无标记**（= 不会触发凸凸头盔、不吃拳套增伤）。这意味着在补全之前，
      凸凸头盔的强度**低于正作**（很多接触招式不反伤），请需求方确认是否要按正作全量补标
- [ ] 讲究系「+50%」与节拍器「+20%/层」目前都用**装备内联乘算**实现，而**不是**能力等级提升。
      批次⑤ 已落地能力等级系统（±6 封顶），效果可能一致，但会与「能力等级上限 +6」的规则脱钩，
      届时需决定是否迁移
- [ ] 装备暂**无跨精灵唯一性校验**（`Player#equip` 已有「同一只精灵换穿自动脱下」，但允许两只精灵
      携带同一件装备）。若打算做成「一件装备只能装一只」，需要在队伍层面加约束
- [ ] ~~**请需求方确认**：本轮批次 ①②④ 三个提交均未推送~~ —— 已按用户指示「把这些完成再上传」
      将批次 ①②④⑤ 一并推送 `dev`，此项关闭
- [x] ~~**【批次⑤ 高优先，需需求方裁决】9 件装备无法实现**~~ —— **2026-09-12 需求方裁决：从需求中移除**
      （「先不实现」「去除」）。紧缠勾爪 / 机变骰子 / 逃脱按键 / 避难背包 / 红牌 / 美丽空壳 / 厚底靴 /
      红线 / 特性护具**均不实现、不建 CSV 行**，依赖的底层系统**不立项**，此项关闭（`QA问题表.md` 第 46 条）
- [ ] **请需求方确认**：批次⑤ 另有九处实现方自主决策（`QA问题表.md` 第 37~45 条）——
      ① 能力等级**封顶 ±6** 且日志按**实际**变化量播报（到顶播报「已经无法再提高了！」）；
      ② **清净坠饰只拦「对手造成的」能力下降**，自身招式自降不受影响；
      ③ 突击背心**禁变化招放在 `useMove` 入口**（不消耗 PP、不推进回合），并为敌方 AI 过滤变化招；
      ④ 突击背心**特防加成只在特殊招式路径**（物理与混乱自伤都不参与）；
      ⑤ 万能伞**只抵天气、不抵场地**；
      ⑥ 万能伞与防尘护目镜**共用**天气削血免疫的判定与播报（语义重叠，如需区分请指出）；
      ⑦ 场地种子在**场地开启（双方）与换人上场**两处触发；
      ⑧ 弱点保险**只在 `effectiveness > 1.0`** 时触发（标靶把免疫抬到 1.0 倍不算克制）；
      ⑨ **必中招式不消耗随机数**（加命中系统零回归的关键，但也意味着「必中招式」不会消耗任何随机数）
- [ ] **`priority`（先制度）仍未被消费**：批次⑤ 已接入命中判定，但出招顺序仍纯按速度。
      电光一闪等先制招式的数据已就位却无效果，请确认是否列入下一批次
- [ ] **`CHOICE`（讲究系）的修正项新增 `ATTACK`**：此前只有 `SPECIAL` 与 `SPEED`（讲究眼镜 / 讲究围巾），
      本版为讲究头带新增 `ATTACK`。若数据侧有硬编码 `SPECIAL|SPEED` 的假设，需同步复核
- [ ] `RouteConfig` 里的刷新相关数值（`MAX_ROUTE_NODES = 5`、常驻必占 3 槽）偏紧，
      随机节点的出现概率被压缩，实测后可能需要放宽上限
- [ ] 旧验收体系 `com.bao01.save.SaveSystemTest` 的偶发失败仍未处理（`QA问题表.md` 第 25 条②），
      影响「全量测试是否全绿」的判断，建议尽早清理或标注为预期失败

### 六、合并远程 `dev` 与能力等级契约统一（当日追加）

- [x] **背景**：批次⑤ 准备推送时发现远程 `dev` 已被协作者推进 **30 个提交**（`origin/dev` = `a319659`，
  merge-base = `72d007c`），其中 `383f70f` / `780c03c` **独立实现了同一套「能力等级 + 命中率 + 先制度」**，
  与本批次机制重叠但契约不同。经确认后裁决：**合并远程、保留对方机制实现、把本地 15 件装备效果
  重新搭到对方的能力等级系统上**。
- [x] **冲突规模**：`git merge --no-commit origin/dev` 共 **13 个源码/数据文件 + 3 份文档**冲突
  （`BattleEngine` / `Pokemon` / `Move` / `GameData` / `moves.csv` / `SaveData` / `SaveCodec` /
  `PokemonMapper` / `GrowthService` / `BattleEngineTrainerTest` / `BattleEngineHeldItemTest` /
  `接口文档_战斗服务.md` / `测试用例草稿.md` / `日报/2026-09-11.md` 等），全部手工解决后 `git add`。
- [x] **契约取舍**：能力项枚举统一取 `org.example.model.Stat`（**不含 `HP`**，显示名「物攻 / 物防 /
  特攻 / 特防 / 速度」）；能力变化载体统一取 `org.example.model.StatChange`
  （`record StatChange(Recipient, Stat, int)`，原 `MoveStatChange` 及其测试**整体删除**）；
  `moves.csv` 列序定为 `…,inflictionChance,statChanges,effect,flags`（`effect` 从未被填写，排在
  `statChanges` 之后让变化技能书写更自然），段格式 `受方:能力项:幅度`（如 `OPPONENT:ATTACK:-1`）。
- [x] **顺带修出的两处真实缺口**：
  1. **清净坠饰在「招式能力变化」路径上失效**——远程 `applyStatChanges` 未做 `CLEAR_AMULET` 判定，
     且日志措辞与装备路径不一致。改为**逐条转交唯一漏斗** `applyStatChange(target, stat, delta, attacker)`
     （`SELF` 目标时 `source == target` 自动豁免），两条路径因此共用守卫与措辞。
  2. **`GrowthService` 未升级时的日志**——远程用例 `GrowthServiceTest:135` 用
     `log().contains(x)`（返回 `List<String>`，是**精确元素匹配**），要求未升级日志**恰好**是
     「名字 获得了 N 点经验！」。本地「附带（当前/升级所需）进度」与之冲突，**保留远程行为**，
     本地测试与 javadoc 同步改写。
- [x] **测试**：合并后全量 `mvn -o -B test` → **547 用例 / 0 失败 / 3 跳过 / BUILD SUCCESS**
  （3 例跳过均为既有 `org.example.view.BattleAnimationSmokeTest`）。定向
  `BattleEngineHeldItemTest` 89/89、`BattleEngineStatStageTest` 20/20、`GrowthServiceTest` 30/30。
- [x] **文档**：`docs/接口文档_战斗服务.md` → **v1.20**（§15.1.2 改 `StatChange`、§15.6 列序与行数 51、
  §17.2 / §17.3 重写、补合并说明）；`docs/对接文档_战斗模块数据对接.md` → **v1.6**（§5.1 契约、
  §5.2 C10 / C11、§5.3 列序与段格式）；`docs/QA问题表.md` 新增第 **47** 条（契约取舍）；
  `docs/测试用例草稿.md` 把 `dev` 侧与之重号的 COMBAT-41~45 统一改为 **COMBAT-89~93**。
- [x] **遗留**：批次⑤ 最初设计的 `MoveStatChange` 已被 `StatChange` 取代；若后续文档 / 代码仍引用旧类名，
  需一并更正。

### 七、商店上架全部可携带装备（当日追加）

- [x] **背景**：需求方提问「我要求更新的道具，没有加入到商店吗」。查证属实 ——
  `ShopStock` 的商品池 `CANDIDATES` 只有 **16 件消耗品、零装备**，77 件装备与树果的唯一来源是
  肉鸽「装备补给」节点（`MainController#resolveRogueEquipmentReward` 从 `allEquipment()` **等概率**
  抽 1 件，重复即落空）。也就是说批次④ / ⑤ 新增的 15 件装备**在商店里买不到**。
- [x] **需求方裁决**：**全部 77 件（47 件装备 + 30 种树果）都加进商店，按段位解锁 + 定价。**
- [x] **实现（3 个文件 + 1 个测试重写）**：
  - **`ShopStock`** 货架拆为**消耗品 / 装备两个分区**，各自按段解锁、各自抽签后合并：
    - 消耗品池沿用原 16 件（`CONSUMABLES`，第 1~5 段解锁）；
    - **装备池改为动态取自 `GameData#allEquipment()`**（不再把 77 件硬编码一遍）——
      解锁段位与基础价由 `effectType` 经 `equipmentUnlockSegment` / `equipmentBasePrice` 推出，
      因此**后续新增装备只要登记进数据表就自动上架**，不必回来改商店；
    - `Entry` record 扩为 `(itemId, itemName, description, price, kind)`，`kind` 区分
      `CONSUMABLE` / `EQUIPMENT`，并补 `isEquipment()`；旧的 3 参构造器保留（界面测试仍在用）；
    - 新增 `forSegment(segment, random, ownedIds)` 重载：**已拥有的装备从池中剔除**（装备全库唯一）；
    - 新增 `withoutEntry(itemId)`：装备售出后从货架下架；新增包级 `unlockedConsumableIds` /
      `unlockedEquipment` 供测试断言解锁口径。
  - **`RouteConfig`** 新增 `BASE_SHOP_EQUIPMENT_STOCK = 1`、`SHOP_EQUIPMENT_STOCK_PER_SEGMENT = 2`、
    `MAX_SHOP_EQUIPMENT_STOCK = 2` 与 `shopEquipmentStockSize(segment)`：装备**固定占位**
    （第 1 段 1 格 → 第 3 段起 2 格），剩余格位全给消耗品 —— 否则 77 件装备会把 16 件消耗品挤出商店。
  - **`ShopView`** 渲染「【装备】/【道具】」前缀与装备效果说明（`Label#setWrapText`），
    按钮文案区分「购买装备（N 金币）」/「购买（N 金币）」，并在货架含装备时补一行
    「装备购买后入库，可在主菜单点击精灵名，在详情页中穿戴」的提示。
  - **`MainController`**：`openShop()` 传入 `player.getEquipment()` 的 id 集合；`buyFromShop` 按
    `Entry#isEquipment()` 分流到新增的 `buyEquipment(entry)` —— 校验金币 → `player.addEquipment` →
    从货架下架 → 刷新，已拥有时提示「你已经拥有【X】了」并下架。
- [x] **定价与解锁口径（实现方自主决策，已登记 `QA问题表.md` 第 48 条）**：
  - **定价**按 `HeldItemEffect` 同类同价，60（后攻之尾 / 标靶）~900（讲究系），再走既有的每段 +15% 通胀，
    不新增 `equipment.csv` 数据列（避免为价格改动数据契约）；
  - **解锁段位**同样按 `effectType`：通用便宜的（属性强化 / 基础树果 / 常见护符）前置到第 1 段，
    强力但可控的（先制类 / 天气类 / 会心类 / 命中兜底类）放第 2 段，稀有反制类第 3 段，
    剩饭 / 突击背心第 4 段，讲究系 / 生命宝珠压到第 5 段。
- [x] **测试**：`ShopStockTest` 13 → **19 例**（新增：每段货架都留出装备格位、全部装备都能在末段上架、
  装备按段位解锁逐段放开、已拥有的装备不再上架、已解锁消耗品商品池与解锁段位一致、
  已售出的装备可单独下架；并改写「首段库存」「展示名与注册表一致」以兼容装备条目与分区格位）。
  全量 `mvn -o -B test` → **553 用例 / 0 失败 / 3 跳过 / BUILD SUCCESS**（基线 547 → 553，+6）。
- [x] **文档**：`docs/接口文档_UI模块.md` → **v0.1.19**（§3.6 `ShopView` 段改写 + 版本记录）；
  `docs/需求文档.md`（§3.4 获取途径补「商店购买」、§4.2 节点总表、§4.5 商店商品数量 / 商品池 / 物价、
  实现进度、§七 商店界面）；`docs/QA问题表.md` 新增第 **48** 条；
  `docs/测试用例草稿.md` 新增 **SHOP-07~14**（8 条）并更新 F 节自动化计数。
