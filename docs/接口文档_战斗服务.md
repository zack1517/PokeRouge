# 战斗服务（BattleService）接口文档

> 版本：v1.18 · 日期：2026-09-12 · 模块：`org.example.battle`（PokeRouge）
>
> v1.1：新增天气与场地系统（状态查询 `getWeather()/getTerrain()`、变化类技能开启、
> 持续回合推进与回合末效果），详见 §7。
>
> v1.2：与「宝可梦、技能、队伍接口.md」对齐 `Move` 数据契约（新增 `priority` 先制度字段、
> `accuracy` 增加 -1=必中 语义），详见 §8。
>
> v1.3：战斗模型按「宝可梦、技能、队伍接口.md」v1.0 温和适配完成（`Nature`/IV/异常状态
> 等字段已就位），并新增 `BattleService` 配套契约类型；差异与取舍详见 §9 及
> 《适配说明_宝可梦技能队伍接口v1.md》。
>
> v1.4：新增**训练师轮战**（`newTrainerBattle`）：敌方持有一整支队伍，**某一方精灵全部倒下
> 才结束战斗**，不可逃跑、不可捕捉。新增查询 `getTrainer()`/`foeActive()`，详见 §3.4、§4。
>
> v1.5：参照原版宝可梦**接入异常状态结算**：6 种主要异常（麻痹/中毒/剧毒/灼伤/睡眠/冰冻）
> + 混乱（挥发性异常）、属性免疫、技能附带异常（`Move.inflicts`/`inflictionChance`）、
> 回合末持续伤害、解除道具（`ItemCategory.CURE`），详见 §10。
>
> v1.6：战斗模块**彻底数据解耦**——新增只读数据端口 `BattleDataPort`（技能 / 种族 / 野生池 /
> 按种族生成个体），引擎不再内建任何数据、也不再依赖 `org.example.data`；数据由外部数据模块
> 实现端口并在组装层注入，详见 §11。
>
> v1.7：战斗模块**只做演算与结算**——把「野生遭遇生成」移出本模块（迁至组装/流程侧
> `org.example.integration.WildEncounter`），并消除模块内最后一处 `Math.random()`，
> 遭遇等级浮动与随机挑选种族改为可注入随机源。`BattleServices` 收敛为纯工厂，详见 §4、§12。
>
> v1.8：道具支持**目标选择**——新增 `useItem(Item item, int partyIndex)`（作用于队伍任意精灵，
> 含替补），`useItem(Item)` 降为作用域为出战精灵的 `default` 便捷重载；明确越界目标、
> 回复道具对倒下精灵无效等目标规则，详见 §3.2、§10.6。
>
> v1.9：**成长判定外移**——战斗模块不再负责「经验增加、升级、学招、进化」，结算出胜利后把
> 「参战且未倒下的己方精灵」与「被击败的对手」**申报**给外部成长端口 `BattleGrowthPort`，
> 由成长模块自行判定；引擎删除经验曲线、习得表与进化规则，`decideLearn` 改为纯转发，
> 详见 §13。
>
> v1.9a：新增 §14，指向面向外部数据模块的落地文档《对接文档_战斗模块数据对接.md》v1.0。
>
> v1.10：新增**可携带装备（HeldItem）**效果结算：伤害类（属性强化/达人带/进化辉石）、
> 回复类（剩饭回合末回血/贝壳之铃吸血）与先手类（先制之爪），详见 §15。装备由外部流程
> 设置到 `Pokemon.heldItem`，战斗模块只读，不经 `BattleDataPort`。
>
> v1.10（成长线）：成长端口新增**捕捉申报** `onCaptured(String speciesId)`（`default` 空实现，故
> `none()` 与既有实现无需改动）；引擎在野生遭遇**捕捉成功**时调用，供成长模块累计「图鉴捕捉
> 次数」并派生出个体值加成（种族值不变）。同时个体值自本版起**参与战斗模型面板演算**
> （标准公式 `(2×种族值 + 个体值) × 等级 / 100`），详见 §13.6。
> 成长进度并落入**本地永久存档**（`<用户主目录>/.pokerouge/growth-progress.txt`，自动保存、
> 关闭程序仍保留、加成封顶 31），详见 §13.7。
>
> v1.11：新增**战斗演出事件**——引擎在结算过程中把「具备动画价值的动作」压入事件队列，经
> `drainEvents()` 一次性取走；事件为纯演出数据，不参与任何结算，供界面层驱动进场 / 放出 /
> 收回 / 技能释放 / 受击 / 倒下 / 投球 / 道具 / 逃跑动画，详见 §16。
>
> v1.11（成长线）：**经验结算改为「每击倒一只即时申报」**——不再等整场战斗结束才把整队被击败对手一次性
> 求和结算。`settle(survivors, defeated)` 语义收敛为「刚被击倒的那一只」，引擎在**野生精灵倒下**
> 与**训练师轮战每只精灵倒下**时各调用一次（战败也不回收已发放的经验）；另新增**获胜申报**
> `onBattleWon(List<Pokemon> survivors)`（`default` 空实现），整场战斗结束且 `PLAYER_WIN` 时调用
> 一次，供成长模块按**场次**累计图鉴对战次数。经验数值本身仍由成长模块决定，见 §13.1、§13.3。
>
> v1.12：**经验发放补充可见反馈**——`GrowthService` 每次结算都会为每只获得经验的精灵追加一行
> 「`名字 获得了 N 点经验！`」战斗日志（未升级时附带「当前/升级所需」进度；升级时该行在前、
> 「`名字 升到了 Lv.N！`」逐行在后），满级不再产生该行。修复「击倒一只对手后经验确实入账、
> 但战斗日志里只有“倒下了！”导致玩家以为没有获得经验」的体感问题，见 §13.3。
>
> v1.13：**测试隔离修复**（无 API 变更）。`new GrowthService(dataPort)` 与
> `PokemonBattleAdapter#battleGrowthPort(dataPort)` 绑定的是 `GrowthProgress#instance()` ——
> **用户主目录下的真实存档**，战斗获胜即 `recordBattle` 直接落盘。此前 `BattleDataPortTest` 的两例
> 走了这两个单参入口，导致**每次跑测试都往开发机真实存档累加对战次数**（假物种 id `mine_sp` /
> `mine_evo` 出现在 `<用户主目录>/.pokerouge/growth-progress.txt`）。现所有成长 / 战斗用例一律注入
> 纯内存的 `new GrowthProgress()`，并在两个单参入口补上 `@apiNote` 警示；见 §13.5。
>
> v1.14：**固定倍率球种扩充**（无引擎代码变更，纯数据 + 商店上架）。`items.csv` 与
> `GameData#registerBuiltinItems` 新增纪念球 ×3、贵重球 ×3、狩猎球 ×4.5、竞赛球 ×4.5，
> 商店（`ShopStock`）随之按段解锁上架；商店展示名改由道具注册表统一提供，
> 修正「醒睡药」在商店显示为「醒神药」的历史不一致。条件倍率球（等级球 / 月亮球 / 沉重球等）
> 需先扩 `Item` 的条件倍率模型与 `Species` 体重字段，本版不做。
>
> v1.15：**携带物（HeldItem）效果大批扩充**（19 种效果，`equipment.csv` 8 → 26 件，§15 全节重写），
> 新增伤害强化（力量头带 / 博识眼镜 / 生命宝珠）、速度与出手顺序（黑色铁球 / 后攻之尾）、
> 判定修正（标靶 / 气球）、回合末效果（黑色污泥 / 火焰宝珠 / 剧毒宝珠）、天气延长（四岩石）、
> 保命（气势披带 / 气势头带）、讲究系（讲究眼镜 / 讲究围巾）。
> 同版修复两处确定性缺陷：①**携带物完全不落存档**（`org.example.save` 的 `PP` 行末新增装备 id 段，
> 见 §15.4）；②`SaveData.PokemonData` 的「省略装备」便捷构造器会被 `SaveCodec` 的
> 「补齐技能后重建队伍」路径误用而**静默丢弃装备**（该构造器已删除，改为必填参数，让编译器兜住）。
> 另：`HeldItem#doubleParam()` 此前对多段参数（如黑色铁球的 `0.5|GROUND`）整体解析失败后返回 1.0，
> 导致速度修正静默失效；现改为「整体解析失败则退化取首段」。
>
> v1.16：**树果机制**（4 种效果 / 30 种树果，`equipment.csv` 26 → 56 件，§15 相应扩写）。
> 新增 `CURE_STATUS`（异常治疗：樱子果 / 桃桃果 / 莓莓果 / 零余果 / 利木果 / 柿仔果 / 木子果）、
> `HEAL_HP`（HP 回复：橙橙果 / 文柚果 / 勿花果·异奇果·乐芭果·芭亚果）、`HEAL_PP`（苹野果）、
> `RESIST_TYPE`（属性减伤 16 种，含 `ALWAYS` 标记的灯浆果）。树果与道具**共用同一模型 / 注册表 /
> 穿戴通路 / 存档通路**，均为一次性消耗品。`HeldItem` 新增 7 个语义取值 API
> （`cureStatuses` / `healThresholdRatio` / `healAmount` / `ppRestoreAmount` /
> `resistTypeParam` / `resistMultiplier` / `resistUnconditional`）。三处实现方自主决策见 §15.3 与
> `QA问题表.md` 第 26~28 条。需求方明确排除「努力值降低」类 6 种树果（本作无努力值系统）。
>
> v1.17：**招式标记 + 会心 + 畏缩 + 连招增伤**（6 种效果 / 6 件装备，`equipment.csv` 56 → 62 件，
> 效果 23 → 29 种）。新增 `org.example.model.MoveFlag`（`CONTACT` 接触 / `PUNCH` 拳 / `POWDER` 粉末），
> `Move` 增加 `flags` 字段与 `isContact()/isPunch()/isPowder()`，`moves.csv` 新增第 12 列 `flags`
> （§15.6）。新增 6 件装备：拳击手套（拳类增伤且不再视为接触）/ 凸凸头盔（接触反伤最大 HP 1/6）/
> 防尘护目镜（免疫粉末招式与沙暴·冰雹回合末伤害）/ 锐利之爪（会心等级 +1）/ 王者之证（10% 畏缩）/
> 节拍器（连招逐次增伤、上限 2 倍）。同版新增**会心系统**（0 级 1/24、1 级 1/8、2 级 1/2、≥3 级必定，
> 伤害 ×1.5）与**畏缩**挥发性状态（`Pokemon#setFlinch/clearFlinch`，回合末与换人时清除），
> 详见 §15.2、§15.3、§15.6。七处实现方自主决策见 `QA问题表.md` 第 30~36 条。
> 测试 382 → **407** 例（新增 25 例：引擎 14 / 装备库 1 / 标记 10），新增失败 0 例。
>
> v1.18：**能力等级系统 + 命中判定 + 场地种子 + 属性反应**（8 种效果 / 15 件装备，`equipment.csv`
> 62 → 77 件，效果 29 → 37 种）。同版新增三项通用机制：
> 1. **能力等级（`statStages`）**——`Pokemon` 新增 `getStatStage/changeStatStage/clearStatStages`
>    与 `Pokemon.stageMultiplier(level)`（±1~±6 按原版分数表），五个 `effective*` 面板全部乘上
>    等级倍率；`StatModifier` 新增中文名与 `parse(String)`。
> 2. **招式能力等级变化**——新增 `org.example.model.MoveStatChange` 与 `Move` 的 `statChanges`
>    字段（12 → **13 参构造器**），`moves.csv` 新增第 13 列 `statChanges`（§15.7），叫声 / 摇尾巴 /
>    硬邦邦 / 高速移动等招式因此真正生效。
> 3. **命中判定**——`accuracy < 0` 视为必中（**不消耗随机数**）、`accuracy ≥ 100` 必中、
>    `accuracy == 0` 永不命中；未命中时播报并触发打空保险。
> 另新增 `MoveFlag.SOUND`（声音类，配合爽喉喷雾）与 15 件装备：讲究头带 / 突击背心 / 清净坠饰 /
> 弱点保险 / 打空保险 / 爽喉喷雾（能力等级联动 6 件），电气·超能·薄雾·青草种子（场地种子 4 件），
> 球根 / 充电电池 / 光苔 / 雪球（属性反应 4 件），万能伞（天气增伤与沙暴·冰雹削血免疫 1 件）。
> 九处实现方自主决策与 9 件**未能实现**的装备见 `QA问题表.md` 第 37~46 条。
> 测试 407 → **454** 例（新增 47 例），新增失败 0 例。

## 1. 概述

本模块把「回合制战斗逻辑」从界面/控制器中独立出来，对外暴露一个稳定接口
`BattleService`，供主界面、战斗界面等调用方使用。调用方只依赖接口，具体结算规则由
实现类 `BattleEngine` 承担，实例统一由静态工厂 `BattleServices` 创建。

设计目标：

- 战斗规则（伤害、速度先后手、捕捉、逃跑、天气与场地）与 UI 完全解耦；
- **战斗模块只消费数据**：技能、种族、野生池等一切数据经只读端口 `BattleDataPort` 由外部
  数据模块注入，模块内不内建任何数据、也不依赖具体数据源（见 §11）；
- **战斗模块只做演算与结算**：「遇到谁、几级」等遭遇生成属流程/组装层职责，不在本模块内
  （见 §12）；模块内不出现 `Math.random()`，随机源一律可注入；**经验增加、升级、学招、
  进化**同样不属于本模块，击倒与获胜时申报给外部成长端口 `BattleGrowthPort`，由成长模块
  自行判定（见 §13）；
- 调用方不直接依赖具体实现类，便于测试注入随机源、数据端口与成长端口、后续替换实现；
- 以「每回合新增日志行」作为行动结果的统一反馈通道。

## 2. 模块结构

```
org.example.battle
├── BattleService.java    [接口] 战斗服务契约（本文档主体）
├── BattleServices.java   [工厂] 创建战斗实例（静态）
├── BattleDataPort.java   [端口] 只读数据端口：技能/种族/野生池/按种族生成个体（见 §11）
├── BattleDataPorts.java  [端口] 端口工厂与空实现（BattleDataPorts.none()）
├── BattleGrowthPort.java [端口] 成长申报端口：结算后交由外部成长模块判定（见 §13）
├── BattleEvent.java      [模型] 战斗演出事件（动画数据源，见 §16）
└── BattleEngine.java     [实现] 回合制对战引擎（默认/唯一实现）
```

> **数据零依赖**：`org.example.battle` 包内不出现任何数据文件、内建数据表或对数据模块
> （如 `org.example.data`）的依赖；引擎所需的技能与种族一律经 `BattleDataPort` 查询。
>
> **无遭遇生成、无成长规则**：本包不包含「野生遭遇生成」逻辑（v1.7 起迁至
> `org.example.integration.WildEncounter`），也不在引擎内实现经验曲线、习得表或进化规则
> （v1.9 起交由外部成长模块，见 §13）；包内不出现 `Math.random()`，所有随机源均为可注入的
> `java.util.Random`。

依赖的模型类型（均位于 `org.example.model`）：`Player`、`Pokemon`、`MoveSlot`、
`Item`、`Bag`、`Trainer`（训练师轮战的敌方队伍，见 §3.4），以及天气/场地系统相关枚举
`Weather`、`Terrain`、`MoveEffect`（见 §7）。

### 类关系

```
                     <<interface>>
                     BattleService
                     ┌──────────────────────────────┐
                     │ + Status (enum)              │
                     │ + useMove(slot) → List<String>│
                     │ + useItem(item, partyIndex)   │
                     │   → List<String>              │
                     │ + useItem(item) (default)     │
                     │ + tryRun() → List<String>     │
                     │ + switchActive(idx) → List<String>│
                     │ + getPlayer/getWild/...       │
                     │ + getWeather() / getTerrain() │
                     │ + drainEvents() → List<Event> │
                     └──────────────┬───────────────┘
                                    ▲ implements
                                    │
   BattleServices (factory)         │
   ┌───────────────────────┐   ┌────┴──────────────────┐
   │ newBattle(...)        ├──►│  BattleEngine          │
   │ newTrainerBattle(...) │   └────┬─────────────┬────┘
   └───────────┬───────────┘        │ 查询（只消费）│ 结算申报（胜利后）
               │                    ▼             ▼
               │        <<interface>> BattleDataPort     <<interface>> BattleGrowthPort
               │        findMove / findSpecies            settle(survivors, defeated)
               │        wildSpeciesPool                   resolveLearn(choice, slot)
               │        createPokemon                             ▲ 实现（组装层注入）
               │                    ▲ 实现（组装层注入）           │
               │                    │                   外部成长模块（如 org.example.growth）
               │      外部数据模块（如 org.example.data 适配器）    └─ 共用同一份数据端口
               └──────────────────────────┘

   ── 组装/流程侧（不属于本模块） ──────────────────────────────
   org.example.integration.WildEncounter
     levelAround(level[, random])                → 遭遇等级浮动
     randomWild(level, dataPort[, random])       → 随机挑选野生精灵
   org.example.growth.GrowthService
     实现 BattleGrowthPort：经验数值 / 升级 / 到级学招 / 进化（见 §13）
   （数据仍经 BattleDataPort 取，随机源可注入）
```

## 3. 接口：`BattleService`

全限定名：`org.example.battle.BattleService`

### 3.1 嵌套枚举 `Status`（战斗状态）

| 常量         | 含义                     | 触发场景                         |
|--------------|--------------------------|----------------------------------|
| `ONGOING`    | 进行中                   | 战斗尚未结束，可继续行动         |
| `PLAYER_WIN` | 玩家获胜                 | 野生精灵倒下（野生遭遇），或训练师整队精灵全部倒下（训练师轮战）；随后向成长模块申报结算（§13） |
| `PLAYER_LOSE`| 玩家战败                 | 队伍全部倒下且无精灵可替换       |
| `FLED`       | 逃跑成功                 | `tryRun()` 成功（仅野生遭遇）   |
| `CAUGHT`     | 捕捉成功                 | 精灵球捕捉成功并加入玩家队伍（仅野生遭遇） |

### 3.2 回合行动方法

所有行动方法均**要求战斗进行中**（`status == ONGOING`），否则抛出
`IllegalStateException`。返回值为**本回合新产生**的日志行（非全量日志）。

#### `List<String> useMove(MoveSlot slot)`

玩家选择技能攻击。敌方（野生精灵，或训练师当前出战精灵）自动选择可用技能；双方都行动时
按速度决定先后手，速度相同则随机。

> 先制说明：`Move` 数据契约已含先制度 `priority`（越大越先出手，0 为普通技能），
> 当前引擎尚未消费该字段，先后手仍按速度判定；先制排序留待后续扩展。

| 项     | 说明 |
|--------|------|
| 参数   | `slot`：当前出战精灵的技能槽；`null` / PP 耗尽 / 非当前精灵的槽位会自动退回当前精灵第一个可用技能；完全无可用技能则本回合不行动 |
| 返回   | 本回合新增日志行 |
| 副作用 | 消耗 PP；**变化类技能**若携带天气/场地效果（`MoveEffect`）则改为开启/重置对应天气/场地（见 §7），不造成伤害；普通技能命中则计算伤害；若击倒野生精灵进入 `PLAYER_WIN`（训练师轮战则换下一只，整队全倒才进入 `PLAYER_WIN`），玩家精灵倒下自动换宠或进入 `PLAYER_LOSE` |

#### `List<String> useItem(Item item, int partyIndex)`

使用道具于**指定队伍精灵**。按道具类别分流：`HEAL` 回复目标精灵 HP；`CURE` 解除目标精灵
异常状态；`POKE_BALL` 尝试捕捉野生精灵（**与目标无关**，始终投向敌方；捕捉公式：血量越低、
捕获率越高、球倍率越大越易成功，目标陷入睡眠/麻痹时再 ×2，必定捕捉球直接成功）。

| 项     | 说明 |
|--------|------|
| 参数   | `item`：要使用的道具；`null` 或背包中数量不足则不行动。`partyIndex`：目标精灵在玩家队伍中的下标 |
| 返回   | 本回合新增日志行 |
| 目标   | `partyIndex` 越界 → 追加「没有可以使用的目标。」并**不消耗道具、不消耗回合**；**回复道具对已倒下精灵无效**（不能以此复活，原版行为），追加提示且不消耗；解除道具对已倒下精灵**仍然有效**（倒下时异常状态保留） |
| 副作用 | 回复/解除道具仅在使用成功（确有效果）时消耗背包数量；精灵球使用后无论成败都会消耗；使用后若战斗未结束，敌方行动一次。**训练师轮战不可捕捉**：对训练师投球会被拒绝，只追加一条提示日志，**精灵球不消耗、也不消耗回合** |

> `useItem(Item item)` 是作用域为**当前出战精灵**的便捷重载（`default` 方法，等价于
> `useItem(item, 出战精灵下标)`）；既有调用点无需改动。

#### `List<String> useItem(Item item)`

`useItem(item, partyIndex)` 的便捷重载：作用于**当前出战精灵**。参数与副作用同上；若当前出战
精灵满血（`HEAL`）或无可解除异常（`CURE`），则追加提示并**不消耗道具、不消耗回合**。

#### `List<String> tryRun()`

尝试逃跑。成功率 = `0.35 + 0.6 × (我方速度 / (我方速度 + 敌方速度))`，
速度越接近成功率越低（约 0.65），远超时接近 0.95。

| 项     | 说明 |
|--------|------|
| 返回   | 本回合新增日志行 |
| 副作用 | 成功 → `FLED`；失败 → 敌方行动一次。**训练师轮战不可逃跑**：只追加「无法逃跑」提示日志，不消耗回合 |

#### `List<String> switchActive(int partyIndex)`

消耗本回合行动切换出战精灵，收换完成后敌方行动一次。

| 项     | 说明 |
|--------|------|
| 参数   | `partyIndex`：玩家队伍中目标精灵下标 |
| 返回   | 本回合新增日志行 |
| 副作用 | 目标与当前相同 / 下标非法 / 目标不存在时不行动（不消耗回合） |

### 3.3 状态查询方法

| 方法签名 | 返回 | 说明 |
|----------|------|------|
| `Player getPlayer()` | 玩家 | 本场战斗的玩家（含队伍与背包） |
| `Pokemon getWild()` | 野生精灵 | 本场战斗的野生精灵；**训练师轮战中为 `null`** |
| `Trainer getTrainer()` | 训练师 | 本场战斗的训练师；**野生遭遇中为 `null`** |
| `Pokemon foeActive()` | 敌方出战精灵 | 野生遭遇为野生精灵，训练师轮战为训练师当前出战精灵；界面应统一用它渲染敌方面板 |
| `Pokemon playerActive()` | 出战精灵 | 玩家当前出战精灵；倒下被引擎自动切换后可能为 `null` |
| `Status getStatus()` | 战斗状态 | 见 3.1 |
| `boolean isOngoing()` | 是否进行中 | 等价 `getStatus() == Status.ONGOING` |
| `List<String> getLog()` | 日志 | 完整战斗日志（只读，不可修改） |
| `List<BattleEvent> drainEvents()` | 演出事件 | **取走**自上一条行动方法调用以来累积的演出事件并清空队列（见 §16）；同一事件只投递一次 |
| `Bag getBag()` | 背包 | 便捷转发，等价 `getPlayer().getBag()` |
| `Weather getWeather()` | 天气 | 当前天气；无天气为 `Weather.NONE`，由天气类变化技能开启（见 §7） |
| `Terrain getTerrain()` | 场地 | 当前场地；无场地为 `Terrain.NONE`，由场地类变化技能开启（见 §7） |

### 3.4 训练师轮战（敌方持有一整支队伍）

敌方由 `org.example.model.Trainer` 描述（队伍上限 6 只，与 `Player` 同一套队伍
契约：首个加入者自动成为出战精灵、倒下后自动切换下一只健康精灵）。判定与野生遭遇的区别：

| 项 | 野生遭遇 | 训练师轮战 |
|----|----------|------------|
| 敌方来源 | `getWild()` | `getTrainer()`（`getWild()` 为 `null`） |
| 当前敌方精灵 | `foeActive()` = 野生精灵 | `foeActive()` = `trainer.getActive()` |
| 结束条件 | **野生精灵倒下**即 `PLAYER_WIN` | **敌方整队精灵全部倒下**才 `PLAYER_WIN` |
| 玩家战败 | 己方整队全部倒下且无精灵可替换 → `PLAYER_LOSE` | 同左 |
| 敌方倒下后 | 战斗结束 | 自动派出下一只健康精灵（训练师不会主动换宠，换宠当回合新精灵不行动） |
| 逃跑 | 可尝试 | 不可（`tryRun()` 只记日志，不消耗回合） |
| 捕捉 | 可投球捕捉 | 不可（投球不消耗道具、不消耗回合） |
| 天气/场地、技能 PP | 整场持续 | 整场持续（换宠不重置） |
| 胜利经验 | **每击倒一只即时申报一次**（野生精灵倒下 / 训练师每只倒下各一次） | 同左：**每只对手倒下即时申报**，不等整场结束（§13） |

> 判定顺序：同一回合敌方出战精灵倒下时，先由训练师派出下一只；派不出（整队全倒）则立即
> `PLAYER_WIN`，随后才处理玩家精灵倒下后的自动换宠/`PLAYER_LOSE`。

### 3.5 学习抉择（技能栏已满）

| 方法签名 | 返回 | 说明 |
|----------|------|------|
| `List<LearnChoice> pendingLearnChoices()` | 待抉择队列 | 成长模块申报结算时挂起的「到级学招但技能栏已满」项；空表示无待抉择。自 v1.11 起经验**逐只即时结算**，故该队列可能**在战斗进行中非空**（不再只在战斗结束时） |
| `List<String> decideLearn(int forgetSlotIndex)` | 本回合新增日志行 | 对队首抉择作出决定：`forgetSlotIndex >= 0` 且合法则遗忘该槽位并学会新技能；`-1` 或越界则放弃学习。队列为空时抛 `IllegalStateException` |

嵌套类型：`BattleService.LearnChoice`（`record(Pokemon pokemon, Move move)`）。

> 队列由引擎管理（出入队、空队列异常），**判定与文本**由成长模块给出（见 §13）：
> 引擎只把玩家的 `forgetSlotIndex` 转发给 `BattleGrowthPort#resolveLearn`，并追加返回的日志行。

## 4. 工厂：`BattleServices`

全限定名：`org.example.battle.BattleServices`（`final`，私有构造，禁止实例化）

| 方法签名 | 返回 | 说明 |
|----------|------|------|
| `BattleService newBattle(Player player, Pokemon wild)` | 战斗实例 | 创建默认随机源的新对战；玩家无存活出战精灵或野生无效时抛 `IllegalArgumentException`，参数为 `null` 抛 `NullPointerException`。数据端口与成长端口均为空实现（不查数据、不结算成长） |
| `BattleService newBattle(Player player, Pokemon wild, Random random)` | 战斗实例 | 同上有随机源版本（便于测试复现） |
| `BattleService newBattle(Player player, Pokemon wild, BattleDataPort dataPort)` | 战斗实例 | 同上并注入**数据端口**（见 §11）；成长端口为空实现 |
| `BattleService newBattle(Player player, Pokemon wild, Random random, BattleDataPort dataPort)` | 战斗实例 | 同上，带随机源与数据端口（便于测试复现） |
| `BattleService newTrainerBattle(Player player, Trainer trainer)` | 战斗实例 | 创建**训练师轮战**（默认随机源）：不可逃跑/捕捉，直到某一方精灵全部倒下才结束（见 §3.4）；玩家无存活出战精灵或训练师队伍无健康精灵时抛 `IllegalArgumentException`，参数为 `null` 抛 `NullPointerException` |
| `BattleService newTrainerBattle(Player player, Trainer trainer, Random random)` | 战斗实例 | 同上，带随机源版本（便于测试复现） |
| `BattleService newTrainerBattle(Player player, Trainer trainer, BattleDataPort dataPort)` | 战斗实例 | 同上并注入数据端口 |
| `BattleService newTrainerBattle(Player player, Trainer trainer, Random random, BattleDataPort dataPort)` | 战斗实例 | 同上，带随机源与数据端口 |
| `BattleService newBattle(Player player, Pokemon wild, BattleDataPort dataPort, BattleGrowthPort growthPort)` | 战斗实例 | 同上并注入**成长端口**（v1.9 起）：胜利后由成长模块判定经验/升级/学招/进化（见 §13） |
| `BattleService newBattle(Player player, Pokemon wild, Random random, BattleDataPort dataPort, BattleGrowthPort growthPort)` | 战斗实例 | 同上，带随机源、数据端口与成长端口 |
| `BattleService newTrainerBattle(Player player, Trainer trainer, BattleDataPort dataPort, BattleGrowthPort growthPort)` | 战斗实例 | 同上（训练师轮战）+ 数据端口与成长端口 |
| `BattleService newTrainerBattle(Player player, Trainer trainer, Random random, BattleDataPort dataPort, BattleGrowthPort growthPort)` | 战斗实例 | 同上，带随机源、数据端口与成长端口 |

> **成长端口也可为 `null`？** 不可。四个成长重载会做 `Objects.requireNonNull`；不接成长模块时
> 请使用不带 `growthPort` 的重载（内部等价于 `BattleGrowthPort.none()`，胜利后不做任何成长）。

> **职责边界**：本工厂**只负责创建对战**。野生遭遇生成（等级浮动 `levelAround`、随机挑选
> 种族 `randomWild`）自 v1.7 起移出本模块，见 `org.example.integration.WildEncounter`
> （§12）。

## 5. 使用示例

```java
// 0) 组装层准备端口（外部模块实现，战斗模块不关心其内部）
BattleDataPort dataPort = new GameDataBattleDataPort();                // 见 §11
BattleGrowthPort growthPort = PokemonBattleAdapter.battleGrowthPort(dataPort); // 见 §13
// 自定义成长模块亦可自行实现 BattleGrowthPort 并在组装层替换

// 1) 组装玩家（队伍 + 背包），进入随机遭遇
Player player = ...;                       // 需有存活出战精灵
int level = WildEncounter.levelAround(player.getActive().getLevel());        // 组装/流程侧
Optional<Pokemon> wild = WildEncounter.randomWild(level, dataPort);          // 组装/流程侧
if (wild.isEmpty()) return;

// 2) 创建战斗服务（返回的是接口类型），注入数据端口与成长端口
BattleService battle = BattleServices.newBattle(player, wild.get(), dataPort, growthPort);

// 3) 回合驱动：进行中则让玩家选择一个行动
while (battle.isOngoing()) {
    List<String> lines = battle.useMove(currentMoveSlot); // 技能 / useItem(item, partyIndex) / tryRun / switchActive
    for (String line : lines) logView.append(line);
    // 技能栏已满时成长模块会挂起学习抉择：取队首项并让玩家选择遗忘槽位（-1 = 放弃学习）
    if (!battle.pendingLearnChoices().isEmpty()) {
        int slot = askForgetSlot();                       // 玩家选择
        for (String line : battle.decideLearn(slot)) logView.append(line);
    }
}

// 4) 依据结果状态收尾
switch (battle.getStatus()) {
    case PLAYER_WIN -> onWin(battle.getPlayer());   // 成长已在每次击倒时由成长模块即时完成
    case PLAYER_LOSE -> onLose();
    case FLED       -> backToMap();
    case CAUGHT     -> onCaught(battle.getWild());
    case ONGOING    -> { /* 不应出现 */ }
}
```

训练师轮战（敌方整队，某一方全灭才结束）：

```java
// 训练师队伍（可从外部数据/流程系统按节点难度组装）
Trainer trainer = new Trainer("路人训练师");
trainer.addPokemon(level20Mon);
trainer.addPokemon(level22Mon);

BattleService battle = BattleServices.newTrainerBattle(player, trainer, dataPort, growthPort);

// 界面渲染统一取 foeActive()：换宠后自动跟随
render(battle.playerActive(), battle.foeActive());

while (battle.isOngoing()) {
    // 训练师战无逃跑/捕捉：tryRun()、对训练师投球只返回提示日志且不消耗回合
    battle.useMove(slot);
    render(battle.playerActive(), battle.foeActive());
}
// battle.getTrainer().isPartyAllFainted() == true 时为 PLAYER_WIN
```

## 6. 回合驱动流程（状态机）

```
        ┌─────────────────────────────────────────────────────┐
        │  ONGOING  (玩家每回合选择 技能/道具/逃跑/换宠)       │
        │  ┌───────────────────────────────────────────────┐  │
        │  │ 引擎内部结算：                                 │  │
        │  │   先后手 → 伤害 → 胜负 → 申报成长（端口）      │  │
        │  │   回合末：结算天气/场地效果并推进剩余回合       │  │
        │  └───────────────────────────────────────────────┘  │
        └──────────┬──────────────┬───────────────┬──────────┘
                   ▼              ▼               ▼
              PLAYER_WIN     PLAYER_LOSE     FLED / CAUGHT
```

- 行动期间精灵倒下会被引擎自动切换下一只健康精灵；
- **训练师轮战**：敌方当前出战精灵倒下后自动派下一只健康精灵（换宠当回合新精灵不行动），
  敌方整队精灵全部倒下才 `PLAYER_WIN`；玩家整队全倒才 `PLAYER_LOSE`；不可逃跑/捕捉（§3.4）；
- 每击倒一只对手就把「参战且未倒下的己方精灵」与「刚被击倒的那一只」申报给成长端口（§13）：
  引擎不计算经验、不判定升级/学招/进化，只把成长模块返回的日志行追加到战斗日志；训练师轮战
  **逐只申报**、不等整场结束，整场以 `PLAYER_WIN` 收尾时另经 `onBattleWon` 申报一次；
- 每回合行动结束后（双方仍站场时）引擎结算天气/场地回合末效果，并推进
  天气/场地剩余回合（见 §7）。

## 7. 天气与场地

战斗双方共享同一片天气（`Weather`）与场地（`Terrain`），由携带对应效果的
**变化类技能**开启，持续固定回合后自然消退，持续期间影响招式威力和回合末效果。

### 7.1 开启与持续

- **开启方式**：使用带效果的变化类技能（如「大晴天」「青草场地」）。普通物理/特殊
  技能不影响天气与场地。效果枚举 `MoveEffect`（`org.example.model`）把技能映射到
  对应 `Weather` / `Terrain`。
- **持续回合**：`Weather.DURATION_TURNS` / `Terrain.DURATION_TURNS` 均为 5 回合
  （含开启当回合）。每回合末引擎推进计数，归零后天气/场地消退并写入结束消息
  （如「沙暴平息了。」「青草场地的效果消失了。」）。
- **覆盖与重置**：再次开启同类天气/场地会重置剩余回合并提示「持续回合重置了！」；
  开启不同类的天气/场地则替换前一类。天气与场地相互独立，可同时生效。

### 7.2 效果明细

| 状态 | 开启技能效果（`MoveEffect`） | 招式威力倍率 | 回合末效果 |
|------|------------------------------|--------------|------------|
| `Weather.SUNNY` 大晴天 | `SUNNY_DAY` | 火系 ×1.5 | — |
| `Weather.RAIN` 下雨 | `RAIN_DANCE` | 水系 ×1.5 | — |
| `Weather.SANDSTORM` 沙暴 | `SANDSTORM` | — | 非岩石/地面系扣最大 HP 的 1/16 |
| `Weather.HAIL` 冰雹 | `HAIL` | — | 非冰系扣最大 HP 的 1/16 |
| `Terrain.ELECTRIC` 电气场地 | `ELECTRIC_TERRAIN` | 电系 ×1.3 | — |
| `Terrain.GRASSY` 青草场地 | `GRASSY_TERRAIN` | 草系 ×1.3 | 场上非飞行系精灵回复最大 HP 的 1/16 |
| `Terrain.MISTY` 薄雾场地 | `MISTY_TERRAIN` | 龙系 ×0.5 | — |
| `Terrain.PSYCHIC` 精神场地 | `PSYCHIC_TERRAIN` | 超能力系 ×1.3 | — |

规则细节：

- 天气与场地的倍率**可叠加**（伤害计算时按 天气倍率 × 场地倍率 连乘）；
- 回合末效果（沙暴/冰雹扣血、青草回复）仅在**双方都仍站场**时结算；满血精灵不会
  产生回血日志；免疫精灵不产生扣血日志；
- 变化类技能同样消耗 PP 与整回合行动，规则与伤害技能一致（只是不造成伤害）；
- **装备介入（v1.18）**：携带 `UTILITY_UMBRELLA` 的精灵**不吃天气的招式威力倍率**
  （场地倍率仍生效），且免疫沙暴/冰雹回合末削血（同 `POWDER_IMMUNE`）；携带 `TERRAIN_SEED`
  的精灵在**场地开启时**（双方出战精灵）与**换上场的瞬间**判定，场地匹配即提升能力并消耗装备；
- 战斗结束（胜负/逃跑/捕获）后天气/场地不残留到下一场。

### 7.3 界面展示

UI 层通过 `getWeather()` / `getTerrain()` 在每次行动后刷新状态条，展示
`Weather.getDisplayName()` / `Terrain.getDisplayName()`（如「大晴天」「青草场地」）。

## 8. 实现与扩展约束

- 唯一实现 `BattleEngine`（同包），构造时校验玩家出战精灵与敌方（野生精灵或训练师出战精灵）均有效；
- 调用方应通过 `BattleServices` 获取实例，禁止 `new BattleEngine(...)`；
- 若需新增自定义实现，实现 `BattleService` 即可，UI 层无需改动；
- 界面渲染敌方面板一律取 `foeActive()`，不要直接读 `getWild()`（训练师轮战中为 `null`）。

### 8.1 `Move` 数据契约对齐（v1.2）

为适配「宝可梦、技能、队伍接口.md」（v1.0，待组长评审），`org.example.model.Move`
数据契约调整如下（本次仅数据模型对齐，不改变引擎行为）：

| 字段 | 语义 |
|------|------|
| `priority`（新增） | 先制度，数值越大越先出手；0 为普通技能。`getPriority()` 读取。当前引擎按速度先后手，暂未启用先制排序 |
| `accuracy` | 命中率 0~100；**-1 表示必中**（负值在构造时归一为 -1，正值 clamp 至 100）。当前引擎恒命中，未做命中判定 |

构造重载保持向后兼容：原 7/8 参构造委托至新完整构造（`priority=0`），
既有 `GameData` 技能注册代码无需改动。

## 9. 战斗模型契约对齐（v1.3）

按《宝可梦、技能、队伍接口.md》v1.0 对战斗相关模型做了**温和适配**（不破坏既有可运行
语义），新增的契约类型与既有模型补齐如下，详情与取舍见
《适配说明_宝可梦技能队伍接口v1.md》：

### 9.1 新增契约类型（`org.example.model` / `util` / `service`）

| 类型 | 说明 |
|------|------|
| `Nature` | 性格：20 预定义常量，`getModifier` 提供 ±10% 修正 |
| `BaseStats` | 种族值六维（含 `from(Stats)`/`getTotal()`） |
| `StatusCondition` | 异常状态枚举（NONE/POISON/BADLY_POISON/PARALYSIS/BURN/SLEEP/FREEZE/CONFUSION/FAINTED），详见 §10 |
| `StatModifier` | 六能力修正枚举（供性格使用） |
| `LearnableMove` | 可学习技能条目（moveId + level） |
| `GrowthEvent` | 成长事件（LEVEL_UP / LEARN_MOVE / EVOLUTION） |
| `PokemonState` | 存档快照（Player + timestamp） |
| `util.ExperienceCalculator` | medium-fast 经验曲线（`expToLevel`/`expToNextLevel`） |
| `service.PokemonService` | 接口：`getInitialPool()`（I-01 初始池） |

### 9.2 既有战斗相关模型补齐（兼容签名）

- `Move`：`isPhysical()` / `isSpecial()`（配合既有 `priority`/`accuracy`）；
- `MoveSlot`：PP 契约方法 `getCurrentPp/getMaxPp/isExhausted/usePp/restorePp/fullRestore`；
- `Stats`：`static randomIv()`（0~31）与六项 `getXxxIv()` IV 别名；
- `Species`：`getLearnableMoves()/addLearnableMove`（出生技∪习得表）；
- `Player`：`isPartyFull/addPokemon/switchActive/getActiveIndex/getHealthyPokemon/isPartyAllFainted`；
- `Pokemon`：新增 `uuid`/`ivs`/`nature`/`status` 字段与
  `getUuid/getIvs/getNature/getStatus/hasMove(String)/canLearnMove/gainExp/fullHeal`。

### 9.3 与战斗引擎的关系

- 上述字段/方法已通过编译与 SmokeTest（31/31），**BattleEngine 行为不变**；
- 性格修正、IV 差异当前仅作为个体数据维护，**尚未接入战斗结算**，待后续战斗模块按需启用；
  （异常状态自 v1.5 起**已接入战斗结算**，见 §10）
- 天气与场地系统（§7）不受本次适配影响。

## 10. 异常状态（v1.5）

参照原版宝可梦，把**异常状态**接入 `BattleEngine` 结算。规则集中在
`org.example.model.StatusCondition`，模型状态由 `Pokemon` 维护，界面由 `BattleView` 展示。

### 10.1 分类与效果

| 枚举 | 显示名 | 类别 | 效果 | 免疫属性 |
|------|--------|------|------|----------|
| `PARALYSIS` | 麻痹 | 主要（可保留） | 实际速度 ×0.5；每次行动有 **25%** 概率无法行动（不消耗 PP） | 电 |
| `POISON` | 中毒 | 主要 | 回合末损失 **1/8** 最大 HP | 毒 |
| `BADLY_POISON` | 剧毒 | 主要 | 回合末损失 `max(1, 计数)/16` 最大 HP，计数逐回合 +1（起始 1） | 毒 |
| `BURN` | 灼伤 | 主要 | 回合末损失 **1/16** 最大 HP；**物理**攻击 ×0.5 | 火 |
| `SLEEP` | 睡眠 | 主要 | 无法行动，随机 **2~5** 回合后自动醒来（醒来当回合即可行动） | — |
| `FREEZE` | 冰冻 | 主要 | 无法行动，每次行动有 **20%** 概率自行解冻（解冻当回合即可行动） | 冰 |
| `CONFUSION` | 混乱 | 挥发性（换宠/倒下即清） | 每次行动有 **33%** 概率**自伤**（威力 40 的无属性物理伤害）；随机 **2~5** 回合后解除 | — |
| `FAINTED` | 倒下 | 特殊 | 不可行动（由 `takeDamage` 自动置位） | — |
| `NONE` | — | — | 无异常状态 | — |

- **主要异常**（`isMajor()`）同一时刻只能存在一种；**挥发性异常**（`isVolatile()`，即混乱）
  可与主要异常**共存**，但不可叠加。
- `majorStatuses()` 返回全部 6 种主要异常的不可变列表（供「万灵药」等判定使用）。
- 常量：`BADLY_POISON_START=1`、`SLEEP_MIN_TURNS=2`、`SLEEP_MAX_TURNS=5`、
  `CONFUSION_MIN_TURNS=2`、`CONFUSION_MAX_TURNS=5`、`PARALYSIS_SKIP_CHANCE=0.25`、
  `CONFUSION_SELF_HIT_CHANCE=0.33`、`FREEZE_THAW_CHANCE=0.2`、`CONFUSION_SELF_HIT_POWER=40`。

### 10.2 规则查询 API（`StatusCondition`）

| 方法 | 说明 |
|------|------|
| `isMajor()` / `isVolatile()` / `preventsAction()` | 分类与「是否彻底无法行动」（睡眠/冰冻/倒下为 true，麻痹为 false） |
| `speedMultiplier()` / `attackMultiplier()` | 实际速度/物攻倍率（麻痹 0.5；灼伤 0.5） |
| `residualDamageRatio(int badlyPoisonCounter)` | 回合末持续伤害占最大 HP 的比例（0 表示无伤害） |
| `residualMessage(int badlyPoisonCounter)` | 持续伤害日志用的状态名（无伤害时为空串） |
| `canApply(Pokemon target)` | 是否可施加：**属性免疫 > 已混乱 > 已有主要异常** 依次判定 |
| `immunityType()` | 免疫该异常的属性（无则 `null`） |
| `parse(String)` / `getDisplayName()` | 按英文名或中文名解析（非法/空白/`null` 归一为 `NONE`）；显示名 |

### 10.3 `Pokemon` 状态契约

`Pokemon` 维护 `status`（主要异常）、`confusionTurns`、`sleepTurns`、`badlyPoisonCounter`，
并提供：

| 方法 | 说明 |
|------|------|
| `getStatus()` / `setStatus(StatusCondition)` | 读取/直接设置主要异常（混乱走混乱计数；睡眠取最短；剧毒计数重置为 1） |
| `tryApplyStatus(StatusCondition, int turns)` | 施加异常（含回合数），成功返回 `true`、失败返回 `false` |
| `cureStatus()` | 解除主要异常，返回被解除的状态（无则 `NONE`） |
| `isConfused()` / `getConfusionTurns()` / `applyConfusion(int)` / `clearConfusion()` | 混乱的查询/施加/清除（`clearConfusion()` 返回此前是否存在混乱） |
| `clearAllStatus()` | 清除全部异常状态（含混乱、睡眠回合数、剧毒计数） |
| `getSleepTurns()` / `tickSleep()` | 睡眠剩余回合数；回合推进（`true`=仍在睡，`false`=本回合醒来且状态已清） |
| `tickConfusion()` / `getBadlyPoisonCounter()` / `increaseBadlyPoisonCounter()` | 混乱回合推进；剧毒计数读取与递增 |
| `effectiveSpeed()` / `effectiveAttack()` | 计入异常倍率后的**实际**速度/物攻（`BattleEngine` 先手判定、逃跑判定、伤害计算一律使用它） |
| `fullHeal()` / `fullRestore()` | 完全回复，内部调用 `clearAllStatus()` |

倒下（`takeDamage` 使 HP 归零）时自动清除自身混乱。

### 10.4 引擎结算流程（`BattleEngine`）

1. **先后手**：按 `effectiveSpeed()` 比较；同速再用随机数决定（`firstMover`）。
2. **玩家行动**（`playerAct`）：先做 `canAct` 判定，**通过后**才 `MoveSlot.use()`
   ——因为 `MoveSlot.use()` 在 PP 耗尽时返回 `false` 且不消耗 PP，判定必须前置。
3. **`canAct(self)`** 判定顺序：`睡眠`（`tickSleep`）→ `冰冻`（20% 解冻）→ `麻痹`（25% 略过）
   → `混乱`（33% 自伤，威力 40）。返回 `false` 表示本回合无法行动。
4. **命中后施加异常**（`tryInflict`，`executeMove`/`performAttack` 中调用）：
   `condition.canApply(defender)` → 概率判定（`nextInt(100) >= chance` 则不触发，即
   `inflictionChance=N` 时为 `N%`）→ 随机回合数（仅睡眠/混乱，2~5）→ `tryApplyStatus`。
   变化类技能只要带 `inflicts` 也会进入结算（`MoveEffect.NONE` 不再提前跳过）。
5. **回合末**（`applyFieldEndEffects` → `statusEndTurn`）：先结算天气/场地，再按
   `residualDamageRatio` 扣血（中毒/剧毒/灼伤），随后剧毒计数 +1。麻痹/睡眠/冰冻/混乱**无**回合末伤害。
6. **换宠**（`switchActive`）：收回的精灵 `clearConfusion()` 并提示「混乱解除了！」；
   主要异常**保留**。
7. **敌方行动**（`foeTurn`）同样先做 `canAct` 判定。
8. `finishRound` 在 `Status.ONGOING` 时才推进回合，避免战斗结束后继续结算。

### 10.5 技能与数据契约

`org.example.model.Move` 新增两个字段（构造器为 11 参重载，旧签名保持不变）：

| 字段 | 说明 |
|------|------|
| `inflicts`（`StatusCondition`） | 命中后尝试施加的异常状态（`NONE` 表示不附带） |
| `inflictionChance`（`int`） | 施加概率，单位 %（`100` 为必定施加） |

查询：`getInflicts()` / `hasInfliction()` / `getInflictionChance()`。

`src/main/resources/data/moves.csv` 扩展为 11 列，**第 9、10 列**为 `inflicts`、`inflictionChance`
（`# id,name,type,category,power,accuracy,maxPp,priority,inflicts,inflictionChance,effect`）。
运行期数据源 `org.example.pokemon.infrastructure.GameData` 的 `loadMoves()` 读第 9/10 列，
`PokemonBattleAdapter.toBattleMove` 负责把字符串解析为 `StatusCondition` 后传入战斗模型。

已有的附带异常招式（节选）：`will-o-wisp`（灼伤 100%）、`sleep-powder`（睡眠 100%）、
`thunder-wave`（麻痹 100%）、`toxic`（剧毒 100%）、`poison-powder`（中毒 100%）、
`confuse-ray`（混乱 100%）、`body-slam`（麻痹 30%）、`thunder`（麻痹 30%）、
`water-pulse`/`confusion`/`psybeam`（混乱 20%/10%/10%）、
`ember`/`fire-punch`（灼伤 10%）、`thunder-shock`（麻痹 10%）、`ice-beam`/`ice-punch`（冰冻 10%）。
内建招式表同步补齐 `m_will_o_wisp`、`m_sleep_powder`、`m_thunder_wave`、`m_toxic`、
`m_poison_powder`、`m_confuse_ray`、`m_body_slam`、`m_ice_fang`，
并为初始种族池补充习得项（`learnable_moves.csv`）。

### 10.6 解除道具

`ItemCategory` 新增 `CURE`；`Item` 新增字段 `curesSpec` 与 6 参构造、
静态工厂 `cureItem(id, name, statusSpec)` / `cureAllItem(id, name)`。

| 方法 | 说明 |
|------|------|
| `getCuresSpec()` | 原始解除范围文本 |
| `canCure(StatusCondition)` | 是否可解除该状态（`ALL` 对全部**主要**异常生效，不含混乱） |
| `curedStatuses()` | 逐项解析后的解除范围列表（`ALL` 时为空列表） |
| `curesAll()` | 是否解除全部主要异常（`ALL`） |

`curesSpec` 支持**多项**，以 `|` / `,` / 空白 分隔，命中任意一项即可解除
（如解毒药 `POISON|BADLY_POISON`）；`ALL` 表示全部主要异常。
`items.csv` 第 6 列为 `curesSpec`；内建道具：`i_antidote`（`POISON|BADLY_POISON`）、
`i_paralyze_heal`、`i_burn_heal`、`i_ice_heal`、`i_awakening`、`i_full_heal`（`ALL`）。
新档由 `PokemonBattleAdapter.grantStartingItems` 发放这些道具。

`BattleEngine.useItem` 对 `CURE` 的处理：调用 `cureWithItem`，**仅在使用成功（确有针对的异常）
时才 `consume`**，随后照常进入 `foeTurn()`。

**目标规则（v1.8）**：回复/解除道具可作用于队伍任意精灵（含替补），目标由 `partyIndex` 指定：

- 下标越界 → 追加「没有可以使用的目标。」，不消耗道具、不消耗回合；
- `HEAL` 面对**已倒下**精灵 → 追加「已经倒下了，【伤药】无法使用。」，不消耗（不能以此复活）；
- `HEAL` 面对满血精灵 → 追加「HP 是满的」提示，不消耗；
- `CURE` 面对**已倒下**精灵 → **仍然有效**（倒下时异常状态保留）；
- `POKE_BALL` 忽略 `partyIndex`（始终投向敌方）。

`BattleController` 相应把道具菜单拆成两步：选道具 → （回复/解除类）选目标精灵；
`isItemDisabled` 的语义由「当前出战精灵」改为「队伍中是否存在任一合法目标」，
精灵球恒可用（能否捕捉交由引擎判定）。

### 10.7 界面展示

- 双方精灵卡片增加**状态徽章**（`BattleView.statusBadgeText`，橙底 `#ffe6c7`/深字 `#a35200`），
  依次展示主要异常、混乱、倒下；
- 招式列表的说明文字叠加异常提示（`moveEffectText` + `inflictionText`）：
  概率 100% 显示「使目标陷入X状态」，否则显示「可能使目标陷入X状态（n%）」；
- 队伍信息与队伍菜单展示异常摘要，并显示实际速度
  （`effectiveSpeed()`，「（实际 n）」）；
- 道具菜单中解除道具会显示适用范围（`BattleController.curesText`，多项用 `/` 连接）。

### 10.8 测试覆盖

`src/test/java/org/example/battle/BattleEngineStatusTest.java`（**38** 个用例）覆盖：
100% 施加、概率未命中、5 种属性免疫、主要异常不可覆盖、混乱共存不可叠加、
麻痹（略过行动/不消耗 PP/降速/降速失先手）、睡眠（不行动/醒来当回合可行动）、
冰冻（未解冻/自行解冻）、混乱（自伤/正常行动/换宠清除/倒下清除）、
中毒 1/8、灼伤 1/16 与物攻减半、剧毒递增、无持续伤害的异常、
解除道具（治愈并消耗/无异常不消耗/不对应不治/万灵药/解毒药解剧毒/完全治疗清全部）、
目标选择（作用于替补精灵/单参重载作用于出战精灵/倒下精灵不可回复但可解除异常/
下标越界不消耗/使用后敌方仍行动一次）、
规则表与文案、`parse` 中英文与非法输入、免疫表、道具契约与内建道具范围。

测试用 `ScriptedRandom` 固定随机源（`nextInt` 取模、`nextDouble` 恒定、`nextBoolean` 恒 `false`），
并让玩家精灵速度远高于敌方以保证先手，从而让随机数只影响被测行为。

## 11. 数据端口：`BattleDataPort`（v1.6）

### 11.1 定位

战斗模块**只消费数据、不拥有数据**。引擎运行期需要的外部数据只有四类，统一抽象为只读端口：

| 需求 | 端口方法 | 使用场景 |
|------|----------|----------|
| 按 id 查技能 | `Move findMove(String moveId)` | 外部成长模块的到级学招（`GrowthService#learnAt`，见 §13） |
| 按 id 查种族 | `Species findSpecies(String speciesId)` | 外部成长模块的进化（`GrowthService#evolve`） |
| 野生遭遇候选池 | `List<String> wildSpeciesPool()` | 组装/流程侧随机遭遇（`WildEncounter.randomWild`） |
| 按种族生成个体 | `Optional<Pokemon> createPokemon(String speciesId, int level)` | 组装/流程侧随机遭遇（`WildEncounter.randomWild`） |

全限定名：`org.example.battle.BattleDataPort`（接口）。

> 查不到即返回 `null`（`findMove`/`findSpecies`）或 `Optional.empty()`（`createPokemon`），
> 调用方（成长模块 / 遭遇生成）据此**静默跳过**对应功能，不抛异常。
>
> **v1.9 注**：引擎本体已不再查询本端口（成长判定外移至 §13），端口仍是战斗相关外部数据的
> **统一入口**，供外部成长模块与遭遇生成共用。

### 11.2 空实现

`org.example.battle.BattleDataPorts` 提供 `BattleDataPorts.none()`：所有查询返回「查不到」，
用于纯规则测试或不接数据源运行的场景。**未注入端口的所有工厂/构造器入口都使用它**，
因此既有调用点（`BattleServices.newBattle(player, wild)` 等）行为保持不变：战斗规则照常运行，
依赖数据的「随机野生遭遇」（§12）与外部成长模块的「到级学招 / 进化」（§13）降级为无操作。

### 11.3 默认实现

`BattleDataPort#createPokemon` 提供默认实现：按 `Species#getMoveIds()` 取种族首批技能
（最多 `Pokemon.MAX_MOVES` 个，跳过端口查不到的技能）生成个体。数据模块可覆盖此方法
（例如按等级筛选可学技能）。

> **已知取舍**：端口本应只描述「需求」，该默认实现却含少量「按种族首批技能生成」的
> **生成策略**。保留它换取实现方免写样板代码；若后续生成规则复杂化，应把策略整体
> 移入数据侧实现，让端口退回纯声明。

### 11.4 外部实现与注入

数据模块通过**适配器**实现端口，把自身数据源暴露给战斗模块；组装层负责创建并注入：

- `org.example.data.GameDataBattleDataPort`：把数据注册表 `GameData` 适配为端口；
- `org.example.integration.PokemonBattleAdapter#battleDataPort()`：组装层统一入口
  （`MainController` 经此注入战斗引擎与成长模块）；
- `org.example.demo.BattleConsole`：演示入口自行构造并注入。

```java
// 组装层（控制器 / 演示入口）
BattleDataPort dataPort = PokemonBattleAdapter.battleDataPort();
BattleGrowthPort growthPort = PokemonBattleAdapter.battleGrowthPort(dataPort);   // 见 §13
BattleService battle = BattleServices.newBattle(player, wild, dataPort, growthPort);
```

### 11.5 解耦约束

- `org.example.battle` 包内**不出现**数据文件、内建数据表，也不引用数据模块
  （如 `org.example.data`）；所有数据一律经端口获取；
- 新增外部数据源（CSV、网络、另一套宝可梦系统）时无需改动战斗模块，只要实现端口并在
  组装层替换即可；
- 道具不属于数据端口：`useItem(Item, int)` 只消费**调用方传入的实例**，道具来源由组装层决定。

### 11.6 测试覆盖

`src/test/java/org/example/battle/BattleDataPortTest.java`（**6** 个用例）覆盖：
空端口不提供任何数据、升级学招经成长模块查询技能（注入成长端口 / 未注入成长端口）、
进化经成长模块查询种族（注入 / 未注入）、端口默认个体生成的技能截断与查不到种族返回空。

随机遭遇相关用例随逻辑一并迁至
`src/test/java/org/example/integration/WildEncounterTest.java`（见 §12）；
成长判定相关用例见 `src/test/java/org/example/growth/GrowthServiceTest.java`（见 §13）。

## 12. 遭遇生成（模块外）

> v1.7 起，「野生遭遇生成」不再属于战斗模块。战斗模块只负责「给定双方进行演算与结算」，
> 「遇到谁、几级」由组装 / 流程侧决定。

### 12.1 位置与迁移对照

全限定名：`org.example.integration.WildEncounter`（`final`，私有构造，禁止实例化）。

| v1.6 旧位置 | v1.7 新位置 | 说明 |
|-------------|-------------|------|
| `BattleServices#wildLevelAround(int)` | `WildEncounter#levelAround(int)` | 等级浮动，改用 `ThreadLocalRandom` |
| — | `WildEncounter#levelAround(int, Random)` | 可注入随机源版本（便于复现） |
| `BattleServices#randomWild(int)` | 移除 | 等价空端口无意义，随迁移删除 |
| `BattleServices#randomWild(int, BattleDataPort)` | `WildEncounter#randomWild(int, BattleDataPort)` | 数据仍经端口取 |
| — | `WildEncounter#randomWild(int, BattleDataPort, Random)` | 可注入随机源版本 |

### 12.2 行为

| 方法 | 返回 | 说明 |
|------|------|------|
| `int levelAround(int playerLevel)` | 建议等级 | 在 `[level-2, level+2]` 内随机，最低 `MIN_LEVEL`（2） |
| `int levelAround(int playerLevel, Random random)` | 建议等级 | 同上，随机源可注入 |
| `Optional<Pokemon> randomWild(int aroundLevel, BattleDataPort dataPort)` | 野生精灵 | 从端口野生池随机挑选并经端口生成个体 |
| `Optional<Pokemon> randomWild(int aroundLevel, BattleDataPort dataPort, Random random)` | 野生精灵 | 同上，随机源可注入 |

常量：`LEVEL_SPREAD = 2`（等级浮动半宽）、`MIN_LEVEL = 2`（等级下限）。

### 12.3 内聚性收益

- 战斗模块内**不再出现 `Math.random()`**：包内所有随机源均为可注入的 `java.util.Random`
  （引擎构造器参数），遭遇侧同理，测试可完全复现；
- `BattleServices` 收敛为**纯工厂**（仅 12 个 `newBattle` / `newTrainerBattle` 重载，含 v1.9
  新增的 4 个成长端口重载）；
- 遭遇生成只依赖 `BattleDataPort` 与 `Random` 两个抽象，与战斗模块**同向解耦**。

### 12.4 调用点

- `org.example.controller.MainController#startRandomBattle()`：`WildEncounter.levelAround(...)`
  → `PokemonBattleAdapter.createWildPokemon(level)` → `BattleServices.newBattle(...)`，
  并注入 `PokemonBattleAdapter.battleDataPort()` 与 `battleGrowthPort(dataPort)`；
- `org.example.demo.BattleConsole#oneWildBattle()`：
  `WildEncounter.randomWild(WildEncounter.levelAround(top), BATTLE_DATA)`
  → `BattleServices.newBattle(player, wild, BATTLE_DATA, GROWTH)`。

### 12.5 测试覆盖

`src/test/java/org/example/integration/WildEncounterTest.java`（**5** 个用例）覆盖：
等级浮动始终落在 `[等级-2, 等级+2]`（200 次随机采样）、不低于下限、
可注入随机源复现、随机遭遇经注入端口取野生池与生成个体、端口无野生池时返回空。

## 13. 成长端口：`BattleGrowthPort`（v1.9）

### 13.1 定位

战斗模块**只做演算与结算**。胜利结算时不自行计算经验、不判定升级 / 学招 / 进化，而是把
「参战且未倒下的己方精灵」（`survivors`）与「被击败的对手」（`defeated`）**申报**给成长端口，
由外部成长模块自行判定一切，并把需要写入战斗日志的文本行返回。

| 需求 | 端口方法 | 说明 |
|------|----------|------|
| 击倒申报（v1.11 语义） | `Settlement settle(List<Pokemon> survivors, List<Pokemon> defeated)` | **每击倒一只对手调用一次**：`defeated` 恒为刚被击倒的那一只；返回日志行与挂起的学招抉择 |
| 获胜申报（v1.11） | `void onBattleWon(List<Pokemon> survivors)` | 整场战斗结束且 `PLAYER_WIN` 时**调用一次**（野生获胜 / 训练师全队倒下各一次）；`default` 空实现，供成长模块按**场次**做事（如累计图鉴对战次数） |
| 学招抉择执行 | `List<String> resolveLearn(BattleService.LearnChoice choice, int forgetSlotIndex)` | 引擎转发玩家的遗忘槽位选择（`-1` = 放弃学习） |
| 捕捉申报（v1.10） | `void onCaptured(String speciesId)` | 引擎在野生遭遇**捕捉成功**时调用；`default` 空实现，不实现即保持原行为 |

常量与嵌套类型：

| 名称 | 形态 | 说明 |
|------|------|------|
| `BattleGrowthPort.Settlement` | `record(log, pendingLearns)` | `List<String> log`（逐行追加到战斗日志）、`List<BattleService.LearnChoice> pendingLearns`（技能栏已满时挂起） |
| `BattleGrowthPort.none()` | 静态方法 | 空端口：`settle` 返回空 `Settlement`，`resolveLearn` 返回空列表，`onCaptured` 与 `onBattleWon` 为空操作 |

全限定名：`org.example.battle.BattleGrowthPort`（接口，契约放在引擎侧被跨模块共享）。

> `onCaptured` 与 `onBattleWon` 之所以是 `default`：战斗模块只负责判定「是否捕捉成功 / 战斗是否
> 获胜」并申报事实，**收益如何换算由成长模块决定**；`none()` 与既有第三方实现因此无需改动即兼容。
>
> **`settle` 的语义变化（v1.11）**：旧版是「整场结束后把整队被击败对手一次性申报，由成长模块
> 求和后发放」；新版改为「**每击倒一只就申报一次**」，成长模块**立即**结算该只的经验。收益差别：
> 中途倒下的己方精灵也能拿到已经击倒对手的经验，**战败不回收**；对战次数统计改由 `onBattleWon`
> 按场次进行，避免逐只申报把一场战斗重复计数。

### 13.2 引擎侧流程

```
每击倒一只对手（v1.11：野生精灵倒下 / 训练师轮战每只精灵倒下）
  └─ settleGrowth(Pokemon defeated)            // BattleEngine 私有
       ├─ survivors = 过滤 player.getParty() 中未倒下者
       ├─ growthPort.settle(survivors, List.of(defeated))   // defeated 恒为一只
       ├─ 逐行 append(settlement.log())          // 引擎只做日志搬运
       └─ pendingLearns.addAll(...)              // 队列管理仍在引擎（可能在战斗中非空）

整场战斗获胜（PLAYER_WIN）
  └─ reportBattleWon()
       └─ growthPort.onBattleWon(survivors())   // 整场一次，供按场次统计（v1.11）

捕捉成功（tryCapture / useItem 路径，v1.10）
  └─ growthPort.onCaptured(wild.getSpecies().getId())   // 引擎只申报物种 id
```

- 击倒申报与胜负**无关**：只要对手倒下就申报，因此**战败也不会回收已发放的经验**；
- 训练师轮战在 `switchToNextHealthy()`（派出下一只）**之前**申报，申报的必是刚倒下的那一只；
- `onBattleWon` 在 `status = PLAYER_WIN` 之后调用，整场战斗**只调用一次**；
- 野生遭遇申报「野生精灵」，训练师轮战**逐只**申报被击败对手（经验分配策略由成长模块决定）；
- 训练师轮战**不可捕捉**，因此 `onCaptured` 只会在野生遭遇路径触发；
- `onCaptured` 在野生精灵**已入队之后**调用，且引擎不追加任何日志行（捕捉文案由原有逻辑产生，
  避免重复输出）；
- `BattleService#pendingLearnChoices()` 返回成长模块挂起的抉择；`decideLearn(int)` 只做
  队列出入队 + 转发给 `resolveLearn`，文本行为全部由成长模块产生；
- 未注入成长端口时使用 `BattleGrowthPort.none()`：战斗规则照常运行，击倒与获胜后不做任何成长。

### 13.3 外部实现：`GrowthService`

全限定名：`org.example.growth.GrowthService`（`final`，实现 `BattleGrowthPort`），
组装层经 `org.example.integration.PokemonBattleAdapter#battleGrowthPort(BattleDataPort[, GrowthProgress])`
创建，与战斗模块**共用同一份** `BattleDataPort`。

| 规则 | 说明 |
|------|------|
| 经验折算（v1.11） | 被击败精灵**种族经验值 `Species#getBaseExpYield()` × 对方等级 / 7**，单只不低于 30；种族未提供 baseExp（= 0）时退化为「六维种族值总和 × 等级 / 5」 |
| 逐只即时发放（v1.11） | 战斗模块**每击倒一只**申报一次，成长模块立即结算该只经验：中途倒下的精灵也能拿到已击倒对手的经验，**战败不回收** |
| 经验可见反馈（v1.12） | 每次结算为每只获得经验的精灵追加一行「`名字 获得了 N 点经验！`」战斗日志；未升级时附带「`（当前/升级所需）`」进度，升级时该行在前、「`名字 升到了 Lv.N！`」逐行在后；满级（`Pokemon#MAX_LEVEL`）不再产生该行。**因此即便一次击倒不足以升级，日志中也能看到经验入账** |
| 到级学招 | `Species#moveLearnedAt(level)` 取技能 id → 经数据端口查 `Move`；有空槽直接学会，满 4 招挂起 |
| 进化 | `canEvolve()` 满足时经数据端口查目标种族（查不到则不进化），进化后重算属性并回满 |
| 降级 | 数据端口查不到技能 / 种族时，学招与进化静默跳过；经验与升级照常结算 |
| 图鉴进度（v1.11） | 持有 `GrowthProgress`：`onBattleWon` 按参战精灵累计该族**对战次数**（按**场次**计，逐只击倒不计数），`onCaptured` 累计该族**捕捉次数** |
| 本地永久存档（v1.10） | `GrowthProgress` 由 `GrowthProgressStore` 载入并绑定自动保存：每次捕捉/对战/清档后立即落盘，**关闭程序再打开仍保留**（详见 §13.7） |

> **职责对照**：升级 `Pokemon#addExp`、习得表 `Species#moveLearnedAt`、进化 `Pokemon#evolveTo`
> 等模型能力仍在 `org.example.model`；**判定规则**（何时算、算多少、学哪招）自 v1.9 起只存在
> 于成长模块，战斗模块不含任何经验曲线或习得表。

### 13.4 解耦收益

- 战斗模块可**单独测试**：不接成长模块时（默认空端口）战斗行为与原版一致，无成长副作用；
- 成长规则可替换：升级曲线、学招策略、进化条件改动只影响成长模块，战斗模块与 UI 不动；
- 结算**申报式**而非查询式：引擎不需要知道成长模块"想查什么"，只把事实（谁参战、谁被击败）
  交出去，成长模块可自由决定经验分配（如仅出战精灵得经验、道具加成等）。

### 13.5 测试覆盖

`src/test/java/org/example/growth/GrowthServiceTest.java`（**24** 个用例）覆盖：
经验按**种族经验值 baseExp × 等级 / 7** 折算、对手等级越高经验越多、对手种族经验值越高经验越多、
30 点下限、种族未提供 baseExp 时退化为「六维种族值总和 × 等级 / 5」、**每击倒一只单独结算一次**、
倒下精灵不结算、无对手不结算、**未升级时也返回带进度的经验反馈日志**、**升级时经验行在升级行之前**、
空槽到级学招、满 4 招挂起抉择、查不到技能时学招降级、
达到进化等级经端口进化、查不到种族时进化降级、抉择遗忘并学会、抉择放弃学习、空抉择返回空日志，
以及**图鉴申报**：捕捉申报累计捕捉次数、空物种 id 申报被忽略、**获胜申报按参战精灵累计对战次数**、
**击倒结算不累计对战次数**、未参战精灵不写入、经成长模块局外查询进度。

`src/test/java/org/example/growth/GrowthProgressTest.java`（**11** 个用例）覆盖图鉴数据接口：
空记录不写入、捕捉次数递增、加成按每 2 次捕捉记 1 点、单族与全局加成封顶 31、
对战次数独立累计、图鉴排序与包含对战-only 种族、跨轮累积、清空、空 id 拒收、加成截断。

`src/test/java/org/example/growth/GrowthProgressStoreTest.java`（**14** 个用例，全部在
`@TempDir` 内进行、不触碰开发者本机存档）覆盖 v1.10 的**本地永久存档**：无文件时空档、
计数往返一致、**进度变化立即落盘且跨「重启」恢复**、多族进度与全局加成跨重启保持、
存档为可读文本且按物种排序、注释与空行忽略、损坏行跳过不整档报废、**存档超量次数仍受上限约束**、
空记录不写入、父目录自动创建、`clear()` 同时清空文件、写入失败不抛异常且仅存内存、
未绑定落盘回调时不触碰磁盘、默认路径位于用户主目录。

`src/test/java/org/example/pokemon/service/PokemonServiceImplGrowthTest.java`（**6** 个用例）覆盖
加成落地：无进度时个体值合法、捕捉后各项 ≥ 加成、满加成必满个体、**种族值不变**、
满个体面板高于零个体、服务暴露进度。

`src/test/java/org/example/integration/PokemonBattleAdapterTest.java`（**8** 个用例）覆盖接缝：
其中 2 例为 v1.10 新增 —— 个体值跨系统搬运、满加成经接缝传导为满个体；
1 例为 v1.11 新增 —— **种族经验值 baseExp 随种族跨系统搬运**。

引擎侧 v1.11 / v1.12 新增：
`src/test/java/org/example/battle/BattleEngineTrainerTest.java`（**14** 个用例）新增 5 例 ——
训练师战每只对手倒下都即时申报成长、野生遭遇倒下时即时申报成长、
**战斗落败也不回收已申报的经验**、未注入成长端口时照常运行、
**击倒一只对手后日志出现「获得了 N 点经验」反馈且经验确已入账**（v1.12，注入真实 `GrowthService`）。

`src/test/java/org/example/battle/BattleDataPortTest.java` 覆盖「注入成长端口后经端口取技能 /
种族」与「未注入成长端口时等级不变、不学招、不进化」两组对照。

> **测试隔离约定（v1.13）**：`GrowthService` 与 `PokemonBattleAdapter` 的单参入口
> （`new GrowthService(dataPort)` / `battleGrowthPort(dataPort)`）使用的是进程级
> `GrowthProgress#instance()`，它绑定**用户主目录下的真实成长存档**，战斗获胜即直接落盘。
> 因此**测试一律走两参入口并传入 `new GrowthProgress()`**（无落盘回调的纯内存实例），
> 以防跑测试污染开发机存档。两个单参入口的 Javadoc 已加 `@apiNote` 警示。

> 回归基线：`mvn -o test` → **290/290 通过**（v1.11 时为 287，v1.10 时为 278，v1.9 时为 108）。

### 13.6 个体值与属性演算（v1.10）

成长机制提升的是**个体值**，因此个体值必须真实参与战斗模型的面板演算，否则加成不可见。

| 项 | 规则 |
|----|------|
| 战斗模型公式 | `Pokemon#computeStats(species, level, ivs)`：HP = `(2×种族值 + 个体值) × 等级 / 100 + 等级 + 10`，其余五项为 `(2×种族值 + 个体值) × 等级 / 100 + 5` |
| 个体值来源 | `Pokemon#create(species, level, movePool[, ivs])`；省略 `ivs` 时按 `Stats.randomIv()` 随机 0~31 |
| 升级重算 | `applyStatsOnLevelUp()` 使用个体持有的 `ivs`，升级不会让个体值漂移 |
| 跨模块搬运 | `PokemonBattleAdapter#toBattlePokemon` 把新体系的个体值经 `toBattleStats` 显式搬运；**此前该字段在接缝处被丢弃并重新随机**（旧行为），v1.10 起保留 |
| 种族值不变 | 个体值加成只作用于个体值，`Species#getBaseStats()` 与性格一律不受影响 |

> 兼容性：个体值全 0 时新公式与旧公式（`种族值 × 2 × 等级 / 100`）等价，因此仅当个体值 > 0
> 时面板才比旧版更高；既有测试用精灵的速度 / 耐久断言不受影响。

### 13.7 成长进度的本地永久存档（v1.10）

个体值成长是**本地单机永久**机制：关闭程序、重开游戏、开启新一轮远征后，已获得的加成必须仍然生效。

| 项 | 规则 |
|----|------|
| 存档位置 | `<用户主目录>/.pokerouge/growth-progress.txt`（`GrowthProgressStore#defaultFile()`）；放用户主目录而非工程目录，重新构建 / 切分支 / 清 `target` 都不丢档 |
| 存档格式 | UTF-8 文本，含格式头与说明注释，每行 `物种id=捕捉次数,对战次数`，按物种 id 排序，人类可读、可手工清档 |
| 载入时机 | `GrowthProgress#instance()`（进程级单例）首次访问时经 `GrowthProgressStore#loadProgress()` 从文件载入并**绑定自动保存**；新增的 `GameSession` / `PokemonServiceImpl` / 战斗组装都会取到同一份已载入的进度 |
| 保存时机 | `recordCapture` / `recordBattle` / `clear` 之后立即落盘（原子写：先写 `.tmp` 再替换，避免中途崩溃损坏存档） |
| 上限 | `IvGrowthRule#MAX_IV = 31`。上限在**规则层**强制，因此即便手工把文件次数改到数千，游戏内加成仍封顶 31 |
| 重开存档 | `GrowthProgress#clear()` 同时清空内存与存档文件 |
| 容错 | 读取失败 / 内容损坏 → 按新档处理；单行格式错误只跳过该行，不整档报废；写入失败（目录只读 / 被占用）→ 只打日志，本局进度仅存内存，**不抛异常、不中断战斗** |
| 测试隔离 | `GrowthProgress` 默认构造（`new`）为纯内存、不触碰磁盘；**v1.13 起所有成长 / 战斗用例一律注入 `new GrowthProgress()`**，不再使用会回落到 `GrowthProgress#instance()`（绑定开发者本机真实存档）的单参入口 `new GrowthService(dataPort)` / `battleGrowthPort(dataPort)`；症状是跑完测试后 `<用户主目录>/.pokerouge/growth-progress.txt` 里出现测试用假物种 id（如 `mine_sp` / `mine_evo`） |

> 与「存档系统」的关系：本机制自带极简存档，全局默认档
> （`GrowthProgress#instance()`）仍指向 `<用户主目录>/.pokerouge/growth-progress.txt`；
> 进入**存档系统**的流程不再使用它 —— `SaveStore#loadGrowth(slot)` / `#createGrowth(slot)`
> 按档位读写 `saves/slotN/growth-progress.txt`，载入后经 `GrowthProgress#attachSaver(...)`
> 交由存档模块托管落盘（新游戏会重建空白图鉴，避免残留上一轮进度）。
> 成长规则与战斗模块均无需改动。

---

## 14. 面向外部数据模块的对接文档（v1.0）

外部数据模块（提供技能 / 种族 / 野生池 / 个体生成与成长规则的开发者）的落地指引已单独成文：
**《对接文档_战斗模块数据对接.md》v1.0**，内容包括：

| 章节 | 内容 |
|------|------|
| 一 | 角色与边界（战斗模块只演算 + 只结算；外部模块提供数据与规则；组装层唯一注入点） |
| 二 | 对外接口清单：`BattleDataPort`（必做，4 方法）、`BattleGrowthPort`（建议做） |
| 三 | 战斗模块**已有**能力（可直接消费）：`BattleService` 方法表、演算规则、12 个工厂重载、不做的事 |
| 四 | 使用方法：三步接入、野生战 / 训练师战 / 学招抉择示例、自定义端口与自定义成长规则骨架 |
| 五 | 需要的数据：`Move` / `Species` / 个体契约字段表、9 条一致性硬约束、现有 CSV 参考 |
| 六 | 需外部模块完成的功能交接清单（D-01~D-09）与数据自检代码骨架 |
| 七 | 已知风险（含 `species.csv` 列序错位导致学招 / 进化静默失效） |
| 八 | 缺数据时的降级行为矩阵 |
| 九 | 验收自测清单 |

> 本文档 §11~§13 是该对接文档的技术依据；两份文档变更时需同步更新。

---

## 15. 可携带装备（HeldItem）效果结算（v1.18）

### 15.1 装备模型与接入方式

装备定义在数据模块：`org.example.model.HeldItem`（id/name/effectType/param/description）与
枚举 `org.example.model.HeldItemEffect`（**37 种效果**）。数据源 `data/equipment.csv` 由数据模块
`GameData` 注册（**77 件内建装备：47 件道具 + 30 种树果** + CSV 覆盖加载，`GameData#equipment(id)` /
`allEquipment()`）。树果与道具走**完全相同的模型/注册/穿戴/存档通路**，区别仅在效果语义
（树果均为**一次性消耗品**，触发即从携带栏移除）。

`param` 一律用 `|` 分隔多段，由 `HeldItem` 统一解析，引擎不自己拆字符串：

| 解析 API | 语义 |
|------|------|
| `textPart(int)` | 取第 `index` 段文本（越界返回空串） |
| `doublePart(int, double)` | 取第 `index` 段数值；非法返回兜底值 |
| `doubleParam()` | 单段/倍率类参数：`DAMAGE_TYPE` 取第 2 段（`FIRE|1.2` → 1.2）；其余取 param 整体；**整体解析失败时退化为取首段**（`0.5|GROUND` → 0.5），保证「参数多带一段装饰信息」不会让倍率静默失效 |
| `chanceParam()` | 概率百分比（`FIRST_STRIKE` / `FOCUS_BAND`） |
| `healRatio()` / `damageRatio()` / `damageMultiplier()` / `recoilRatio()` | `POISON_HEAL` / `LIFE_ORB` 的分段语义取值 |
| `choiceKind()` / `choiceMultiplier()` | `CHOICE` 的修正项（`ATTACK` 物理 / `SPECIAL` 特殊 / `SPEED` 速度）与倍率 |
| `weatherParam()` / `durationTurns()` | `WEATHER_DURATION` 的天气名与回合数 |
| `statusParam()` | `END_TURN_STATUS` 的状态英文名 |
| `cureStatuses()` | `CURE_STATUS` 能治愈的异常集合（param 为 `\|` 分隔的异常英文名，填 `ALL` 表示全部异常含混乱；无法识别的名称跳过，非本类型返回空集合） |
| `healThresholdRatio()` / `healAmount()` | `HEAL_HP` 的触发阈值比例 / 回复量（回复量 < 1 视为最大 HP 比例，≥ 1 视为固定点数） |
| `ppRestoreAmount()` | `HEAL_PP` 的回复点数 |
| `resistTypeParam()` / `resistMultiplier()` / `resistUnconditional()` | `RESIST_TYPE` 的减伤属性 / 承伤倍率 / 是否不要求效果拔群（param 第 3 段为 `ALWAYS`） |
| `statParam()` / `statLevels()` / `statLevelsParam()` | **v1.18**：能力等级类参数的取值方式。`statLevelsParam()` = `doublePart(0, 0)`（单段写法，`WEAKNESS_POLICY` 的 `2`）；`statParam()` = `textPart(1)` + `statLevels()` = `doublePart(2, 0)`（三段写法，`ELECTRIC\|DEFENSE\|1`） |
| `seedTerrainParam()` / `reactionTypeParam()` | **v1.18**：`TERRAIN_SEED` 的场地英文名 / `TYPE_REACTION` 的属性英文名，均取 `textPart(0)` |

接入方式与道具一致：装备由**外部流程**（肉鸽 REWARD 事件发放 + UI 图鉴弹窗穿戴）设置到
`Pokemon#setHeldItem`，战斗模块**只读取**，不走 `BattleDataPort`；未穿戴（`heldItem == null`）时全部效果静默不生效。

> **讲究系的锁招状态不进模型**：锁定记录在引擎实例内的 `choiceLocks`（键为精灵 `uuid`），
> 属**本场战斗内存态**——不写回 `Pokemon`、不落存档、换场不残留。节拍器的连续使用记录
> （v1.17）同样存在引擎内存态（键为精灵 `uuid`）。

#### 15.1.1 招式标记 `MoveFlag`（v1.17 新增）

v1.17 之前，招式只有属性 / 类别 / 威力等数值，引擎无法区分「这招是不是接触类」「是不是拳类」
「是不是粉末类」。v1.17 引入 `org.example.model.MoveFlag`：

```java
public enum MoveFlag {
    CONTACT,   // 接触类：可触发凸凸头盔反伤
    PUNCH,     // 拳类：受拳击手套增伤 / 豁免
    POWDER,    // 粉末类：被防尘护目镜完全免疫
    SOUND;     // 声音类：触发爽喉喷雾（v1.18 新增）

    public static Set<MoveFlag> parseFlags(String text);  // "CONTACT|PUNCH" → EnumSet；null/空 → 空集；无法识别的名称忽略
}
```

`org.example.model.Move` 相应新增 `flags` 字段与查询 API：

| 成员 | 语义 |
|------|------|
| `Move(..., Set<MoveFlag> flags)` | **v1.17 的 12 参构造器**（末位为 flags），v1.18 起内部委托给 13 参版本并传 `List.of()` |
| `Move(...)`（旧 11 参构造器） | **保留**，内部委托给 13 参版本并传 `Set.of()` + `List.of()`，既有调用方不受影响 |
| `getFlags()` | 返回不可变标记集合（无标记时为空集，**不返回 null**） |
| `hasFlag(MoveFlag)` | 单个标记判定 |
| `isContact()` / `isPunch()` / `isPowder()` / `isSound()` | 四个常用标记的语义化快捷方法 |
| `getStatChanges()` / `hasStatChanges()` | **v1.18**：招式附带的能力等级变化（无变化时为空列表） |

`parseFlags` **忽略无法识别的名称**（不抛异常），因此在 `moves.csv` 里写错标记名只会静默丢失
该标记、不会让整张表加载失败；空串 / `null` 一律返回空集。

#### 15.1.2 招式能力等级变化 `MoveStatChange`（v1.18 新增）

v1.18 之前变化类招式（叫声、摇尾巴、硬邦邦……）只走 `MoveEffect`，没有任何字段能表达
「降低目标 1 级物攻」，因此这些招式在战斗里**只播报、不产生任何影响**。v1.18 引入
`org.example.model.MoveStatChange`：

```java
public record MoveStatChange(StatModifier stat, int delta, boolean self) {
    public static List<MoveStatChange> parse(String text);  // "DEFENSE|-1;ATTACK|1|SELF" → 不可变列表
}
```

| 语法元素 | 说明 |
|------|------|
| 多段分隔 | `;`（如 `ATTACK\|1\|SELF;DEFENSE\|-1`） |
| 单段格式 | `能力项\|±变化量[\|SELF]` |
| `能力项` | `StatModifier` 英文名（`ATTACK`/`DEFENSE`/`SP_ATTACK`/`SP_DEFENSE`/`SPEED`），大小写与前后空白不敏感 |
| `±变化量` | 整数，可显式带 `+` 号；**`0` 会被丢弃**（无意义） |
| `SELF` | 作用于**使用者自己**；省略则作用于招式目标 |

容错：`null` / 空串 / 全空白 → 空列表；**无法识别的片段被静默跳过**（非法能力项、
非法数值、`HP`（没有能力等级）、缺分隔符的碎句），因此只丢弃脏片段、不影响同字段其余部分；
返回值为 `Collections.unmodifiableList`。

`StatModifier` 相应新增 `getDisplayName()`（中文名，供日志播报）与
`static StatModifier parse(String)`（无法解析返回 `null`，不抛异常）。

### 15.2 效果一览（37 种 / 77 件）

**道具（33 种 / 47 件）**

| 效果类型 | param 约定 | 装备 |
|------|------|------|
| `DAMAGE_TYPE` | `属性\|倍率` | 木炭 / 神秘水滴 / 磁铁 |
| `SUPER_EFFECTIVE` | 倍率 | 达人带 |
| `PHYSICAL_DAMAGE` | 倍率 | 力量头带 |
| `SPECIAL_DAMAGE` | 倍率 | 博识眼镜 |
| `LIFE_ORB` | `倍率\|反伤比例` | 生命宝珠 |
| `SPEED_MULTIPLIER` | `倍率[\|GROUND]` | 黑色铁球（`0.5\|GROUND`） |
| `MOVE_LAST` | 忽略 | 后攻之尾 |
| `FIRST_STRIKE` | 触发百分比 | 先制之爪 |
| `IGNORE_IMMUNITY` | 忽略 | 标靶 |
| `GROUND_IMMUNE` | 忽略 | 气球 |
| `END_TURN_HEAL` | 最大 HP 比例 | 剩饭 |
| `POISON_HEAL` | `回血比例\|扣血比例` | 黑色污泥 |
| `END_TURN_STATUS` | 状态英文名 | 火焰宝珠 / 剧毒宝珠 |
| `LIFE_STEAL` | 造成伤害比例 | 贝壳之铃 |
| `WEATHER_DURATION` | `天气英文名\|回合数` | 炽热 / 潮湿 / 沙沙 / 冰冷岩石 |
| `FOCUS_SASH` | 忽略 | 气势披带 |
| `FOCUS_BAND` | 概率百分比 | 气势头带 |
| `CHOICE` | `修正项\|倍率` | 讲究头带（`ATTACK\|1.5`） / 讲究眼镜（`SPECIAL\|1.5`） / 讲究围巾（`SPEED\|1.5`） |
| `EVOLITE` | 双防倍率 | 进化辉石 |

**招式标记与判定类（6 种 / 6 件，v1.17 新增，依赖 `MoveFlag`）**

| 效果类型 | param 约定 | 装备 |
|------|------|------|
| `PUNCH_BOOST` | 倍率 | 拳击手套（`1.1`） |
| `CONTACT_PUNISH` | 最大 HP 比例 | 凸凸头盔（`0.1667`） |
| `POWDER_IMMUNE` | 忽略（可留空） | 防尘护目镜（无参数） |
| `CRIT_BOOST` | 会心提升等级 | 锐利之爪（`1`） |
| `FLINCH_CHANCE` | 概率百分比 | 王者之证（`10`） |
| `CONSECUTIVE_BOOST` | 每层增幅 | 节拍器（`0.2`） |

**能力等级联动类（7 种 / 14 件，v1.18 新增，依赖 §15.1.2 的能力等级系统）**

| 效果类型 | param 约定 | 装备 |
|------|------|------|
| `ASSAULT_VEST` | 特防倍率 | 突击背心（`1.5`） |
| `CLEAR_AMULET` | 忽略（可留空） | 清净坠饰（无参数） |
| `WEAKNESS_POLICY` | 提升等级 | 弱点保险（`2`：物攻与特攻各 +2） |
| `BLUNDER_POLICY` | 提升等级 | 打空保险（`2`：速度 +2） |
| `THROAT_SPRAY` | 提升等级（配合 `MoveFlag.SOUND`） | 爽喉喷雾（`1`：特攻 +1） |
| `TERRAIN_SEED` | `场地\|能力项\|等级` | 电气种子（`ELECTRIC\|DEFENSE\|1`） / 超能种子（`PSYCHIC\|SP_DEFENSE\|1`） / 薄雾种子（`MISTY\|SP_DEFENSE\|1`） / 青草种子（`GRASSY\|DEFENSE\|1`） |
| `TYPE_REACTION` | `属性\|能力项\|等级` | 球根（`WATER\|SP_ATTACK\|1`） / 充电电池（`ELECTRIC\|ATTACK\|1`） / 光苔（`WATER\|SP_DEFENSE\|1`） / 雪球（`ICE\|ATTACK\|1`） |

**天气免疫类（1 种 / 1 件，v1.18 新增）**

| 效果类型 | param 约定 | 装备 |
|------|------|------|
| `UTILITY_UMBRELLA` | 忽略（可留空） | 万能伞（无参数） |

**树果（4 种 / 30 件，均为一次性消耗品，触发即从携带栏移除）**

| 效果类型 | param 约定 | 树果 |
|------|------|------|
| `CURE_STATUS` | `状态英文名[\|状态英文名…]`；`ALL` = 全部异常（含混乱） | 樱子果（`PARALYSIS`） / 桃桃果（`POISON\|BADLY_POISON`） / 莓莓果（`BURN`） / 零余果（`SLEEP`） / 利木果（`FREEZE`） / 柿仔果（`CONFUSION`） / 木子果（`ALL`） |
| `HEAL_HP` | `阈值比例\|回复量`（回复量 < 1 视为最大 HP 比例，≥ 1 视为固定点数） | 橙橙果（`0.5\|10`） / 文柚果（`0.5\|0.25`） / 勿花果·异奇果·乐芭果·芭亚果（`0.25\|0.125`） |
| `HEAL_PP` | 回复点数 | 苹野果（`10`） |
| `RESIST_TYPE` | `属性\|承伤倍率[\|ALWAYS]` | 巧可果（`FIRE\|0.5`） / 千香果（`WATER`） / 烛木果（`ELECTRIC`） / 罗子果（`GRASS`） / 番荔果（`ICE`） / 莲蒲果（`FIGHTING`） / 通通果（`POISON`） / 腰木果（`GROUND`） / 扁樱果（`FLYING`） / 霹霹果（`PSYCHIC`） / 莓榴果（`BUG`） / 草蚕果（`ROCK`） / 佛柑果（`GHOST`） / 刺耳果（`DARK`） / 洛玫果（`FAIRY`）—— 均 `\|0.5`；灯浆果（`NORMAL\|0.5\|ALWAYS`，一般属性无克制关系故需 `ALWAYS`） |

> **需求方排除项**：「努力值降低」类 6 种树果（榴石果 / 藻根果 / 比巴果 / 哈密果 / 萄葡果 / 茄番果）
> 经需求方明确要求**不纳入**（本作无努力值系统）。

### 15.3 效果结算点

| 效果 | 结算位置 | 规则 |
|------|------|------|
| `FIRST_STRIKE` | `firstMover` | 携带者按 `chanceParam()` 概率无视速度先手；**双方均触发时回退到速度判定**；日志「XX 的先制之爪抢先行动！」 |
| `MOVE_LAST` | `firstMover` | 在速度比较**之前**判定；**仅一方携带**时携带方后出手（双方都带则回退速度）；日志「XX 因后攻之尾而最后行动！」 |
| `SPEED_MULTIPLIER` | `Pokemon#effectiveSpeed()` | 实际速度乘 `doubleParam()`；先后手与所有速度比较统一走该入口 |
| 地面化（`SPEED_MULTIPLIER` 第 2 段为 `GROUND`） | `typeEffectiveness` | 属性免疫（单属性 0 倍）遇地面系招式时抬到 1.0（黑色铁球）。**近似实现**：原作是「去掉飞行属性的免疫」，本作按「0 倍抬到 1 倍」处理，对双属性计算等价 |
| `IGNORE_IMMUNITY` | `performAttack`（判定在**防守方**） | 0 倍（无效）招式抬到 1.0，随后照常结算（标靶） |
| `GROUND_IMMUNE` | `performAttack` | 地面系招式直接判定为「没有命中」并**消耗**装备（气球）；非地面系不触发 |
| `DAMAGE_TYPE` | `computeDamage` | 攻击方携带且招式属性与 `typeParam()` 一致时乘 `doubleParam()` |
| `SUPER_EFFECTIVE` | `computeDamage` | 攻击方携带且克制（effectiveness > 1.0）时乘 `doubleParam()` |
| `PHYSICAL_DAMAGE` / `SPECIAL_DAMAGE` | `computeDamage` | 按招式类别（`MoveCategory`）匹配时乘倍率 |
| `LIFE_ORB` | `computeDamage` + `performAttack` | 无条件乘 `damageMultiplier()`；命中并造成伤害后按 `recoilRatio()` × 最大 HP 反伤（至少 1 点），日志「XX 因生命宝珠 失去了 N HP！」 |
| `CHOICE` | `computeDamage` / `effectiveSpeed` / `useMove` | `SPECIAL\|1.5` 时特殊招式伤害 ×1.5；`SPEED\|1.5` 时实际速度 ×1.5；两者都把携带者锁在**本场第一个使用过的招式**上 |
| `EVOLITE` | `computeDamage` | 防守方携带且仍有进化目标（`getEvolvesToId() != null`）时，物防/特防乘 `doubleParam()` |
| `LIFE_STEAL` | `performAttack` | 攻击方携带且实际造成伤害 > 0 时，回复 `伤害 × doubleParam()`（截断，贝壳之铃 1/8） |
| `END_TURN_HEAL` | `applyFieldEndEffects` | 双方携带者未倒下时回复 `maxHp × doubleParam()`（截断，至少 1） |
| `POISON_HEAL` | `applyFieldEndEffects` | 毒属性回复 `maxHp × healRatio()`；非毒属性扣 `maxHp × damageRatio()`（黑色污泥 1/16 ↔ 1/8） |
| `END_TURN_STATUS` | `applyFieldEndEffects` | 尚无主要异常时陷入 `statusParam()` 指定的异常（火焰宝珠灼伤 / 剧毒宝珠剧毒） |
| `WEATHER_DURATION` | `setWeather` | 携带者开启的天气与 `weatherParam()` 一致且回合数更大时，持续回合改为 `durationTurns()`（四岩石 8 回合） |
| `FOCUS_SASH` | `performAttack` → `survivedByHeldItem` | 满 HP 时受致死伤害保留 1 HP，**触发后消耗** |
| `FOCUS_BAND` | `performAttack` → `survivedByHeldItem` | 按 `chanceParam()` 概率保留 1 HP，不消耗 |
| `CURE_STATUS` | `tryInflict`（`tryApplyStatus` **成功之后**） | 刚被招式施加的异常在 `cureStatuses()` 内时立即治愈并**消耗**；`CONFUSION` 走 `clearConfusion()`（`cureStatus()` 只管主要异常） |
| `HEAL_HP` | `performAttack` 末尾 **+** `applyFieldEndEffects` 末尾 | HP 不高于 `阈值比例 × maxHp` 时回复（回复量 < 1 按最大 HP 比例、≥ 1 按固定点数）并**消耗**；未倒下、回复量 > 0 才触发（满 HP 不白白消耗） |
| `HEAL_PP` | `useMove`（招式槽 `exhausted()`）/ `foeTurn`（`pickFoeMove()` 返回 null） | 指定招式或第一个已耗尽的招式回补 `ppRestoreAmount()` 后**消耗**；玩家侧补完 PP 会**重试出手**（补不上仍按 PP 不足放弃本回合） |
| `RESIST_TYPE` | `performAttack`（`computeDamage` 之后、保命判定**之前**） | 招式属性与 `resistTypeParam()` 一致且（默认）效果拔群时，伤害 `floor(伤害 × resistMultiplier())`（至少 1）并**消耗**；param 第 3 段为 `ALWAYS` 时**不看克制关系**（灯浆果对一般属性招式） |
| `CRIT_BOOST` | `performAttack` → `critStage` / `rollCritical` | 会心等级取 `doubleParam()`（0 级 **1/24**、1 级 **1/8**、2 级 **1/2**、≥ **3** 级**必定**会心，常量 `CRIT_DENOMINATORS = {24, 8, 2}` / `CRIT_ALWAYS_STAGE = 3`）；会心时伤害 × `CRIT_MULTIPLIER`（1.5）并在日志追加「**，击中要害！**」 |
| `PUNCH_BOOST` | `computeDamage` **+** `applyContactPunish` | 拳类招式（`MoveFlag.PUNCH`）威力乘 `doubleParam()`；且携带者使出拳类招式时**该招式不再视为接触**，从而不触发对手的凸凸头盔反伤 |
| `CONTACT_PUNISH` | `performAttack`（伤害结算后、异常 / 畏缩之前） | 携带者被**接触类**招式（`MoveFlag.CONTACT`）命中时，攻击方扣除 `max(1, maxHp × doubleParam())`；**不消耗**装备、**不受攻击方保命装备保护**、**可击倒攻击方**（会照常播报倒下） |
| `FLINCH_CHANCE` | `performAttack` → `applyFlinchItem` | **造成伤害且目标未倒下**后按 `chanceParam()` 概率使目标 `畏缩`（日志「XX 因 YY 的 ZZ 畏缩了！」）；**变化类招式不触发** |
| `CONSECUTIVE_BOOST` | `computeDamage` → `consecutiveMoveMultiplier` | 连续使用**同一招式**时每层乘 `1 + param × (连续次数 − 1)`，上限 `CONSECUTIVE_MAX_MULTIPLIER`（**2.0**）；**换招即归零重算**；连续使用记录是本场战斗内存态（键为精灵 `uuid`，不落存档） |
| `POWDER_IMMUNE` | `executeMove` **入口** + `weatherChip` | 粉末类招式（`MoveFlag.POWDER`）对携带者**直接落空**并播报「`XX 借助防尘护目镜，粉末类招式没有命中！`」，**变化类的催眠粉 / 毒粉同样被拦截**；沙暴 / 冰雹的回合末削血也免疫（播报「`XX 借助防尘护目镜，不受沙暴影响！`」） |
| `ASSAULT_VEST` | `computeDamage`（特防）+ `useMove` **入口** / `pickFoeMove`（禁变化招） | ①**仅特殊招式**伤害计算时把**防守方**的特防乘 `doubleParam()`（物理招式不受影响）；②携带者**不能使用变化类招式**：`assaultVestAllows()` 在 `useMove` 与 `choiceAllows` 相邻处判定（**不消耗 PP**、不走回合推进），播报「`XX 因突击背心的效果，无法使用变化招式！`」；③`pickFoeMove()` 为携带该装备的敌方**过滤掉变化招式槽位** |
| `CLEAR_AMULET` | `applyStatChange`（唯一漏斗） | 携带者被**对手造成**的能力**下降**（`delta < 0 && source != null && source != target`）被拦截并播报「`XX 借助清净坠饰，能力没有被降低！`」；**不消耗**；自身招式造成的下降（如近身战自降）与自身提升一律不受影响 |
| `WEAKNESS_POLICY` | `performAttack`（克制判定后） | 携带者被**效果拔群**招式命中（`effectiveness > 1.0`）后物攻与特攻**各提升 `statLevelsParam()` 级**并**消耗**（弱点保险 +2/+2）；非效果拔群不触发 |
| `BLUNDER_POLICY` | `executeMove` **未命中分支** | 携带者招式**未命中**时速度提升 `statLevelsParam()` 级并**消耗**（打空保险 +2）；命中时不触发 |
| `THROAT_SPRAY` | `applyStatusMove` / `performAttack`（伤害结算后） | 携带者使用**声音类**招式（`MoveFlag.SOUND`）后特攻提升 `statLevelsParam()` 级并**消耗**（爽喉喷雾 +1）；**变化类与攻击类两条路径都判定**；非声音类不触发 |
| `TERRAIN_SEED` | `setTerrain` → `applyTerrainSeeds()` **+** `switchActive` → `applyTerrainSeed()` | 携带者所处场地与 `seedTerrainParam()` 一致时，按 `statParam()` 提升 `statLevels()` 级并**消耗**（四种子均 +1 级对应防御）；**已开启场地时换人上场也立即触发** |
| `TYPE_REACTION` | `applyStatusMove` / `performAttack`（伤害结算前） | 招式属性与 `reactionTypeParam()` 一致时按 `statParam()` 提升 `statLevels()` 级并**消耗**（球根 / 充电电池 / 光苔 / 雪球 +1 级）；属性不匹配不触发 |
| `UTILITY_UMBRELLA` | `computeDamage` + `weatherChip` | ①**天气**的招式威力修正（`weather.moveTypeMultiplier`）对携带者**完全失效**（晴天火 1.5 / 雨天水 1.5 均被取消），**场地修正不受影响**；②沙暴 / 冰雹的回合末削血免疫（与 `POWDER_IMMUNE` 共用「`XX 不受沙暴影响！`」播报）；**不消耗** |

回合末结算顺序（`applyFieldEndEffects`）：
`天气削血` → `青草场地回血` → `剩饭` → `黑色污泥` → `异常状态` → `宝珠` → **`HP 回复树果`**。
宝珠排在异常状态之后，因此**刚陷入灼伤/剧毒的当回合不会被该异常扣血**；
HP 回复树果排在**最后**，因此回合末的天气/异常扣血也能触发果实回复。
`天气削血` 一栏中携带防尘护目镜的精灵**不参与扣血**（v1.17）。

**畏缩的清除时机**（v1.17）：`finishRound()` 在 `applyFieldEndEffects` 之后对双方调用
`clearFlinch()`，`switchActive()` 换人时也清除。因此畏缩**只在本回合内有效**——只有「目标本回合
尚未行动」时才会实际生效（与正作一致）；`canAct()` 命中畏缩时播报「XX 畏缩了，无法行动！」并跳过本回合。

> **树果三处语义决策（实现方自主决定，见 `QA问题表.md` 第 26~28 条）**：
> 1. 异常治疗树果**只在「刚被招式施加异常」时触发**——换人上场时已带异常、战斗开始时已带异常、
>    宝珠/天气等其它施加路径都**不会**触发。
> 2. **木子果（`ALL`）包含混乱**，按需求表「治疗所有异常状态」执行（原作木子果只治主要异常）。
> 3. 6 种危果树果的「**性格不喜味道会混乱**」副作用**未实现**——本作无性格/口味系统。

> **批次④七处语义决策（实现方自主决定，见 `QA问题表.md` 第 30~36 条）**：
> 1. **会心与畏缩只在「招式命中并造成伤害」后判定**——变化类招式、0 倍（无效）招式都不参与；
>    会心判定先于 `computeDamage`（每次攻击恰好消耗一次 `random.nextInt(分母)`）。
> 2. **凸凸头盔反伤不消耗装备、不受保命装备保护、可击倒攻击方**——反伤打在攻击方身上，
>    气势披带 / 气势头带只对「被招式命中」的伤害生效。
> 3. **畏缩只在「目标本回合尚未行动」时实际生效**——回合末与换人时清除，与正作一致。
> 4. **`POWDER_IMMUNE` 同时免疫沙暴 / 冰雹回合末伤害**——与粉末招式免疫同源（`HeldItemEffect`
>    javadoc 自批次④ 起即如此声明，本版补齐实现）。
> 5. **粉末免疫判定放在 `executeMove` 入口而非伤害路径**——催眠粉 / 毒粉是变化类招式，
>    `executeMove` 会先走 `tryInflict` 分支，若只在 `performAttack` 里判定会**永远漏掉**它们。
> 6. **拳类招式同时具备 `CONTACT|PUNCH` 两个标记**——`moves.csv` 按此标注，因此拳类招式默认
>    仍会触发凸凸头盔反伤，**只有攻击方携带拳击手套时**才豁免。
> 7. **节拍器用装备内联乘算而非能力等级**——与讲究系（§15.3「讲究系的锁招状态不进模型」及
>    `QA问题表.md` 第 24 条）同一处理方式，批次③ 能力等级系统落地后需决定是否迁移。

> 装备倍率参与公式：`floor(基础 × STAB × 克制 × 天气场地 × 装备 × 随机浮动)`，
> 与天气/场地加成同层级相乘。

> **保命范围**：`FOCUS_SASH` / `FOCUS_BAND` 只在「**被招式命中且会倒下**」时判定
> （`survivedByHeldItem` 仅在 `performAttack` 内调用）。天气削血、异常状态扣血、生命宝珠反伤
> **不会**被保命装备救回。

> **随机序列保护**：`survivedByHeldItem` 用短路求值，只有 `FOCUS_BAND` 才消耗随机数，
> 避免「携带气势披带」改变后续伤害浮动的随机序列。

> **讲究系锁招的死锁规避**：本作**没有「挣扎」机制**，若锁定招式 PP 耗尽将无法出招。
> `choiceAllows()` 因此在锁定招式 `exhausted()` 时**自动解禁**（允许换招），属实现方自主决策，
> 见 `QA问题表.md` 第 24 条。

> **批次⑤九处语义决策（实现方自主决定，见 `QA问题表.md` 第 37~45 条）**：
> 1. **能力等级封顶 ±6**，`changeStatStage` 返回**实际**变化量（越界时可能小于请求量，甚至为 0），
>    日志按实际量播报「`XX 的攻击提高了 N 级！`」/「`已经无法再提高了！`」。
> 2. **`CLEAR_AMULET` 只拦「对手造成」的下降**——自身招式自降能力（近身战等）、自身强化不受影响。
> 3. **`ASSAULT_VEST` 的禁招放在 `useMove` 入口且不消耗 PP**，与 `choiceAllows` 同一位置，
>    因此**不推进回合**；同时让敌方 AI（`pickFoeMove`）直接不选变化招，避免 AI 每回合空过。
> 4. **`ASSAULT_VEST` 的特防加成只在特殊招式路径生效**，物理招式与 `selfHit`（混乱自伤）都不参与。
> 5. **`UTILITY_UMBRELLA` 只抵消「天气」倍率，不抵消「场地」倍率**——原作伞只针对天气。
> 6. **`UTILITY_UMBRELLA` 的天气削血免疫与 `POWDER_IMMUNE` 合并判定**（共用播报与分支），
>    两件装备在天气免疫上的语义完全一致。
> 7. **场地种子在「场地开启」与「换人上场」两处触发**——`setTerrain` 对**双方出战精灵**判定，
>    `switchActive` 对**换上的精灵**判定，避免「场地先开、种子后上场」永不触发。
> 8. **`WEAKNESS_POLICY` 只在 `effectiveness > 1.0` 时触发**（`IGNORE_IMMUNITY` 抬到 1.0 不算克制），
>    与 `SUPER_EFFECTIVE` 的判定口径一致。
> 9. **命中判定不改变既有随机序列**——`accuracy < 0`（必中）与 `accuracy ≥ 100` 直接返回命中、
>    **不消耗随机数**，以免给既有招式凭空多出一次掷骰而打乱伤害浮动；`accuracy == 0` 仍正常掷骰
>    （永不命中）。这条约定是「加命中系统却零回归」的关键。

> **9 件未能实现的装备（需求方条目，本版不做，见 `QA问题表.md` 第 46 条）**：
> 紧缠勾爪（需「无法逃走」状态）、机变骰子（需多段攻击段数）、逃脱按键 / 避难背包 / 红牌
> （需战斗中强制换人链路）、美丽空壳（同上）、厚底靴（需入场危害地形系统）、红线（需「能力下降时
> 反弹给对手」）、特性护具（需特性替换机制）。以上均依赖本作尚未建立的大系统，**不是数据遗漏**。

### 15.3.1 `executeMove` 结算顺序（v1.18）

```
粉末免疫gate → 命中判定(rollHit) → 未命中(播报 + 打空保险) →
  ├─ 变化类(STATUS) → applyStatusMove（场地/异常/能力变化 → 爽喉喷雾 → 属性反应 → hit 事件）
  └─ 攻击类          → performAttack（克制/会心 → 伤害 → 保命 → 吸血 →
                                   弱点保险 → 爽喉喷雾 → 属性反应 → 树果回复）
```

命中率语义（`Move` 构造器统一归一化，`rollHit` 判定）：

| `accuracy` | 语义 | 是否消耗随机数 |
|------|------|------|
| `< 0` | **必中**（数据用 `-1`） | **否** |
| `0` | 永不命中 | 是（必定失败） |
| `1 ~ 99` | 按百分比掷骰 | 是 |
| `≥ 100` | 必中（构造器截断到 100） | **否** |

`applyStatChange(Pokemon target, StatModifier stat, int delta, Pokemon source)` 是**能力等级的唯一漏斗**：
跳过 `null` / 已倒下 / `StatModifier.HP` / `delta == 0`；`delta < 0` 且 `source != null && source != target`
时先过 `CLEAR_AMULET` 守卫；随后按实际变化量播报。

### 15.4 装备与存档（v1.15 新增）

装备**此前完全不落存档**——`org.example.save` 的读写路径没有任何装备字段，肉鸽奖励发放的
装备读档后全部消失。v1.15 起：
| 环节 | 变更 |
|------|------|
| 文件格式 | `PP` 行末新增第 16 段（index 15）`heldItemId`，空串表示未携带 |
| `SaveData.PokemonData` | 新增 `heldItemId` 字段；compact 构造器把 `null` 归一为空串 |
| `SaveCodec` | `render` 写出该段（`escape` 转义）；`parse` 用 `field(parts, 15)` 读取 |
| `PokemonMapper` | `toData` 取 `getHeldItem().getId()`；`toPokemon` 经 `applyHeldItem` 按 id 查注册表还原 |
| `FORMAT_VERSION` | **保持 2**。`field(parts, index)` 是 `index < parts.size() ? parts.get(index) : ""`，旧档 `PP` 行只有 15 段（index 0~14），`field(parts, 15)` 自然返回空串 → 未携带，**天然向前兼容**，无需升版本 |

其他两处同版修复：

1. **`PokemonData` 的「省略装备」便捷构造器已删除**。它会被 `SaveCodec` 的
   「补齐技能后重建队伍」路径（`parse` 末尾 `partyWithMoves`）误用，**静默丢弃装备**——
   表现是写出的文本里有装备、读回来却没有。删掉后该路径必须显式传 `p.heldItemId()`，
   由编译器兜住同类遗漏。
2. **`HeldItem#doubleParam()` 对多段参数解析失败返回 1.0**，使黑色铁球
   （`0.5\|GROUND`）的速度减半静默失效。现改为「整体解析失败则退化取首段」。

### 15.5 测试覆盖

`src/test/java/org/example/battle/BattleEngineHeldItemTest.java`（**89** 个用例）覆盖：
- 属性强化装备增伤 20%、属性不匹配无效、达人带克制增伤/非克制不变、剩饭回合末回血 1/16、
  贝壳之铃吸血 1/8、先制之爪触发/不触发/双方同时触发回退速度、进化辉石减伤 1.5 倍/最终进化无效；
- v1.15 新增：力量头带/博识眼镜按类别增伤、生命宝珠增伤与反伤、黑色铁球减速与地面化、
  后攻之尾后出手、标靶解除免疫、气球免疫地面并消耗、黑色污泥双向结算、火焰/剧毒宝珠回合末上异常、
  四岩石延长天气至 8 回合、气势披带保留 1 HP 并消耗、气势头带概率保命、讲究眼镜增伤与锁招、
  讲究围巾加速与锁招；
- v1.16 新增（14 例树果）：异常治疗树果治愈刚陷入的异常并消耗 / 只对参数所列异常生效（未触发不消耗）、
  木子果治愈全部异常含混乱、HP 回复树果受击后立即回复 / 回合末结算 / 满 HP 不触发、
  文柚果回复最大 HP 1/4、危果树果仅在 HP 低于 1/4 时触发、苹野果在 PP 耗尽时回补并消耗 /
  PP 未耗尽不触发、属性减伤树果使效果拔群招式伤害减半并消耗 / 属性不匹配不触发 /
  非效果拔群不触发、灯浆果对一般属性招式无条件减伤；
- v1.17 新增（14 例招式标记 / 会心 / 畏缩）：拳击手套对拳类招式增伤 / 非拳类不变、
  凸凸头盔在接触招式命中后反伤攻击方 1/6 且不消耗、防尘护目镜免疫催眠粉（变化类）与
  沙暴回合末削血、锐利之爪提升会心等级并稳定触发会心伤害、
  王者之证在造成伤害后按 10% 概率使目标畏缩（含触发与不触发两组种子）、
  节拍器连用同一招式逐层增伤 / 换招归零 / 上限 2 倍；
- v1.18 新增（29 例）：**招式能力等级变化**——叫声降目标物攻 1 级 / 摇尾巴降目标物防 1 级 /
  硬邦邦·高速移动作用于自身 / 能力等级真实影响后续伤害 / 等级不越过 ±6（含「无法再提高」播报）；
  **命中判定**——命中率 0 的招式永不造成伤害 / 必中招式不受命中判定影响；
  **能力等级联动装备**——讲究头带增伤 1.5 倍 / 对特殊招式无效、突击背心降低特殊招式伤害且对
  物理招式无效 / 禁止携带者使用变化招式（不消耗 PP）/ 使敌方 AI 放弃变化招式、清净坠饰免疫
  对手造成的能力下降且不影响自身的下降、弱点保险被效果拔群命中后双攻 +2 并消耗 / 非效果拔群不触发、
  打空保险未命中后速度 +2 并消耗 / 命中时不触发、爽喉喷雾使用声音招后特攻 +1 并消耗 /
  非声音招不触发 / 攻击招路径同样生效、属性反应装备受对应属性招式后提升能力并消耗 / 属性不匹配不触发；
  **场地种子与万能伞**——场地匹配时提升能力并消耗 / 场地不匹配不触发 / 换上的精灵在场地已开启时
  立即触发、万能伞取消晴天火属性增伤 / 免疫沙暴回合末伤害。
  > **测试基座**：本类所有精灵一律用固定个体值（`FIXED_IVS = new Stats(31,31,31,31,31,31)`）创建。
  > `Pokemon.create(species, level, movePool)` 每次都会调用 `Stats.randomIv()`，会让「有装备」与
  > 「无装备」两组的面板（HP/攻/防/速）都不同，伤害比、回血量、最大 HP 比例全部不可比——
  > 这正是 dev 基线上 6 例既有失败的真实根因（此前误判为「随机伤害浮动」）。
  > **随机种子注意**：`java.util.Random` 小种子的首个 `next(31)` 高位近似线性，直接
  > `new Random(i)` 循环会让 `nextInt(8)` 退化为常数。概率类断言（会心 1/24、畏缩 10%）
  > 必须先打散种子（`new Random(i * 0x9E3779B97F4A7C15L + 0x9E3779B97F4A7C15L).nextLong()`），
  > 或改用「遍历种子找到会触发的那一个」的确定性写法。

`src/test/java/org/example/model/HeldItemTest.java`（**16** 个用例）覆盖 equipment.csv 加载（77 件全部可查询）、
多段参数解析（含 `doubleParam()` 的首段退化）、**v1.16 新增树果参数解析**（4 种效果类型与参数、
`cureStatuses()` 的单项/多项/`ALL`/非法名跳过/非本类型返回空、`healThresholdRatio`/`healAmount`/
`ppRestoreAmount`/`resistTypeParam`/`resistMultiplier`/`resistUnconditional` 的越界与坏值回退）、
**v1.17 新增批次④ 6 件装备的效果类型与参数**（`PUNCH_BOOST 1.1` / `CONTACT_PUNISH 0.1667` /
`POWDER_IMMUNE` 空参数 / `CRIT_BOOST 1` / `FLINCH_CHANCE 10` / `CONSECUTIVE_BOOST 0.2`）、
**v1.18 新增批次⑤ 15 件装备的效果类型与参数**（讲究头带 `ATTACK\|1.5`、突击背心 `1.5`、
`CLEAR_AMULET` 与 `UTILITY_UMBRELLA` 空参数、三件能力提升装备的等级读取、
四种子的 `场地\|能力项\|等级`、四件属性反应的 `属性\|能力项\|等级`）与
`StatModifier.parse()` 的可解析 / 不可解析 / `null` 分支，
装备库去重/换穿自动脱下等模型行为。

`src/test/java/org/example/model/MoveFlagTest.java`（**11** 个用例，v1.17 起）覆盖标记本身：
`parseFlags` 的 `\|` 分隔多标记 / 单标记、前后空白与大小写不敏感（`powder` = `POWDER`）、
`null` 与空串与 `"||"` 返回空集、无法识别片段被忽略（含「前缀相同的错误名称」不被误匹配）、
重复标记去重、返回集合不可变、**v1.18 新增 `SOUND` 标记（单独 / 与其它标记共存 / 大小写不敏感）**；
以及 `Move` 的标记 API：旧 11 参构造器等价于无标记、
12 参构造器写入标记且 `hasFlag()/isContact()/isPunch()/isPowder()` 一致、
`null`/空集归一为空集、对传入的可变集合做防御性拷贝。

`src/test/java/org/example/model/MoveStatChangeTest.java`（**8** 个用例，v1.18 新增）覆盖
`MoveStatChange.parse`：单段作用于目标 / 单段 `SELF` 作用于自身 / 多段保持原文顺序 /
大小写与空白容错并接受显式 `+` 号 / `null` 与空串与空白串返回空列表 /
无法识别片段被静默跳过（缺分隔符、未知能力项、非法数值、`0`、`HP`）/
部分脏数据只丢弃脏片段 / 返回列表不可变。

`src/test/java/org/example/model/PokemonStatStageTest.java`（**7 个用例，v1.18 新增**）覆盖
`Pokemon` 能力等级：初始等级全为 0（含 `HP` 与 `null` 的兜底）、`stageMultiplier` 的
±1/±2/±5/±6 分数表与越界截断、`changeStatStage` 返回实际变化量、正负六截断（含已到上限/下限时为 0）、
能力等级影响 `effective*` 五个面板、`clearStatStages` 恢复原始面板、实际面板最低为 1。

`src/test/java/org/example/save/PokemonMapperTest.java`（**13** 个用例）与
`SaveCodecTest`（**19** 个用例）v1.15 新增装备落存档回归：装备随存档往返（含 effectType 与 param）、
未携带读档后仍为空、未知/已下架装备被忽略、装备 id 随文本往返、旧档缺少装备字段仍可读。
**树果复用同一通路**（`heldItemId` 存档 + `applyHeldItem` 查注册表还原），无需额外用例。
**v1.18 的能力等级与招式数据均为「本场战斗内存态 / 只读数据」**，不落存档，无需额外用例。

> 回归基线：`mvn -o -B test` → **454** 个用例，仅 1 例失败且**为改动前既有**
> （`org.example.model.NodeGeneratorTest` 1 例；`com.bao01.save.SaveSystemTest` 亦是既有偶发失败，
> 本版全量跑未复现），2 例跳过。
> v1.17 基线为 407 个用例（2 例既有失败），批次⑤ 新增 47 例
> （+29 引擎 / +2 装备库 / +1 标记 / +8 变化解析 / +7 能力等级），**无新增失败**。
> `SaveSystemTest` 的偶发性根因见 `QA问题表.md` 第 25 条。

### 15.6 招式数据列 `flags` / `statChanges`（v1.17 起，v1.18 扩至 13 列）

`data/moves.csv` 表头由 11 列先后扩为 12 列（v1.17 的 `flags`）与 **13 列**（v1.18 的 `statChanges`）：

```
# id,name,type,category,power,accuracy,maxPp,priority,inflicts,inflictionChance,effect,flags,statChanges
tackle,撞击,NORMAL,PHYSICAL,40,100,35,0,,,,CONTACT,
growl,叫声,NORMAL,STATUS,0,100,40,0,,,,SOUND,ATTACK|-1
tail-whip,摇尾巴,NORMAL,STATUS,0,100,30,0,,,,,DEFENSE|-1
harden,硬邦邦,NORMAL,STATUS,0,-1,30,0,,,,,DEFENSE|1|SELF
agility,高速移动,PSYCHIC,STATUS,0,-1,30,0,,,,,SPEED|2|SELF
sleep-powder,催眠粉,GRASS,STATUS,0,75,15,0,SLEEP,100,,POWDER,
ice-beam,冰冻光束,ICE,SPECIAL,90,100,10,0,FREEZE,10,,,
```

| 环节 | 实现 |
|------|------|
| 解析 `flags` | `GameData#applyMoveRow`：`Set<MoveFlag> flags = c.length > 11 ? MoveFlag.parseFlags(c[11]) : Set.of();` |
| 解析 `statChanges` | `List<MoveStatChange> changes = c.length > 12 ? MoveStatChange.parse(c[12]) : List.of();` |
| 容错 | **列数不足时按「无标记 / 无变化」处理**（`c.length > 11` / `c.length > 12` 判断），因此 v1.16 的 11 列旧表与 v1.17 的 12 列表**都仍能加载**；标记名或能力项写错只丢该条（解析器忽略无法识别的片段） |
| 数据行数 | 仍为 **36** 条（只是多了两列），**没有任何招式因为标记或能力变化而增删** |
| 与其它列的关系 | `effect` / `flags` / `statChanges` 三列**没有占位耦合**：都留空时该行结尾是连续的 `,,,`；`effect` 是变化招的天气·场地效果，与能力变化互不影响 |

**`flags` 当前标注（36 条招式中 13 条有标记）**：

| 标记 | 条数 | 招式 |
|------|------|------|
| `CONTACT` | 9 | 撞击 / 抓 / 电光一闪 / 怪力 / 泰山压顶 / 藤鞭 / 踢倒 + 火焰拳 / 暗影拳 / 冰冻拳（3 个拳类） |
| `PUNCH` | 3 | 火焰拳 / 暗影拳 / 冰冻拳 |
| `POWDER` | 2 | 催眠粉 / 毒粉 |
| `SOUND` | 1 | 叫声（v1.18 新增） |

**`statChanges` 当前标注（36 条招式中 4 条有变化）**：

| 招式 | 标注 | 含义 |
|------|------|------|
| 叫声（`growl`） | `ATTACK\|-1` | 目标物攻 −1 级（同时是唯一的声音类招式） |
| 摇尾巴（`tail-whip`） | `DEFENSE\|-1` | 目标物防 −1 级 |
| 硬邦邦（`harden`） | `DEFENSE\|1\|SELF` | **自身**物防 +1 级 |
| 高速移动（`agility`） | `SPEED\|2\|SELF` | **自身**速度 +2 级 |

> **命中率数据约定（v1.18）**：`accuracy` 用 **`-1` 表示必中**（硬邦邦 / 守住 / 睡觉 / 高速移动 /
> 暗影拳等），与 §8 的 `Move` 契约一致。`Move` 构造器统一归一化：
> `accuracy < 0 → -1`、`accuracy > 100 → 100`。本版只是**明确了既有 `-1` 行的语义**并让引擎
> 真正按必中结算，**没有任何数据行的 accuracy 被改写**。

> **标注原则**：`CONTACT` = 攻击方以身体接触目标（撞击 / 踢击 / 啃咬 / 拳类）；`PUNCH` = 拳类招式
> （**同时必须带 `CONTACT`**）；`POWDER` = 粉末类招式（目前仅催眠粉 / 毒粉两个变化类招式，
> 「蘑菇孢子」尚未进表）；`SOUND` = 声音类招式（目前仅叫声，「吼叫」「唱歌」尚未进表）。
> **未标注的招式一律无标记**，不触发任何相关装备。

## 16. 战斗演出事件：`BattleEvent`（v1.11）

### 16.1 定位

`BattleEvent` 是战斗引擎产出的**结构化演出数据**，与 `getLog()` 的**文本**日志互补：

| 通道 | 形态 | 用途 | 谁用 |
|------|------|------|------|
| `getLog()` | `List<String>` 文本行 | 给人读的战斗播报 | 日志面板、控制台演示 |
| `drainEvents()` | `List<BattleEvent>` 结构数据 | 驱动动画：谁、对谁、什么属性/分类的招式 | 战斗界面（`BattleView`） |

日志只说「发生了什么」，无法结构化解读；事件携带阵营、精灵名、招式名、属性、分类与成败标志，
界面据此选择动画形态与配色（如火系特殊招式 → 橙色发光球，变化招式 → 空心脉冲环）。

> **重点是「不参与结算」**：事件是纯演出旁路。忽略全部事件的调用方（例如控制台演示
> `org.example.demo.BattleConsole`）行为与本特性引入前**完全一致**；事件不改变伤害、命中、
> 捕捉、逃跑等任何判定结果。

### 16.2 契约

```java
public record BattleEvent(Kind kind, Side side, String actor, String moveName,
                          ElementType element, MoveCategory category, boolean success)
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `kind` | `Kind` | 事件类型（见 16.3） |
| `side` | `Side` | 事件主角阵营；`BATTLE_START` 为 `null`（表示双方） |
| `actor` | `String` | 主角名：精灵名；`CAPTURE` 为球名、`ITEM` 为道具名 |
| `moveName` | `String` | 招式名；非 `MOVE` 事件为空串 |
| `element` | `ElementType` | 招式属性（决定动画配色）；无数据时为 `null` |
| `category` | `MoveCategory` | 招式分类（决定动画形态）；无数据时为 `null` |
| `success` | `boolean` | 该事件是否成功（投球是否捕捉成功、逃跑是否成功）；其余事件恒为 `true` |

嵌套枚举：

| 枚举 | 常量 |
|------|------|
| `BattleEvent.Kind` | `BATTLE_START` / `SEND_OUT` / `RECALL` / `MOVE` / `HIT` / `FAINT` / `CAPTURE` / `ITEM` / `RUN` |
| `BattleEvent.Side` | `PLAYER`（玩家一方）/ `FOE`（对手一方） |

实例方法：`Side opponent()` —— 返回与本事件阵营相对的另一方（`BATTLE_START` 返回 `null`）。

常量：`BattleEvent.STRUGGLE_NAME`（`"挣扎"`）—— PP 耗尽后无招式攻击时 `MOVE` 事件携带的招式名。

### 16.3 事件类型与产生时机

| `Kind` | 对应动画 | 产生时机 | `side` |
|--------|----------|----------|--------|
| `BATTLE_START` | 进场动画 | 引擎构造完成时（每个战斗实例恰好一次） | `null` |
| `SEND_OUT` | 放出动画 | 玩家主动换宠、倒下后自动换宠、训练师派出下一只 | 派出方 |
| `RECALL` | 收回动画 | 玩家主动换宠（先收回再放出） | `PLAYER` |
| `MOVE` | 技能释放（施法） | 任一方成功使出招式（含变化招式与挣扎） | 施法方 |
| `HIT` | 受击（命中） | **实际造成伤害**时；变化招式命中目标时 | 被打方 |
| `FAINT` | 倒下 | 精灵 HP 归零（攻击、混乱自伤、回合末持续伤害、天气侵蚀） | 倒下方 |
| `CAPTURE` | 投球 | 玩家投出精灵球（野生遭遇） | `FOE` |
| `ITEM` | 回复特效 | 玩家使用回复 / 解除类道具且实际生效 | `PLAYER` |
| `RUN` | 逃跑 | 玩家尝试逃跑（仅野生遭遇） | `PLAYER` |

**不发事件的情形**（有意为之，避免"无效果也演一遍"的错位感）：

- 招式落空、被属性免疫、打出「没有效果……」时**不产生** `HIT`；
- PP 不足、目标非法、道具未实际生效等「本回合不行动」的情况不产生任何事件；
- 训练师轮战的逃跑 / 投球被拒绝时只追加日志，不产生事件。

**顺序保证**：事件严格按产生顺序排列，且与同回合日志行顺序一致。例如「我方一招击倒敌方，
训练师派出下一只」的事件序列为：

```
MOVE(PLAYER, ...) → HIT(FOE, ...) → FAINT(FOE, ...) → SEND_OUT(FOE, ...)
```

### 16.4 取走语义

`drainEvents()` 是**取走并清空**：返回自上次调用以来新产生的事件，队列随即清空，同一事件不会
重复投递；无新事件时返回空列表。

```java
engine.useMove(slot);                       // 1. 结算（事件入队）
List<BattleEvent> events = engine.drainEvents(); // 2. 取走事件
view.playEvents(events, () -> view.render());    // 3. 播动画 → 结束后再刷新界面
```

> **推荐顺序**：行动 → 取事件 → 播动画 → **动画结束后**再刷新界面。若先刷新界面再播动画，
> 玩家会看到 HP 条 / 立绘在动画开始前就变化，且引擎可能已在结算时自动换宠，导致倒下与放出
> 动画作用到错误的精灵上（事件自带精灵名，动画按名切图即可避免）。

### 16.5 测试覆盖

`src/test/java/org/example/battle/BattleEventTest.java`（**11** 个用例）覆盖：开场事件只投递一次
且取走即清空、伤害招式的 `MOVE`+`HIT` 及其属性/分类/阵营、一击必杀追加 `FAINT`、变化招式产生
`STATUS` 类 `HIT`、玩家主动换宠产生 `RECALL`+`SEND_OUT`、敌方与我方倒下后的自动派出
`SEND_OUT`、投球与逃跑的成败标志、训练师战逃跑不产生事件、回复道具产生 `ITEM` 事件。

界面侧的演出链路另由**冒烟测试** `src/test/java/org/example/view/BattleAnimationSmokeTest.java`
验证（默认跳过，需显式开启）：在真实 JavaFX 工具包 + 真实窗口布局下播放全部 14 种事件，断言
串联的 `setOnFinished` 不会断链、完成回调必被触发，且控制器在演出结束后会解锁行动输入
（不会把界面永久锁死）。运行方式：

```
mvn -o test -Dtest=BattleAnimationSmokeTest -Dbattle.smoke=true -DfailIfNoSpecifiedTests=false
```


