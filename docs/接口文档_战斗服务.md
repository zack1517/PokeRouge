# 战斗服务（BattleService）接口文档

> 版本：v1.3 · 日期：2026-09-08 · 模块：`org.example.battle`（PokeRouge）
>
> v1.1：新增天气与场地系统（状态查询 `getWeather()/getTerrain()`、变化类技能开启、
> 持续回合推进与回合末效果），详见 §7。
>
> v1.2：与「宝可梦、技能、队伍接口.md」对齐 `Move` 数据契约（新增 `priority` 先制度字段、
> `accuracy` 增加 -1=必中 语义），详见 §8。
>
> v1.3：战斗模型按「宝可梦、技能、队伍接口.md」v1.0 温和适配完成（`Nature`/IV/异常状态
> 等字段已就位），并新增 `BattleService` 配套契约类型；差异与取舍详见 §9 及
> 《适配说明_宝可梦技能队伍接口v1.md》。
>
> v1.4：新增**训练师轮战**（`newTrainerBattle`）：敌方持有一整支队伍，**某一方精灵全部倒下
> 才结束战斗**，不可逃跑、不可捕捉。新增查询 `getTrainer()`/`foeActive()`，详见 §3.4、§4。

## 1. 概述

本模块把「回合制战斗逻辑」从界面/控制器中独立出来，对外暴露一个稳定接口
`BattleService`，供主界面、战斗界面等调用方使用。调用方只依赖接口，具体结算规则由
实现类 `BattleEngine` 承担，实例统一由静态工厂 `BattleServices` 创建。

设计目标：

- 战斗规则（伤害、速度先后手、捕捉、逃跑、经验/升级/学招/进化、天气与场地）与 UI 完全解耦；
- 调用方不直接依赖具体实现类，便于测试注入随机源、后续替换实现；
- 以「每回合新增日志行」作为行动结果的统一反馈通道。

## 2. 模块结构

```
org.example.battle
├── BattleService.java   [接口] 战斗服务契约（本文档主体）
├── BattleServices.java  [工厂] 创建战斗实例 + 遭遇环境助手（静态）
└── BattleEngine.java    [实现] 回合制对战引擎（默认/唯一实现）
```

依赖的模型类型（均位于 `org.example.model`）：`Player`、`Pokemon`、`MoveSlot`、
`Item`、`Bag`、`Trainer`（训练师轮战的敌方队伍，见 §3.4），以及天气/场地系统相关枚举
`Weather`、`Terrain`、`MoveEffect`（见 §7）。

### 类关系

```
                     <<interface>>
                     BattleService
                     ┌──────────────────────────────┐
                     │ + Status (enum)              │
                     │ + useMove(slot) → List<String>│
                     │ + useItem(item) → List<String>│
                     │ + tryRun() → List<String>     │
                     │ + switchActive(idx) → List<String>│
                     │ + getPlayer/getWild/...       │
                     │ + getWeather() / getTerrain() │
                     └──────────────┬───────────────┘
                                    ▲ implements
                                    │
   BattleServices (factory)         │
   ┌───────────────────────┐   ┌────┴──────────────────┐
   │ newBattle(...)        ├──►│  BattleEngine          │
   │ newTrainerBattle(...) │   └───────────────────────┘
   │ wildLevelAround(...)  │
   │ randomWild(...)       │
   └───────────────────────┘
```

## 3. 接口：`BattleService`

全限定名：`org.example.battle.BattleService`

### 3.1 嵌套枚举 `Status`（战斗状态）

| 常量         | 含义                     | 触发场景                         |
|--------------|--------------------------|----------------------------------|
| `ONGOING`    | 进行中                   | 战斗尚未结束，可继续行动         |
| `PLAYER_WIN` | 玩家获胜                 | 野生精灵倒下（野生遭遇），或训练师整队精灵全部倒下（训练师轮战），结算经验/升级/进化 |
| `PLAYER_LOSE`| 玩家战败                 | 队伍全部倒下且无精灵可替换       |
| `FLED`       | 逃跑成功                 | `tryRun()` 成功（仅野生遭遇）   |
| `CAUGHT`     | 捕捉成功                 | 精灵球捕捉成功并加入玩家队伍（仅野生遭遇） |

### 3.2 回合行动方法

所有行动方法均**要求战斗进行中**（`status == ONGOING`），否则抛出
`IllegalStateException`。返回值为**本回合新产生**的日志行（非全量日志）。

#### `List<String> useMove(MoveSlot slot)`

玩家选择技能攻击。敌方（野生精灵，或训练师当前出战精灵）自动选择可用技能；双方都行动时
按速度决定先后手，速度相同则随机。

> 先制说明：`Move` 数据契约已含先制度 `priority`（越大越先出手，0 为普通技能），
> 当前引擎尚未消费该字段，先后手仍按速度判定；先制排序留待后续扩展。

| 项     | 说明 |
|--------|------|
| 参数   | `slot`：当前出战精灵的技能槽；`null` / PP 耗尽 / 非当前精灵的槽位会自动退回当前精灵第一个可用技能；完全无可用技能则本回合不行动 |
| 返回   | 本回合新增日志行 |
| 副作用 | 消耗 PP；**变化类技能**若携带天气/场地效果（`MoveEffect`）则改为开启/重置对应天气/场地（见 §7），不造成伤害；普通技能命中则计算伤害；若击倒野生精灵进入 `PLAYER_WIN`（训练师轮战则换下一只，整队全倒才进入 `PLAYER_WIN`），玩家精灵倒下自动换宠或进入 `PLAYER_LOSE` |

#### `List<String> useItem(Item item)`

使用道具。按道具类别分流：`HEAL` 回复当前精灵 HP；`POKE_BALL` 尝试捕捉野生精灵
（捕捉公式：血量越低、捕获率越高、球倍率越大越易成功，必定捕捉球直接成功）。

| 项     | 说明 |
|--------|------|
| 参数   | `item`：要使用的道具；`null` 或背包中数量不足则不行动 |
| 返回   | 本回合新增日志行 |
| 副作用 | 回复道具会消耗背包对应数量；精灵球使用后无论成败都会消耗；使用后若战斗未结束，敌方行动一次。**训练师轮战不可捕捉**：对训练师投球会被拒绝，只追加一条提示日志，**精灵球不消耗、也不消耗回合** |

#### `List<String> tryRun()`

尝试逃跑。成功率 = `0.35 + 0.6 × (我方速度 / (我方速度 + 敌方速度))`，
速度越接近成功率越低（约 0.65），远超时接近 0.95。

| 项     | 说明 |
|--------|------|
| 返回   | 本回合新增日志行 |
| 副作用 | 成功 → `FLED`；失败 → 敌方行动一次。**训练师轮战不可逃跑**：只追加「无法逃跑」提示日志，不消耗回合 |

#### `List<String> switchActive(int partyIndex)`

消耗本回合行动切换出战精灵，收换完成后敌方行动一次。

| 项     | 说明 |
|--------|------|
| 参数   | `partyIndex`：玩家队伍中目标精灵下标 |
| 返回   | 本回合新增日志行 |
| 副作用 | 目标与当前相同 / 下标非法 / 目标不存在时不行动（不消耗回合） |

### 3.3 状态查询方法

| 方法签名 | 返回 | 说明 |
|----------|------|------|
| `Player getPlayer()` | 玩家 | 本场战斗的玩家（含队伍与背包） |
| `Pokemon getWild()` | 野生精灵 | 本场战斗的野生精灵；**训练师轮战中为 `null`** |
| `Trainer getTrainer()` | 训练师 | 本场战斗的训练师；**野生遭遇中为 `null`** |
| `Pokemon foeActive()` | 敌方出战精灵 | 野生遭遇为野生精灵，训练师轮战为训练师当前出战精灵；界面应统一用它渲染敌方面板 |
| `Pokemon playerActive()` | 出战精灵 | 玩家当前出战精灵；倒下被引擎自动切换后可能为 `null` |
| `Status getStatus()` | 战斗状态 | 见 3.1 |
| `boolean isOngoing()` | 是否进行中 | 等价 `getStatus() == Status.ONGOING` |
| `List<String> getLog()` | 日志 | 完整战斗日志（只读，不可修改） |
| `Bag getBag()` | 背包 | 便捷转发，等价 `getPlayer().getBag()` |
| `Weather getWeather()` | 天气 | 当前天气；无天气为 `Weather.NONE`，由天气类变化技能开启（见 §7） |
| `Terrain getTerrain()` | 场地 | 当前场地；无场地为 `Terrain.NONE`，由场地类变化技能开启（见 §7） |

### 3.4 训练师轮战（敌方持有一整支队伍）

敌方由 `org.example.model.Trainer` 描述（队伍上限 6 只，与 `Player` 同一套队伍
契约：首个加入者自动成为出战精灵、倒下后自动切换下一只健康精灵）。判定与野生遭遇的区别：

| 项 | 野生遭遇 | 训练师轮战 |
|----|----------|------------|
| 敌方来源 | `getWild()` | `getTrainer()`（`getWild()` 为 `null`） |
| 当前敌方精灵 | `foeActive()` = 野生精灵 | `foeActive()` = `trainer.getActive()` |
| 结束条件 | **野生精灵倒下**即 `PLAYER_WIN` | **敌方整队精灵全部倒下**才 `PLAYER_WIN` |
| 玩家战败 | 己方整队全部倒下且无精灵可替换 → `PLAYER_LOSE` | 同左 |
| 敌方倒下后 | 战斗结束 | 自动派出下一只健康精灵（训练师不会主动换宠，换宠当回合新精灵不行动） |
| 逃跑 | 可尝试 | 不可（`tryRun()` 只记日志，不消耗回合） |
| 捕捉 | 可投球捕捉 | 不可（投球不消耗道具、不消耗回合） |
| 天气/场地、技能 PP | 整场持续 | 整场持续（换宠不重置） |
| 胜利经验 | 按野生精灵折算 | 按**整队被击败对手**一次性结算（`Σ` 各只经验） |

> 判定顺序：同一回合敌方出战精灵倒下时，先由训练师派出下一只；派不出（整队全倒）则立即
> `PLAYER_WIN`，随后才处理玩家精灵倒下后的自动换宠/`PLAYER_LOSE`。

## 4. 工厂：`BattleServices`

全限定名：`org.example.battle.BattleServices`（`final`，私有构造，禁止实例化）

| 方法签名 | 返回 | 说明 |
|----------|------|------|
| `BattleService newBattle(Player player, Pokemon wild)` | 战斗实例 | 创建默认随机源的新对战；玩家无存活出战精灵或野生无效时抛 `IllegalArgumentException`，参数为 `null` 抛 `NullPointerException` |
| `BattleService newBattle(Player player, Pokemon wild, Random random)` | 战斗实例 | 同上有随机源版本（便于测试复现） |
| `BattleService newTrainerBattle(Player player, Trainer trainer)` | 战斗实例 | 创建**训练师轮战**（默认随机源）：不可逃跑/捕捉，直到某一方精灵全部倒下才结束（见 §3.4）；玩家无存活出战精灵或训练师队伍无健康精灵时抛 `IllegalArgumentException`，参数为 `null` 抛 `NullPointerException` |
| `BattleService newTrainerBattle(Player player, Trainer trainer, Random random)` | 战斗实例 | 同上，带随机源版本（便于测试复现） |
| `int wildLevelAround(int playerLevel)` | 建议等级 | 野生等级在 `[playerLevel-2, playerLevel+2]` 内随机，最低 2 |
| `Optional<Pokemon> randomWild(int aroundLevel)` | 野生精灵 | 从数据注册表（`GameData#wildPool()`）随机挑选；无野生池数据返回 `Optional.empty()` |

## 5. 使用示例

```java
// 1) 组装玩家（队伍 + 背包），进入随机遭遇
Player player = ...;                       // 需有存活出战精灵
int level = BattleServices.wildLevelAround(player.getActive().getLevel());
Optional<Pokemon> wild = BattleServices.randomWild(level);
if (wild.isEmpty()) return;

// 2) 创建战斗服务（返回的是接口类型）
BattleService battle = BattleServices.newBattle(player, wild.get());

// 3) 回合驱动：进行中则让玩家选择一个行动
while (battle.isOngoing()) {
    List<String> lines = battle.useMove(currentMoveSlot); // 技能 / useItem / tryRun / switchActive
    for (String line : lines) logView.append(line);
}

// 4) 依据结果状态收尾
switch (battle.getStatus()) {
    case PLAYER_WIN -> onWin(battle.getPlayer());
    case PLAYER_LOSE -> onLose();
    case FLED       -> backToMap();
    case CAUGHT     -> onCaught(battle.getWild());
    case ONGOING    -> { /* 不应出现 */ }
}
```

训练师轮战（敌方整队，某一方全灭才结束）：

```java
// 训练师队伍（可从 GameData/流程系统按节点难度组装）
Trainer trainer = new Trainer("路人训练师");
trainer.addPokemon(level20Mon);
trainer.addPokemon(level22Mon);

BattleService battle = BattleServices.newTrainerBattle(player, trainer);

// 界面渲染统一取 foeActive()：换宠后自动跟随
render(battle.playerActive(), battle.foeActive());

while (battle.isOngoing()) {
    // 训练师战无逃跑/捕捉：tryRun()、对训练师投球只返回提示日志且不消耗回合
    battle.useMove(slot);
    render(battle.playerActive(), battle.foeActive());
}
// battle.getTrainer().isPartyAllFainted() == true 时为 PLAYER_WIN
```

## 6. 回合驱动流程（状态机）

```
        ┌─────────────────────────────────────────────────────┐
        │  ONGOING  (玩家每回合选择 技能/道具/逃跑/换宠)       │
        │  ┌───────────────────────────────────────────────┐  │
        │  │ 引擎内部结算：                                 │  │
        │  │   先后手 → 伤害 → 胜负 → 经验/升级/进化        │  │
        │  │   回合末：结算天气/场地效果并推进剩余回合       │  │
        │  └───────────────────────────────────────────────┘  │
        └──────────┬──────────────┬───────────────┬──────────┘
                   ▼              ▼               ▼
              PLAYER_WIN     PLAYER_LOSE     FLED / CAUGHT
```

- 行动期间精灵倒下会被引擎自动切换下一只健康精灵；
- **训练师轮战**：敌方当前出战精灵倒下后自动派下一只健康精灵（换宠当回合新精灵不行动），
  敌方整队精灵全部倒下才 `PLAYER_WIN`；玩家整队全倒才 `PLAYER_LOSE`；不可逃跑/捕捉（§3.4）；
- 胜利后向队伍中未倒下精灵发放全额经验，逐级结算升到级学招与进化
  （训练师轮战按整队被击败对手一次性结算）；
- 每回合行动结束后（双方仍站场时）引擎结算天气/场地回合末效果，并推进
  天气/场地剩余回合（见 §7）。

## 7. 天气与场地

战斗双方共享同一片天气（`Weather`）与场地（`Terrain`），由携带对应效果的
**变化类技能**开启，持续固定回合后自然消退，持续期间影响招式威力和回合末效果。

### 7.1 开启与持续

- **开启方式**：使用带效果的变化类技能（如「大晴天」「青草场地」）。普通物理/特殊
  技能不影响天气与场地。效果枚举 `MoveEffect`（`org.example.model`）把技能映射到
  对应 `Weather` / `Terrain`。
- **持续回合**：`Weather.DURATION_TURNS` / `Terrain.DURATION_TURNS` 均为 5 回合
  （含开启当回合）。每回合末引擎推进计数，归零后天气/场地消退并写入结束消息
  （如「沙暴平息了。」「青草场地的效果消失了。」）。
- **覆盖与重置**：再次开启同类天气/场地会重置剩余回合并提示「持续回合重置了！」；
  开启不同类的天气/场地则替换前一类。天气与场地相互独立，可同时生效。

### 7.2 效果明细

| 状态 | 开启技能效果（`MoveEffect`） | 招式威力倍率 | 回合末效果 |
|------|------------------------------|--------------|------------|
| `Weather.SUNNY` 大晴天 | `SUNNY_DAY` | 火系 ×1.5 | — |
| `Weather.RAIN` 下雨 | `RAIN_DANCE` | 水系 ×1.5 | — |
| `Weather.SANDSTORM` 沙暴 | `SANDSTORM` | — | 非岩石/地面系扣最大 HP 的 1/16 |
| `Weather.HAIL` 冰雹 | `HAIL` | — | 非冰系扣最大 HP 的 1/16 |
| `Terrain.ELECTRIC` 电气场地 | `ELECTRIC_TERRAIN` | 电系 ×1.3 | — |
| `Terrain.GRASSY` 青草场地 | `GRASSY_TERRAIN` | 草系 ×1.3 | 场上非飞行系精灵回复最大 HP 的 1/16 |
| `Terrain.MISTY` 薄雾场地 | `MISTY_TERRAIN` | 龙系 ×0.5 | — |
| `Terrain.PSYCHIC` 精神场地 | `PSYCHIC_TERRAIN` | 超能力系 ×1.3 | — |

规则细节：

- 天气与场地的倍率**可叠加**（伤害计算时按 天气倍率 × 场地倍率 连乘）；
- 回合末效果（沙暴/冰雹扣血、青草回复）仅在**双方都仍站场**时结算；满血精灵不会
  产生回血日志；免疫精灵不产生扣血日志；
- 变化类技能同样消耗 PP 与整回合行动，规则与伤害技能一致（只是不造成伤害）；
- 战斗结束（胜负/逃跑/捕获）后天气/场地不残留到下一场。

### 7.3 界面展示

UI 层通过 `getWeather()` / `getTerrain()` 在每次行动后刷新状态条，展示
`Weather.getDisplayName()` / `Terrain.getDisplayName()`（如「大晴天」「青草场地」）。

## 8. 实现与扩展约束

- 唯一实现 `BattleEngine`（同包），构造时校验玩家出战精灵与敌方（野生精灵或训练师出战精灵）均有效；
- 调用方应通过 `BattleServices` 获取实例，禁止 `new BattleEngine(...)`；
- 若需新增自定义实现，实现 `BattleService` 即可，UI 层无需改动；
- 界面渲染敌方面板一律取 `foeActive()`，不要直接读 `getWild()`（训练师轮战中为 `null`）。

### 8.1 `Move` 数据契约对齐（v1.2）

为适配「宝可梦、技能、队伍接口.md」（v1.0，待组长评审），`org.example.model.Move`
数据契约调整如下（本次仅数据模型对齐，不改变引擎行为）：

| 字段 | 语义 |
|------|------|
| `priority`（新增） | 先制度，数值越大越先出手；0 为普通技能。`getPriority()` 读取。当前引擎按速度先后手，暂未启用先制排序 |
| `accuracy` | 命中率 0~100；**-1 表示必中**（负值在构造时归一为 -1，正值 clamp 至 100）。当前引擎恒命中，未做命中判定 |

构造重载保持向后兼容：原 7/8 参构造委托至新完整构造（`priority=0`），
既有 `GameData` 技能注册代码无需改动。

## 9. 战斗模型契约对齐（v1.3）

按《宝可梦、技能、队伍接口.md》v1.0 对战斗相关模型做了**温和适配**（不破坏既有可运行
语义），新增的契约类型与既有模型补齐如下，详情与取舍见
《适配说明_宝可梦技能队伍接口v1.md》：

### 9.1 新增契约类型（`org.example.model` / `util` / `service`）

| 类型 | 说明 |
|------|------|
| `Nature` | 性格：20 预定义常量，`getModifier` 提供 ±10% 修正 |
| `BaseStats` | 种族值六维（含 `from(Stats)`/`getTotal()`） |
| `StatusCondition` | 异常状态枚举（NONE/POISON/PARALYSIS/BURN/SLEEP/FREEZE/FAINTED） |
| `StatModifier` | 六能力修正枚举（供性格使用） |
| `LearnableMove` | 可学习技能条目（moveId + level） |
| `GrowthEvent` | 成长事件（LEVEL_UP / LEARN_MOVE / EVOLUTION） |
| `PokemonState` | 存档快照（Player + timestamp） |
| `util.ExperienceCalculator` | medium-fast 经验曲线（`expToLevel`/`expToNextLevel`） |
| `service.PokemonService` | 接口：`getInitialPool()`（I-01 初始池） |

### 9.2 既有战斗相关模型补齐（兼容签名）

- `Move`：`isPhysical()` / `isSpecial()`（配合既有 `priority`/`accuracy`）；
- `MoveSlot`：PP 契约方法 `getCurrentPp/getMaxPp/isExhausted/usePp/restorePp/fullRestore`；
- `Stats`：`static randomIv()`（0~31）与六项 `getXxxIv()` IV 别名；
- `Species`：`getLearnableMoves()/addLearnableMove`（出生技∪习得表）；
- `Player`：`isPartyFull/addPokemon/switchActive/getActiveIndex/getHealthyPokemon/isPartyAllFainted`；
- `Pokemon`：新增 `uuid`/`ivs`/`nature`/`status` 字段与
  `getUuid/getIvs/getNature/getStatus/hasMove(String)/canLearnMove/gainExp/fullHeal`。

### 9.3 与战斗引擎的关系

- 上述字段/方法已通过编译与 SmokeTest（31/31），**BattleEngine 行为不变**；
- 异常状态、性格修正、IV 差异当前仅作为个体数据维护，**尚未接入战斗结算**，
  待后续战斗模块按需启用；
- 天气与场地系统（§7）不受本次适配影响。


