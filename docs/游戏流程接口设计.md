# 游戏流程（Roguelike 流程与经济）接口文档

> 版本：v0.1 · 日期：2026-09-09 · 模块：流程与经济（Roguelike Run）
> 角色：开发 C（流程与经济） · 状态：**待组长评审**
> 依据：《README》§「游戏流程与节点设计」「特殊事件进阶规则」「失败与惩罚规则」
> 依赖方：UI / 主循环、战斗模块、养成（抓捕/合并/性格）、存档模块
> 协作约定：本模块为**接口先行**设计，实现类集中于 `com.bao01.flow` 包，未实现前不阻塞其它模块并行开发。

---

## 1. 概述

### 1.1 模块定位

游戏流程模块负责 **一次 Run 从出发到结算的完整推进**，是连接「战斗 / 养成 / 经济」的主控逻辑。核心职责：

- **分段路线与行动点（AP）记账**：按「段（Segment）」推进路线，每段拥有固定上限的行动点，玩家在额度内自主选择节点；
- **节点生成与进入**：常驻节点固定出现、随机节点按概率出现、特殊事件节点带进度/次数限制；
- **战斗编排与结算**：为战斗节点构造敌人队伍并交给战斗模块，战斗结束后按节点类型执行奖励 / 惩罚 / 濒死 / 自回血结算；
- **经济系统**：金币的获得（路人、火箭队）与支出（商店、失败罚金）；
- **剧情线状态机**：神兽偶遇、火箭队剧情线、首领侵略事件的状态流转与「每 Run 一次」限制；
- **Run 结束判定**：胜利通关（普通 / 真结局）、失败结束、中途失败重试等。
- **抓捕记账**：登记每次成功捕获的物种与次数（驱动养成模块的「努力值成长」，见《README》特色机制）。

### 1.2 对外提供的服务

| 服务 | 说明 | 使用方 |
|------|------|--------|
| Run 生命周期 | 开局（携带初始队伍）、逐段推进、结局判定 | UI、主循环 |
| 路线/节点视图 | 当前段可选节点、AP 余额、段号 | UI（路线地图） |
| 节点结算 | 战斗结果 → 金币/道具/惩罚/治疗/推进 | UI、战斗模块联动 |
| 商店与经济 | 商品列表、购买、金币变动 | UI |
| 剧情线状态查询 | 神兽/火箭队/侵略事件可触发性 | UI、事件节点 |
| 抓捕记账 | 物种→捕获次数（供养成模块读取） | 养成模块 |
| 存档协同 | 提供 Run 快照编解码（建议并入存档系统） | 存档模块 |

### 1.3 设计约束

- 纯 JDK 17 实现，**不引入 JSON 等第三方序列化依赖**，不依赖任何 JavaFX 类型，可独立测试。
- 随机性全部经由注入的随机源（`RandomSource`）驱动，便于测试确定性与复现。
- 战斗细节（出手顺序、伤害、天气、异常）**不属于本模块**：本模块只负责构造敌方队伍、调用战斗、按结果结算。
- 数值（AP 上限、金币数额、商店规模、敌人等级曲线等）集中放 `FlowConfig`，标注 `[待配置]` 的项待组长评审后定值。
- 包位置：`com.bao01.flow`（建议实现文件：`FlowData.java`、`FlowConfig.java`、`FlowController.java`）。

---

## 2. 数据模型（Model）

> 数据模型集中在 `com.bao01.flow.FlowData` 内；不可变 `record` / `enum`，可变状态由 `FlowController` 持有。

### 2.1 FlowData（流程数据容器）

```java
package com.bao01.flow;

public final class FlowData {
    private FlowData() { }   // 纯容器，禁止实例化
}
```

### 2.2 NodeType（节点类型）

```java
/** 节点类别：决定结算方式。 */
public enum Cat { COMBAT, SERVICE, STORY, MILESTONE }

public enum NodeType {
    TRAINER    ("路人",          Cat.COMBAT),     // 常驻；胜得金币，败扣金币
    WILD       ("野外精灵",       Cat.COMBAT),     // 常驻；可捕获；败扣金币
    ROCKET     ("火箭队",         Cat.COMBAT),     // 随机；不可失败，败即结束 Run
    LEGEND     ("神兽偶遇",       Cat.COMBAT),     // 随机+进度限制；每 Run 一次；败扣金币
    HOSPITAL   ("医院",          Cat.SERVICE),    // 常驻；治疗濒死/受伤
    SHOP       ("商店",          Cat.SERVICE),    // 随机；购买道具/回复品/技能机（[待配置]）
    ROCKET_BOSS("火箭队抓捕神兽", Cat.STORY),      // 后期剧情事件；可自选进入；首领战不可失败
    GYM        ("道馆战",         Cat.MILESTONE),  // 必然节点；AP 耗尽触发；可失败一次
    ELITE_FOUR ("四天王连打",     Cat.MILESTONE),  // 路线终点前必然触发；可失败一次
    CHAMPION   ("Boss 冠军战",    Cat.MILESTONE),  // 路线终点必然触发；不可失败
    INVASION   ("首领侵略战",     Cat.MILESTONE);  // 条件必然触发（见 §6）；不可失败

    private final String label;
    private final Cat cat;
    public String label();   // 中文展示名
    public Cat cat();
}
```

> 说明：`NodeType` 只表达「种类」，**不表达行动点消耗**。AP 消耗与“随机为 0 / 必然 0”等修饰，由 `RouteOption` 在实例级表达（见 2.4），避免枚举内写死分支规则。

### 2.3 StoryFlags（剧情线状态 · 每 Run）

```java
public record StoryFlags(
        boolean rocketEntered,          // 是否进入过任意一次火箭队节点（开启后期抓捕线）
        boolean legendaryEncountered,   // 神兽偶遇是否已发生（每 Run 限一次）
        boolean masterBallHeld,         // 是否已获得大师球（火箭队首领战后）
        boolean rocketBossDefeated,     // 抓捕事件首领是否已被击败（击败后不再触发侵略战）
        boolean championDefeated,       // 冠军是否已击败（触发侵略战的时点）
        boolean invasionDone) { }       // 首领侵略战是否已结束
```

### 2.4 RouteOption（当前段的一个可选节点实例）

```java
public record RouteOption(
        NodeType type,      // 节点种类
        String display,     // 展示文案（如「路人 小刚」「野生的 焰尾狐」）
        int apCost,         // 进入需消耗 AP（可被规则改为 0）
        boolean forced) { } // true=必入（如 GYM/四天王/冠军/侵略/大师球触发的神兽）
```

### 2.5 CaptureLedger（抓捕记账）

```java
public record CaptureLedger(Map<String, Integer> catchesBySpecies) {
    public static CaptureLedger empty();
    public void record(String species);       // 成功捕获后 +1
    public int countOf(String species);       // 该物种累计捕获次数
}
```

> 本模块只负责**记账**；「次数 → 努力值成长」「同类合并」「性格选择」为养成模块逻辑，通过 2.6 的只读视图消费。

### 2.6 RunSummary（对外只读快照 · 供 UI / 存档 / 养成）

```java
public record RunSummary(
        int segmentNo,          // 当前段号（从 1 开始）
        int apLeft,             // 剩余 AP
        int apCap,              // 本段 AP 上限
        int gold,               // 当前金币
        StoryFlags story,       // 剧情线状态
        CaptureLedger ledger,   // 抓捕计数
        List<RouteOption> options,  // 当前可选节点
        NodeType nextMilestone) { } // 下一个必然节点（GYM/ELITE_FOUR/CHAMPION/INVASION）
```

### 2.7 Ending（结局）

```java
public enum Ending {
    RUN_OVER,      // Run 失败结束（含不可失败战斗战败、道馆/四天王二败）
    NORMAL_CLEAR,  // 普通通关（未开火箭队线：击败冠军即通关）
    TRUE_CLEAR     // 真结局（开线且未提前击败首领：冠军后再过首领侵略战）
}
```

---

## 3. 服务接口（Service）

### 3.1 FlowConfig（流程配置 · 数值集中）

```java
public final class FlowConfig {

    // ===== AP（每段上限随段号提升，数值 [待配置]）=====
    public static int segmentApCap(int segmentNo);

    // ===== AP 消耗（默认值，可由节点实例/规则改写为 0）=====
    public static int apCost(NodeType type);   // 路人2 / 野外1 / 医院1 / 商店1 / 火箭队2 / 神兽1 / 其余0

    // ===== 概率 [待配置] =====
    public static double shopSpawnChance(int segmentNo);   // 商店随机出现，随进度升高
    public static double rocketSpawnChance();              // 火箭队随机节点概率
    public static double wildZeroApChance();               // 野外偶遇消耗为 0 的概率
    public static double wildEncounterChance();            // 每可选池中野外节点出现概率

    // ===== 经济 [待配置] =====
    public static int trainerReward();                     // 路人胜利金币
    public static int rocketReward();                      // 火箭队胜利金币（大量）
    public static double rocketItemDropChance();           // 火箭队特殊道具掉落概率
    public static int gymFailPenalty();                    // 道馆战失败扣金币（大量）
    public static int eliteFailPenalty();                  // 四天王失败扣金币（大量）
    public static int trainerFailPenalty();                // 路人/野外/神兽失败扣金币

    // ===== 成长曲线 [待配置] =====
    public static int enemyLevel(int segmentNo);           // 敌人基础等级
    public static int shopSize(int segmentNo);             // 商店商品数量
    public static int inventorySeedGold();                 // 初始金币
    public static int hospitalCost();                      // 医院是否收费（默认 0）[待确认]
}
```

### 3.2 RandomSource（随机源 · 可注入测试）

```java
public interface RandomSource {
    int nextInt(int bound);
    double nextDouble();
    boolean chance(double p);
    static RandomSource system();   // ThreadLocalRandom 实现
}
```

### 3.3 BattleAdapter（战斗桥 · 由战斗模块对接实现）

> 职责边界：流程模块不直接操作 `BattleController` 的内部状态，只通过本桥发起战斗并取回结果。

```java
public interface BattleAdapter {
    /**
     * 开局一场流程战斗。kind 决定是否可逃跑（WILD 可逃）。
     * 返回开局日志；对局本身由 UI 通过战斗模块继续驱动。
     */
    List<String> startBattle(Team playerTeam, Team foeTeam,
                             com.bao01.model.Battle.Opponent kind);

    /** 当前正在进行的对局（供 UI 驱动与结算）。 */
    com.bao01.model.Battle currentBattle();

    /** 取回结局（IN_PROGRESS / PLAYER_WON / FOE_WON / PLAYER_FLED）。 */
    com.bao01.model.Battle.Outcome outcome();
}
```

> 注：敌方 `Team` 的构造（按段等级、按节点类型选物种）由流程模块负责，见 3.5。

### 3.4 FlowController（流程门面）

> 采用**实例有状态**控制器（类比 `BattleController`），由主循环/UI 持有。方法返回 `List<String>` 叙事日志，便于 JavaFX 视图直接展示。

```java
public final class FlowController {

    /** 开局：携带初始队伍进入第 1 段，重置 AP，生成本段可选节点池。 */
    public static FlowController startRun(Team starter, FlowConfig cfg, RandomSource rnd, BattleAdapter battle);

    // ============ 状态查询 ============
    public RunSummary summary();
    public int gold();
    public int apLeft();
    public int segmentNo();
    public StoryFlags story();
    public CaptureLedger ledger();
    public boolean isOver();
    public Ending ending();

    // ============ 节点选择 ============
    /** 返回本段当前可选节点（常驻 + 随机 + 剧情可选项），按序可进入。 */
    public List<RouteOption> options();

    /** 进入可选节点（下标对应 options()）：扣 AP → 执行该节点逻辑。 */
    public List<String> enter(int optionIndex);

    // ============ 节点逻辑 ============
    /** 医院：治疗全部濒死/受伤成员。 */
    public List<String> visitHospital();

    /** 商店：返回本段商品（随段号变多）。 */
    public List<ShopOffer> shopStock();

    /** 购买下标商品；金币不足返回提示日志。 */
    public List<String> buy(int shopIndex);

    /** 构造某 COMBAT 节点的敌方队伍（等级=FlowConfig.enemyLevel(segmentNo) 附近）。 */
    public com.bao01.model.Team buildFoeTeam(NodeType type);

    // ============ 战斗结算 ============
    /** 战斗结束后由 UI 调用：按节点类型执行奖惩、濒死处理、自回血、剧情线推进、必然节点触发。 */
    public List<String> settleFight(NodeType fought, com.bao01.model.Battle.Outcome outcome);

    /** 捕获尝试：对目标（战败的野生/神兽）执行捕获判定（结合捕获率配置 [待配置]）。 */
    public List<String> attemptCatch(com.bao01.model.Pokemon target);

    // ============ 存档协同 ============
    /** Run 快照 → 文本（建议随 SaveData.World 一并落盘，见 §7）。 */
    public String toText();
    /** 恢复 Run 快照（需与战斗/世界存档顺序配合，见 §7）。 */
    public static FlowController restored(RunSummary snapshot, ...);
}
```

### 3.5 ShopOffer（商店商品）

```java
public record ShopOffer(
        String itemName,   // 道具名（Items.byName 解析）
        int price,         // 售价（金币）
        String description) { }
```

---

## 4. 节点结算规则（流程主状态机）

> 由 `settleFight` / `enter` 统一驱动，规则出处：《README》「行动点机制 / 失败与惩罚规则 / 濒死恢复规则 / 节点自回血」。

### 4.1 AP 消耗（表）

| 节点 / 行为 | AP | 备注 |
|---|---|---|
| 野外精灵（偶遇） | 1 | 有概率消耗为 0（`wildZeroApChance`） |
| 路人 | 2 | |
| 医院 | 1 | |
| 商店 | 1 | 随机出现，规模随进度增长 |
| 神兽偶遇（正常触发） | 1 | 仅后期、每 Run 一次 |
| 神兽偶遇（火箭队线必然触发） | 0 | 大师球获得后必然出现、强制进入 |
| 火箭队节点 | 2 | 与路人一致 |
| 火箭队抓捕神兽事件 | 0 | 后期剧情选项（[待确认] 是否需 AP） |
| 道馆 / 四天王 / Boss / 侵略 | 0 | 必然触发 |

- 段开始时 AP 重置为该段上限（`segmentApCap(segmentNo)`）。
- **AP 耗尽** → 无可进入的可选节点 → 强制触发 `GYM` → 通过后进入下一段。
- 行动点不足以进入任何剩余节点且未耗尽？——视为不可再进，直接进入必然节点（实现细节：`enter` 拒绝 apCost > apLeft 的选项）。

### 4.2 战斗失败惩罚（表）

| 战斗类型 | 败后处理 | 是否中断 Run |
|---|---|---|
| 路人 / 野外精灵 / 神兽战 | 扣金币 `trainerFailPenalty` | 否，可继续 |
| 火箭队战 / 抓捕事件首领战 / 首领侵略战 | **不可失败** | 是（`RUN_OVER`） |
| 道馆战 / 四天王连打 | 允许失败一次：扣大量金币；**再次失败**结束 Run | 仅二败时 |
| Boss 冠军战 | 不可失败 | 是（`RUN_OVER`） |

### 4.3 濒死与自回血

- 战斗结算时：我方濒死（HP=0）成员**不恢复**；只能靠医院或道具（复活药等）治疗。
- **每通过一个节点后**：队伍中未濒死成员自动恢复 `maxHp / 5`（整数除法向下取整，最小 1，向上取整到 1 便于观测 [待确认]）；濒死成员不享受。
- 医院节点：治疗全部成员（含濒死）至满血（是否收费 `hospitalCost`，默认 0）。

### 4.4 抓捕流程

```
战斗胜利（野外/神兽战，目标未被我方行动 KO 而是被捕获状态）
  → UI 调 attemptCatch(target)
    → 捕获判定（概率 [待配置]）
      → 成功：ledger.record(物种)；加入队伍/触发同类合并/性格选择（调用养成模块，接口见 §8）
      → 失败：目标逃跑或继续（规则 [待确认]，默认仅一次判定，失败即该个体不可再抓）
```

---

## 5. 一段路线的典型推进（伪代码）

```
startRun(starterTeam)
  loop:
    options = generateOptions(segmentNo, story)     // 常驻 3 类 + 随机商店/火箭队/神兽(受次数/进度限制)
    while 有可进入的可选节点:
      ui 选择 node
      enter(idx):
        apLeft -= apCost
        if COMBAT → buildFoeTeam + battle.startBattle(...)   // 交由战斗模块
        if HOSPITAL → visitHospital
        if SHOP → 展示 shopStock / buy
        settleFight(后置结算):
          自回血（非必然节点）
          剧情线推进（§6）
          若 apLeft == 0 → 强制 GYM（或段终点强制 四天王/冠军）
    if 本段必然节点已过 → segmentNo++，重置 AP，敌人等级抬升
```

---

## 6. 剧情线状态机（神兽 / 火箭队 / 侵略）

> 规则出处：《README》「特殊事件进阶规则：神兽与火箭队」「一次 Run 的典型节奏」。

### 6.1 触发条件汇总

| 节点/事件 | 出现条件 |
|---|---|
| 神兽偶遇 | 仅后期段；`!legendaryEncountered`（每 Run 一次） |
| 火箭队节点 | 各时期随机 |
| 「火箭队抓捕神兽」剧情事件 | `rocketEntered` 且后期段可选；可自选进入 |
| 抓捕事件首领战 | 进入上述事件后触发；**不可失败**；胜 → `masterBallHeld=true`、`rocketBossDefeated=true` |
| 神兽偶遇（必然） | `masterBallHeld` 时强制出现一次（`forced=true`，AP=0） |
| 四天王连打 | Boss 战前必然（可失败一次） |
| Boss 冠军战 | 路线终点必然（不可失败） |
| 首领侵略战 | `championDefeated && rocketEntered && !rocketBossDefeated`（**不可失败**） |

### 6.2 结局判定

```text
击败冠军后：
  若 rocketEntered == false        → NORMAL_CLEAR（无抓捕/侵略事件）
  若 rocketEntered && bossDefeated → NORMAL_CLEAR（首领已提前击败，无侵略）
  若 rocketEntered && !bossDefeated→ 触发 INVASION
      击败侵略首领 → TRUE_CLEAR
      战败         → RUN_OVER
```

---

## 7. 存档协同（扩展点 · 建议）

- 现状：《存档接口设计.md》的 `SaveData.World` 含 `playerTeam + battle`，**不含 Run 推进状态**。
- 建议：`World` 追加可选字段（如 `RunSummary run`），随存档文本扩行（`RUN_SEG/AP/GOLD/STORY/...`），`FORMAT_VERSION` 递增到 2；旧档缺省 `run=null`（读档后由主循环决定是否从段首重启）。
- 恢复顺序：`playerTeam.restore()` → `battle` 存在则 `Battle.resumed(...)` → `run` 存在则重建 `FlowController` 并校验与队伍一致（如段号、金币非负）。
- 战斗中读档：`run + battle` 双快照同时恢复；结算仍走 `settleFight`。

---

## 8. 与其它模块的接口规约（依赖/提供）

| 模块 | 本模块依赖其 | 本模块向其提供 |
|---|---|---|
| 战斗（开发 A） | `Battle`/`BattleController` 开局、`Battle.Outcome` 结局；`BattleAdapter` 实现 | 敌方 `Team` 构造、节点类型与结算输入 |
| 队伍/养成（开发 B） | 队伍治疗/濒死查询（`Team`/`Pokemon`）；抓捕合并/性格选择入口（`[待配置] CaptureService`） | `CaptureLedger` 捕获计数视图；队伍携带状态 |
| 配置（开发 A/B） | `SamplePokemon`、`Items` 注册表（按名构造/解析） | 节点生成参数、商店内容来源 |
| 存档（已设计） | `SaveData`/`SaveManager` 编解码约定 | `RunSummary` 快照文本（§7 扩展点） |
| UI / 主循环（开发 D） | — | `FlowController` 门面、`RunSummary` 只读视图 |

---

## 9. 健壮性、扩展与验证

### 9.1 健壮性

| 场景 | 行为 |
|---|---|
| `enter` 选项 AP 不足 | 拒绝并返回提示，不扣 AP |
| AP=0 且仍有可选节点 | 不再允许进入，强制进入必然节点 |
| 金币为负（失败罚金超额） | 扣至 0，不产生负余额 |
| 商店金币不足 | 拒绝购买 |
| 神兽偶遇重复触发 | 受 `legendaryEncountered` 每 Run 一次限制，不重复 |
| 已击败抓捕首领后 | 不再触发侵略战（`rocketBossDefeated` 短路） |
| 段末敌人等级 | `enemyLevel(segmentNo)` 单调递增，越界按末段处理 |
| 存档快照不一致 | 校验失败返回异常并拒绝恢复 |

### 9.2 向后兼容扩展

- 数值全部集中 `FlowConfig`；新增节点 = `NodeType` + 结算分支 + AP 表，不动其它模块。
- `NodeType` 的语义只增不改；`RouteOption` 承载实例级修饰（0 AP、强制）。
- 预留给未来的扩展点：道具掉落表、技能机商店、天气对流程的影响、难度选项——新增配置项即可。

### 9.3 验证

- 拟以无头主程序 `RunDemo`（类比 `SaveDemo`）串联「一次 Run 典型节奏」（《README》第 6 步）并打印各段推进与结局，作为回归基线；用例清单进入测试计划。
- 关键断言点：段号/AP 变化、金币收支、每节点自回血 1/5、濒死不恢复、道馆二败结束 Run、四天王失败一次不结束、不可失败战斗失败即 `RUN_OVER`、侵略战触发条件与两种通关结局。
