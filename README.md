# PokeRouge（实训项目）

> 本项目为 **Day01 工程骨架（v0.0.1-skeleton）**。选题、组员等个人信息尚未填写，均以 `[待确认]` 标注，定稿后删除标记。

## 一、项目简介

[待确认：一句话说明本项目做什么、面向谁、核心玩法/功能]

- 技术栈：JDK 17 + Maven + JavaFX 17（JavaFX 17.0.20，与 JDK 17 版本对齐）
- 界面层级：Stage → Scene → Pane → Node；数据层预留 model 包，采用"二维数组保存数据 → Rectangle 渲染"的映射思路
- 里程碑：首个 tag `v0.0.1-skeleton`（工程骨架）

## 二、运行方式

### 环境要求
| 工具 | 版本 | 说明 |
| --- | --- | --- |
| JDK | 17 | Project SDK 与 Module SDK 两处均需检查 |
| Maven | 3.9+ | 自带 Maven Wrapper 亦可 |
| JavaFX | 17.0.20 | 已通过 Maven 依赖引入，无需单独安装 |

### 构建
```bash
mvn clean compile
```

### 运行（JavaFX 从 JDK 11 起已剥离，**不能直接 `java -jar`**）
方式一（推荐，命令行）：
```bash
mvn javafx:run
```
方式二（IDEA）：
1. 打开 Maven 面板 → `untitled1` → `Plugins` → `javafx` → 双击 `javafx:run`；
2. 或以 Debug 方式启动该 Maven 目标进行断点调试（课程要求使用 Debug 而非 System.out.println）。

## 三、组员与角色

> 课程规则：组内分为 PM / 开发 / 测试，每两天轮换一次。

| 姓名 | 角色（首轮） | 职责 | 说明 |
| --- | --- | --- | --- |
| [待确认] | PM | 进度、日报、对外沟通 | [待确认] |
| [待确认] | 开发 | 架构与编码 | [待确认] |
| [待确认] | 测试 | 用例、执行、缺陷记录 | [待确认] |

## 四、分工计划

| 天 | 阶段 | 交付物 / 任务 | 负责人 |
| --- | --- | --- | --- |
| Day01 | 项目启动与工程环境 | 脚手架定稿、Git 初始化、README、日报 | 全员 |
| Day02 | 需求分析 | 需求文档、QA 问题表、测试用例草稿 | [待确认] |
| Day03 | 概要设计 | 架构与模块划分、界面原型 | [待确认] |
| Day04 | 详细设计 | 类/方法设计、数据映射设计 | [待确认] |
| Day05 | 编码（第一轮） | 最小可玩/可用版本，保证可运行 | [待确认] |
| Day06 | 编码（迭代） | 按优先级补功能 | [待确认] |
| Day07 | 单元/集成测试 | 用例执行与缺陷记录 | [待确认] |
| Day08 | 系统测试与修复 | 全流程回归、稳定性 | [待确认] |
| Day09 | 收尾 | 文档整理、答辩 PPT | [待确认] |
| Day10 | 交付答辩 | 演示、复盘 | 全员 |

## 五、Git 使用约定

- 分支模型：`main`（稳定，仅合入）→ `dev`（开发主干）→ `feature/*`（功能分支，命名如 `feature/score`）
- 新功能一律从 `dev` 切出 `feature/*`，完成后合回 `dev`，评审后再合入 `main`
- 提交信息规范：`<type>: <简述>`，type 取值 `feat / fix / docs / refactor / chore / test`
- 里程碑打 tag：当前 `v0.0.1-skeleton`；后续按阶段递增
- 禁止提交：`target/`、`.idea/`、`*.iml`、`logs/`（见 .gitignore）
- 全仓库统一 UTF-8，`.editorconfig` 已随仓库提交

## 六、AI 使用与核对说明

### 核对原则（来自课程）
1. **一次只让 AI 生成一类文件**，生成后立即用 `mvn clean compile` 验证；
2. 核对三点：**版本匹配**（JavaFX 与 JDK 17 对齐）、**最小性**（无多余依赖）、**可构建**；
3. AI 不确定处一律标注 `[待确认]`，人工核对定稿；
4. 功能交付必带单元测试并全量跑通 `mvn test`，关键判定分支人工抽查；
5. 实现与文档同步升级（需求/接口文档走版本记录机制）。

### 全周期重要 AI 使用与核对记录

> 完整的 AI 使用记录、人工核对结果与问题留痕见 **[docs/AI使用与核对说明.md](docs/AI使用与核对说明.md)**。
> 覆盖：工程骨架 → CSV 数据接入 → 技能链路修复 → 立绘加载 → 训练师轮战/异常状态 → 端口化架构收敛 → 肉鸽合并 → 图鉴弹窗与装备系统（当前 124 用例全绿）。
